# Design Coverage Manifest

Every concrete deliverable from `DESIGN.md`, mapped to the phase it lands in and its current status.

**Status legend**:
- ✅ **DONE** — fully implemented and verified
- 🟡 **PARTIAL** — data/scaffolding exists, behavior pending later phase
- ⏳ **PENDING** — not started, scheduled
- 💭 **OPEN** — open-ended ("many more X" — track count over time)

**Phase exit ritual** (what I MUST do before declaring a phase complete):
1. Read this file end-to-end.
2. For every row tagged with the current phase, verify status against the codebase.
3. If anything tagged for this phase is still PENDING or PARTIAL, fix it or explicitly defer with the user's approval before marking the phase complete.
4. Update statuses in this file whenever a row's state changes.

**Last full audit**: 2026-05-22 (**DESIGN.md sync pass — Treasury / alive-exit / cap-scaling table / class behaviours.** Updated DESIGN.md to match shipped reality: (1) Treasury row in the L3 unlocks table now describes the flat +5g daily stipend + the +1-adv-per-Treasury arrival-rate downside (the spec's "4 chests + alive-exit-required theft" was retired 2026-05-02 in the loot-pickup cleanup and is now strikethrough'd in the Top-level rules section). (2) Top-level "All chest/loot theft requires alive exit" rule strikethrough'd with a SCOPE_CHANGED note. (3) Cap-scaling table extended with a **Library** column (1/2/3/4 at L2/L4/L6/L8 — each Lib unlocks a deeper AdvIntel tier per the 2026-05-22 expansion); Barracks row marked as shipping at 1→9 instead of 1→5 (QoL deviation kept per user). Class behaviours re-audited and ALL 4 of Beast Master / Barbarian / Monk / Bard flipped from 🟡 → ✅ DONE — Barbarian `Unstoppable` is wired at `AISystem.js:2068`, Beast Master `bm_scout_ahead` ships in `ClassAbilitySystem.js`, Monk `Inner Peace` ships at line 39/575; the "armor_pierce" and "command_beast" items in my previous audit were obsolete (both removed per DESIGN.md line 170). Previous audit 2026-05-22 (**Doc-vs-code audit pass.** User flagged that several rows still tracking PENDING/PARTIAL had actually shipped silently or referenced removed scope. Walked every PENDING / PARTIAL / 🟡 row vs current code and flipped statuses; deviations now explicitly tagged. **Headline findings**: **Class behaviours** — Bard auras ✅ DONE; Barbarian rage scaling ✅; Monk dodge ✅ when Focus active (armor_pierce still pending); Beast Master tame+companion+defection ✅ (no explicit command_beast). **Room behaviours** — **13 rooms now ✅ DONE** that the doc had as ⏳/🟡: Crypt, Trap Factory (+5 slots), Watchtower (first-strike aura), Wandering Gate (60/35/5 teleport), Veil of Forgetting (door-connected intel scrub), Catacombs (death→T2 revenant), Wishing Well (heads/tails buff/Marked), False Exit (teleport-near-boss), Hall of Madness (ATTACK_ALLY goal), Sanctum (boss + minion regen aura), plus the Mimic Vault redesign and the RoomBehaviorSystem garrison spawner. Hall of Trials + Throne Room ship the core mechanic but their cap/tier scaling is partial. **Room-system rules** — Roster/Garrison split ✅ DONE (`Minion.class`), adjacency = direct shared door ✅ DONE (via `getNeighborRooms`), boss level cap = 10 ✅ DONE, cap-scaling-by-level table 🟡 PARTIAL (per-instance scaling exists but the unified table isn't wired), alive-exit-required-theft 🚫 SCOPE_CHANGED (loot pickup retired 2026-05-02; Treasury became stipend, Mimic Vault became chest-disguise — original rule no longer applies). **Phase 30 tileset system** — substantially ✅ DONE: TilesetEditor + RoomTileEditor scenes shipped + cheat-gated from MainMenu; FS Access API wrapper used by both editors; sprite library + manifest live in `assets/themes/`; DungeonRenderer paints per-cell from `tileLayout` via `themesprite-<id>` textures. Two original design rows marked 🚫 SCOPE_CHANGED: per-room `theme:` indirection (none use it — replaced by per-cell `tileLayout`) and random variant-pick (replaced by explicit authoring). Span rendering + tile mirror brushes still ⏳. **`build-action-sell-minion`** ✅ DONE — NightPhase's sell tool handles minion clicks. **Trap subsystem** — `trapTypes.json` has 8 traps + full system live (memory note about it being empty was stale; pre-dates the May rebuild). **`💭 More mechanics`** — target was ≥30; current pool **64** in `dungeonMechanics.json`. **Still genuinely outstanding** (the user-facing punch list): Monk armor_pierce, Barbarian `unstoppable` flag, Beast Master `command_beast` action, starter_corridor cap-scaling, starter_barracks 1→5 scaling, starter_guard_post behaviour, Armory/Library/Hall of Trials/Throne Room cap/tier scaling, room-tile-mirror brush, span-rendering, `emote-trigger-doors` (no locked-door event), 1.0 unified cap-scaling-by-boss-level table, Treasury "raises arrival rate" + "4 chests" sub-mechanics (or formal design re-think if dropped). Personality `decisionOverrides[]`/`reactions[]` fields remain unread by any system (still dead — `personality_revamp` deferred per user). Boss base stats remain identical across all 12 archetypes (still deferred per user). Previous audit 2026-05-21 (Section 35 — Lilith dialogue expansion, batch 5 (the deferred extras) shipped. Bank now **964 lines**. (1) **Streak** — `streak` category; the Director tracks consecutive full-party-wipe days (`_wipedToday` set on `KNOWLEDGE_PARTY_WIPED`, evaluated on `DAY_PHASE_ENDED`) and calls out milestones 3/5/8/12/20/30. (2) **Named-minion milestones** — `minion_milestone` category; the Director counts per-minion kills off `ADVENTURER_DIED.killerId` (`_minionKills` Map) and celebrates a *named* veteran by name at 5/12/25/40/60 kills. (3) **Per-archetype-ability reactions** — 8 `arch_*` categories wired to 9 signature-ability events (`GOLEM_EARTHQUAKE_FIRED`, `DEMON_SACRIFICE_FIRED`, `DEMON_HELLGATE_SPAWNED`, `BEHOLDER_PETRIFY_FIRED`, `VAMPIRE_CHARM_MARKED` + `SUCCUBUS_CHARM_APPLIED` → shared `arch_charm`, `GNOLL_HUNTERS_PACK_REFILLED`, `MYCONID_SPORE_DAY_BEGAN`, `WRAITH_HAUNT_SPAWNED`); high-frequency per-hit/per-tick archetype events deliberately excluded so she doesn't machine-gun. (4) **Event-choice reactions** — `gamble_won`/`gamble_lost` on the Gambler's Coin (`GAMBLER_COIN_FLIP` / `GAMBLER_DOUBLE_RESULT`); other night-modal events expose no choice-outcome event, and their announce is already covered. Two new `_buildCtx` token kinds (`count`, `minionMilestone`). Verified by isolated-component test: archetype reaction, gamble win/lose, named-minion counting to a milestone, wipe-streak tracking + `{count}` resolution. Section 35 is now fully complete — every row ✅ DONE. Previous audit 2026-05-21 (Section 35 — Lilith dialogue expansion shipped across 4 batches. Her bank grew from ~500 to **920 lines**. (1) **Specifics mechanism** — `npcLines.json` gained a keyed `specifics` bank; `NpcDirector` prefers a bespoke line for an entity id (`_specificId` resolves the key per domain) and falls back to the generic category. (2) **Batch 1** — 11 boss-archetype banks (drawn into idle musings) + 13 adventurer-class banks (on entry). (3) **Batch 2** — 26 dungeon-event banks + 20 room banks. (4) **Batch 3** — 19 minion-family banks (definitionId minus trailing digits) + 8 trap banks + 4 item banks; the generic `sold` split into `sold_room`/`sold_minion`/`sold_trap`/`sold_item` (non-sell removals with a `reason` skipped); a new `ROOM_MOVED` event emitted from NightPhase's move-drop drives `moved` commentary. (5) **Batch 4** — a build-suggestion advisor (`suggestion` bank, 16 game-state conditions via `_checkCond`, surfaced in night idle, includes playstyle observations); escalating inactivity nudges (`idle_nudge`, 3 tiers, `_lastActionAt` reset by any player action, fires after 70s night-phase idle); `combo` commentary on `PERSONALITY_COMBO_ACTIVATED`; `day_milestone` callouts; `last_life` tension lines. All 42 expressions in regular use. Each batch verified by isolated-component test (real modules + real npcLines.json + real EventBus). **Deferred** (noted on `npc-d-extras`): streak commentary, named-minion milestones, per-archetype-ability reactions, event-choice reactions — a possible future micro-batch. Previous audit 2026-05-21 (Section 35 — Lilith expansion shipped. All 5 expansion rows ✅ DONE. (1) **Intro** — she delivers the new-run intro herself: a 6-page bubble (self-intro + keeper role + game premise + a Yes/No tutorial-hints choice). `intro` block added to `npcLines.json`; `WelcomeIntroOverlay` emits `NPC_DELIVER_INTRO` instead of opening its modal when the companion is on; the bubble gained a choice-page mode; `NpcDirector._onChoice` sets `meta.introSeen`/`tutorialEnabled` + emits `INTRO_DISMISSED`. (2) **In-bubble hint opt-out** — tutorial bubbles carry a "turn off hints" link. (3) **Docked-beside-menu mode** — a generic `HUD_MENU_OPENED/CLOSED { kind }` signal added to the `Overlay` shell (opt-in via `npcKind`), tagged on all 8 info overlays; the companion gained a `data-dock` state that CSS-animates her from the corner peek to a full-body stance at the screen's left edge (z 8→160 to clear the z:150 overlay backdrop); 8 `menu_*` commentary categories + a 15s follow-up cadence. (4) **Pact broker** — `PactPicker.js` dropped its entire demon-broker sprite system (~150 lines: `BROKERS` roster, frame-players, speech bubble); broker dialogue now routes to Lilith via `NPC_BROKER_SAY`, and she docks beside the Grimoire. (5) Corner portrait bumped 202→236px. Verified via isolated-component test (real modules + EventBus): all 5 HUD/director modules import clean, intro choice flow → INTRO_DISMISSED, dock/undock on menu open/close, menu commentary, broker-line routing, hint opt-out. NOTE: full in-game visual layout still not screenshot-verified (preview tab runs backgrounded — game can't pass the font-gated Boot scene); user should do a final visual pass. Previous audit 2026-05-21 (Section 35 — Companion NPC "Lilith" shipped end-to-end. All 9 rows ✅ DONE. A persistent succubus companion in the HUD: a bottom-left peeking portrait + RPG chat bubble that reacts to ~30 EventBus events, fills silence with context-aware idle chatter, breaks the fourth wall, and delivers the tutorials herself as paged dialogue. Pieces: (1) `tools/bake-npc-sprites.mjs` downscales 42 source expression PNGs (1618×2400, 142.8MB) → `assets/npc/<id>.webp` (560×831, 3.2MB). (2) `src/data/npcLines.json` — 41 categories / 442 lines, data-driven, priority + cooldown per category, `{token}` substitution. (3) `src/systems/NpcDirector.js` — the reactive brain (priority bands, per-category cooldowns, interrupt + queue, recently-said dedup, idle timer, SHOW_TUTORIAL reroute); wired in Game.js before TutorialSystem, destroyed in shutdown. (4) `src/hud/NpcCompanion.js` + a `.qf-npc` CSS block — DOM portrait with two-layer expression cross-fade, typewriter bubble, paged tutorial mode; mounted as a HudRoot panel. (5) `qf.gameplay.companion` Off/Quiet/Normal setting in userSettings.js + a GAMEPLAY radio in SettingsOverlay.js; TutorialOverlay defers to her unless Off. Verified via isolated component test (real modules + real npcLines.json + real EventBus): idle auto-fire, event reactions with correct expression, token resolution, 3-page tutorial delivery + onClose firing, off/normal toggle. NOTE: full in-game visual layout (her on-screen position against the live HUD) was not screenshot-verified — the preview tab runs backgrounded so the game can't get past the font-gated Boot scene; the user should do a final visual glance. Previous audit 2026-05-19 (Section 34 — Polish pass closed 4 previously-deferred follow-ups + 1 audit catch. (1) **FRIENDS tab removed from LeaderboardOverlay** — left with GLOBAL + PERSONAL; the friends backend never landed and the tab was inert. (2) **Leaderboard `leaks_count` plumbed end-to-end**: KnowledgeSystem._onAdventurerFled now emits `INTEL_LEAKED { adventurer, count }` where count = total room/trap/fountain/chest/loot intel that survivor is leaking; RunHistorySystem subscribes and accumulates `run.totals.intelLeaks` + `run.totals.leakEvents`; GameOverOverlay now submits to the leaderboard (the Phaser submit path was gated under newhud — caught during audit) with `meta.leaks_count` + `meta.leak_events`; LeaderboardOverlay already read `meta.leaks_count` so the display works end-to-end. (3) **Status-text floats wired**: new `STATUS_APPLIED { targetId, label, color? }` event channel; DungeonFx renders a generic `qf-fx-float-status-<label>` float at the target's worldX/Y; emit sites added at BossArchetypeSystem petrify / charm / lizardman-venom-first-stack / myconid-corpse-first-stack and BossSystem mechanic petrify. POISONED / PETRIFIED / CHARMED actively fire today; STUNNED / BLEEDING infrastructure ready for future statuses. (4) **PactPicker rarity-particle finale**: on confirm, 18 rarity-tinted sparks burst from the picked card and arc to the TopBar buff-slot row, which gets a 1200ms gold pulse on landing. Web Animations API powers the per-particle trajectory; per-dot stagger (18ms × 18 = 324ms) keeps the finale from reading as a single pulse. Whole sequence fits in the existing 1400ms pre-PACT_SEALED window. Also surfaced in this pass: the new DOM GameOverOverlay wasn't submitting to the leaderboard at all under newhud (the Phaser GameOver scene path was gated off but the DOM port hadn't picked up the submission); fixed by adding `_submitRun()` to GameOverOverlay.show(). Previous audit 2026-05-19 (Section 34 — **PHASE 34 COMPLETE. The DOM HUD port is done.** Every row in Section 34 is now ✅ DONE or 🚫 REMOVED. Final pass this session closed the last four ⏳ items: (1) `hud2-audio-cues` — extended `SfxSystem` with 4 new event subscriptions (`PACT_SEALED`, `INTEL_LEAKED`, `SHOW_GAME_OVER`, `MINION_BOUNTY_POSTED`) all reusing existing audio assets through the SfxVolume table; new `src/hud/HudSfx.js` adds a DOM-side bridge with delegated `click` + `pointerover` listeners on `#hud-stage` for generic button chips, danger-button variant via class regex, and a `playUi('toast')` hook from ToastQueue. All cues rate-limited (80–200ms cooldowns) and respect `SfxVolume.isMuted()`. (2) `hud2-settings-wiring-particles` — emitter audit complete. `AbilityVfx.particleBurst`, `BossPactVfx` (ember swarm + smoke burst + streak lines), and `CoinBurstRenderer._onAward` all scale their counts by the `qf.video.particles` localStorage value (off / low ×0.4 / med ×0.7 / high ×1.0) with `Math.max(1, …)` floor so low-quality bursts don't vanish; `HitSparkSystem` skips on off only (per-hit single sprites aren't density-scaled). Inline localStorage reads avoid cross-folder src/hud/ imports from src/ui/ + src/systems/. (3) `hud2-aux-archetype-strip` — `src/hud/BossArchetypeStrip.js` renders the EARTHQUAKE / SACRIFICE buttons as a DOM strip above the new BottomBar (only during day phase, only for golem/demon archetypes, pulse-animates when armed). Phaser BossArchetypeUI's ~400 lines of archetype VFX keep working through the Game scene canvas; only the player-facing buttons skip under newhud. (4) `hud2-section31-deprecation` — added SUPERSEDED banners pointing to DOM replacements on every Phaser chrome module: ActionBar, BossTopBar, BuildMenu, MiniMapPanel, KnowledgePin, DungeonLog, DossierPanel, BossFightOverlay (Phaser), EventBanner (Phaser), WantedPoster, MinionInspector (Phaser), NameEntryPanel (Phaser), AudioControls, plus all 13 files in src/ui/popups/. Phaser files stay in the repo as the `?newhud=0` fallback per CLAUDE.md's removal-not-deletion policy. **Total Phase 34 ship list** (across all 6 sub-phases A → E): 34A scaffolding (HUD stage, dom helpers, feature flag); 34B chrome (TopBar, BottomBar, LeftPanels, RightPanels, ToastQueue, HotkeyHints, RoomTooltip, sprites); 34C.1 (Overlay shell, PauseOverlay, SettingsOverlay); 34C.2 (10 info overlays — BossOverview, Roster, KnowledgeMap, AdvIntel, ConfirmPopup, PactDetail, WelcomeIntro, Tutorial, BossLevelUp, LongGame); 34C.3 (5 cinematic moments — PhaseTransition, PactPicker, PostWave, GameOver, FullLog); 34C.4 (MainMenu, Leaderboard); 34C.5 (settings wiring including audio + particles); 34D (5 FX surfaces — chromatic, phase tint, ambient particles, floating combat numbers, plus the briefly-shipped braziers); 34E (5 aux surfaces — BossFightOverlay, EventBanner, MinionInspector, NameEntry, BossArchetypeStrip, plus the Wanted-poster fold into ToastQueue + Dossier marked superseded + Section 31 deprecation pass). The new DOM HUD is default-on; legacy Phaser chrome stays as `?newhud=0` fallback. All file count tracking row notes are accurate. Previous audit 2026-05-19 (Section 34 — Phase 34E aux-surface bundle SHIPPED. Four rows closed in one pass. **`hud2-aux-wantedposter`**: folded into the existing ToastQueue as a new `'bounty'` kind. ToastQueue subscribes to `MINION_BOUNTY_POSTED` and pushes a parchment-styled 3-line toast (header / type · kills · gear / italic flavor) with a longer 5200ms dwell. Phaser `WantedPoster` gated under `!useNewHud` in Game.js. **`hud2-aux-minioninspector`**: new `src/hud/MinionInspectorOverlay.js`. Opens on `MINION_CLICKED`, rebuilds on `MINION_LEVELED_UP` / `MINION_EVOLVED` / `MINION_NAMED`, auto-closes on phase change. Mid-right popup with title + ✎ rename + × close + stats block + evolution history. Phaser `MinionInspector` gated under `!useNewHud` in Game.js. **`hud2-aux-nameentry`**: new `src/hud/NameEntryOverlay.js`. Centered modal using a real `<input type="text">` for proper text editing (vs the Phaser version's per-keystroke loop). Used by both RosterOverlay's RENAME button (previously inert — now functional) and MinionInspectorOverlay's ✎ pencil. Enter/Escape/backdrop-click handling. **`hud2-aux-dossier`**: marked ✅ DONE as superseded — the Phaser DossierPanel was already disabled in Phase 31C (see DayPhase.js comment) and the new HUD's design source uses AdvIntel instead. Phase 34E now has 5 of ~9 rows complete (the 2 cinematics + these 3 ports + Dossier-as-superseded). Remaining: `hud2-aux-archetype-strip` (low priority) + `hud2-section31-deprecation` (final cleanup). Previous audit 2026-05-19 (Section 34 — Cinematic-layering bundle SHIPPED. The two Phaser cinematics that were obscured by the DOM HUD chrome (boss-fight intro/HP-bar/result slates + dungeon-event top banner) are now DOM-native, closing `hud2-cinematic-layering`. **`src/hud/BossFightOverlay.js`**: intro slate (portrait + name + tagline + diamond + lives, 400/1000/400ms timeline), persistent bottom HP bar with rAF tick, color-threshold fill, shake + flash on hits (shake gated by `userSettings.isShakeEnabled()`), result slate with party-won/boss-won variants. Portrait is a live `<canvas>` snapshot of the Phaser `${bossArchetypeId}-idle` texture so it matches the in-game sprite (not the static bestiary bust). Vignette is a single inset-box-shadow layer for the edge-dim effect. **`src/hud/EventBanner.js`**: 5 themes (warn/accent/soul/gold/green) with per-theme fill+border+text palettes matching the Phaser version, anchored at `top: 130px` to clear the new TopBar, 350/4500/600ms timeline with a translate-Y arrival motion. Mid-flight bumps clear timers + force reflow before re-opening so the new banner doesn't fight a stale fade. **Wiring**: both constructed in `HudRoot._mountPanels()` after the `mount()` call (same race rule as DungeonFx — anything that appends to `#hud-stage` during construction must happen after the panels mount or `replaceChildren()` wipes it). The Phaser counterparts in HudScene gated behind `!useNewHud` so they don't double-fire. Mounted at `z-index: 8`, sitting above the FX layer (5) and below HUD chrome (9–10). Phase 34E now has 2 of ~9 aux-surface rows complete; next candidates are DossierPanel / WantedPoster / MinionInspector / NameEntryPanel. Previous audit 2026-05-19 (Section 34 — `hud2-fx-braziers` REMOVED at user request. Brazier light cones cut from `DungeonFx.js`: `_refreshBraziers`, the `_braziers` cache, `ROOM_PLACED` / `ROOM_REMOVED` subscriptions, and the per-frame `requestAnimationFrame` tick (which only existed to follow the camera for the cones) are all gone. `.qf-fx-brazier` / `.brazier-light` CSS keyframes preserved in `styles.css` per the removal-not-deletion policy. Phase 34D remains COMPLETE with 4 active FX surfaces (chromatic / phase tint / ambient particles / floating combat numbers). Previous audit 2026-05-19 (Section 34 — Phase 34D COMPLETE. Dungeon-view FX layer landed end-to-end in a single `src/hud/DungeonFx.js` module mounted into `#hud-stage` at z-index 5 (between the Phaser canvas at z:0 and HUD chrome at z:9–10). All 5 FX surfaces shipped: (1) chromatic aberration full-screen layer with day/night variants of the existing `.chromatic`-style gradients; (2) phase tint cool-blue at night / warm-red by day, swap on `DAY_PHASE_BEGAN` / `NIGHT_PHASE_BEGAN`; (3) ambient particles — motes + embers — count scaled by `userSettings.particlesMultiplier()` (off/low/med/high) with `refreshFromSettings()` hook for live SettingsOverlay APPLY; (4) brazier light cones at each boss-chamber corner, refreshed on `ROOM_PLACED` / `ROOM_REMOVED` and re-positioned every frame via `requestAnimationFrame` tick to track camera scroll/zoom; (5) DOM floating combat numbers on `COMBAT_HIT` / `ADVENTURER_DIED` / `MINION_DIED` (damage / crit / miss / kill / gold / minion-fell kinds, 24-deep ring buffer). Coord conversion `(worldX - cam.scrollX) * cam.zoom` works under Phaser Scale.FIT because canvas logical-px == stage logical-px. **Mount-order bug fixed**: DungeonFx now constructs AFTER `mount(stage, panels)` in HudRoot — `mount()` calls `replaceChildren()` which was wiping the FX layer that DungeonFx appended in its constructor. The status-text float kinds (POISONED / STUNNED / BLEEDING) are noted as an open follow-up — they need a new event emit from the status systems. `hud2-settings-wiring-particles` is largely closed by the new ambient-layer wiring, though full audit of `AbilityVfx.js` / `BossPactVfx.js` / `CoinBurstRenderer.js` emitters remains. Previous audit 2026-05-19 (Section 34 — Phase 34C.4 COMPLETE. MainMenuOverlay + LeaderboardOverlay shipped. MainMenu Phaser scene's `create()` now early-returns under the newhud flag and lazy-mounts the DOM overlay via a singleton; same pattern for the Leaderboard scene. CONTINUE button reads `SaveSystem.hasSave()` for gating + populates the current-boss heading from the saved gameState (archetype name from `bossArchetypes.json` cache, day + kills from `meta.dayNumber` + `player.totalKills`). LeaderboardOverlay pulls from the existing Supabase-backed `Leaderboard.fetchTop(50)` — same data the Phaser scene used. Top-3 get IMMORTAL/BUTCHER/CUNNING accolades + per-rank gold/silver/bronze color; "YOU" row detected via `PlayerProfile.getName()` match. End-to-end verified in preview: MainMenu mounts with 7 items, CONTINUE dimmed when no save, "A NEW DUNGEON AWAITS" heading, LEADERBOARD click opens the Hall of Evil with 3 tab strip + correct title. Open follow-ups noted: `leaks_count` isn't in the leaderboard schema yet; FRIENDS tab has no backend; VIEW FULL CHRONICLE is inert (per-run logs not persisted to leaderboard rows). Previous audit 2026-05-19 (Section 34 — Phase 34C.3 COMPLETE. All 5 cinematic moments shipped: PhaseTransition (2.8s on phase change), PostWaveOverlay (replaces PostWaveSummaryPopup), GameOverOverlay (replaces the Phaser GameOver scene under newhud), FullLogOverlay (opens from GameOver), PactPicker (replaces DarkPactPopup with rarity-tinted cards + REROLL). All five gated behind `useNewHud` in HudScene (`SHOW_POST_WAVE_SUMMARY` / `SHOW_DARK_PACT`) or via a new `SHOW_GAME_OVER` event emitted by Game.js's `_onBossFinal`. End-to-end verified in preview: phase transition renders DAY 01 title and auto-clears at 2.8s, PostWave mounts with all stat tiles + ticker, PactPicker shows 3 cards + REROLL + title, GameOver renders QUEST FAILED + 6 stat tiles + 3 MVP cards + 4-day timeline, FullLog opens with 6 filter buttons + 4 day sections. **Open follow-ups noted in row notes**: PactPicker's 18 rarity-particle fly-to-buff-row finale is deferred polish; GameOver's leaderboard submission still happens in the Phaser GameOver scene path. Previous audit 2026-05-19 (Section 34 — comprehensive audit pass before moving to 34C.3. Surveyed every file in `src/ui/` and `src/ui/popups/` to flush out untracked surfaces. Added 11 new rows: a Phase 34E "auxiliary in-game UI surfaces" section covering BossArchetypeUI (golem/demon action strip), BossFightOverlay (boss-fight cinematic), EventBanner (Dungeon Event top slate), DossierPanel (daily briefing), WantedPoster (bounty notification), MinionInspector (click-minion popup), CartographerOverlay (stays Phaser), KnowledgeOverlay (stays Phaser), NameEntryPanel (Roster RENAME), AudioControls (effectively dead under newhud), plus a Section-31-deprecation tracking row. Also surfaced a real layering bug as `hud2-cinematic-layering`: Phaser cinematics (BossFightOverlay + EventBanner) are obscured by the DOM HUD because the Phaser canvas sits below `#hud-root` in the z-stack. Resolution lands when those two get DOM ports. Updated `hud2-fx-roomtooltip` to ✅ (it actually shipped under `hud2-room-tooltip` in 34B). Previous audit 2026-05-19 (Section 34 — Phase 34C.5 wiring shipped (minus particles). SettingsOverlay values now take effect across the actual subsystems. AUDIO: master×sfx → `SfxVolume.setVolume()`, master×music → `TitleMusic.setVolume()`, live-preview on slider drag, CANCEL restores. VIDEO: scanlines / vignette toggle CSS classes on `#hud-root`, fullscreen calls the browser API, screen-shake gated at all 4 call sites (ScreenShakeSystem + BossArchetypeUI Earthquake + DungeonRenderer big-hit + DarkPactPopup) via inline `qf.video.shake !== 'false'` reads. GAMEPLAY: confirm-abandon gates PauseOverlay's ABANDON-with-confirm path, autosave gates the 3 phase-transition save sites (NightPhase start + NightPhase end + DayPhase end + EndOfDay → night), hotkey-hints visibility tied to the existing HotkeyHints component. Added `src/hud/userSettings.js` as the shared read helper for new code that wants to honor these flags. End-to-end verified: SFX slider at 25% with master 70% → SfxVolume=0.175 (exact math), scanlines class toggles on/off correctly. Only the **particles** setting remains unwired — split into `hud2-settings-wiring-particles` as a tracked follow-up. Previous audit 2026-05-18 (Section 34 — Phase 34B COMPLETE. All 13 rows in 34B now ✅ DONE. This pass finished the 7 remaining items: buff-slots-data (TopBar reads `activeMechanics` + resolves via `dungeonMechanics.json` with per-rarity glyph + color + legendary blink-pip), buff-tooltip (routes through `PactDetailPopup` with new `anchor: 'below'` mode), sprites (vanilla port of `sprites.jsx` 16×16 grids + 14×14 room icons + alias table for non-design minion families — wired into LeftPanels construction cards), hotkey-hints (bottom strip gated by `qf.gameplay.hotkeys` localStorage), toast-events (VETERAN APPROACHING on `ADVENTURER_ENTERED_DUNGEON` with `escapeCount > 0`, GOLD LOOTED on `LOOT_GOBLIN_ESCAPED`), incoming-wave-data (replaces the stub with a live forecast computed from the same formula as `DayPhase._spawnDailyAdventurers`), room-tooltip (canvas-bridge `pointermove` → tile hit-test → DOM 2-tab tooltip with STATS / HISTORY). RightPanels bumped from 🟡 to ✅. Sprites row bumped from 🟡 to ✅. End-to-end verified in preview: 3 seeded pacts render with rarity tint + tooltip on hover, minion-card sprites present, hotkey hints visible by default, wave-forecast shows count + threat + Gold Rush note, veteran + gold-looted toasts fire on their canonical events, room tooltip mounts on `SHOW_ROOM_TOOLTIP` with both tabs. Previous audit 2026-05-18 (Section 34 — Phase 34C.2 COMPLETE. All 10 rows in 34C.2 now ✅ DONE: BossOverview, Roster, KnowledgeMap, AdvIntel, ConfirmPopup, PactDetail, WelcomeIntro, Tutorial, BossLevelUp, LongGame. The remaining 4 re-skin overlays shipped this pass — WelcomeIntroOverlay auto-fires on game boot with the 3-paragraph game-loop explainer + tutorial-enabled checkbox; TutorialOverlay handles SHOW_TUTORIAL with sticky modal + GOT IT; BossLevelUpOverlay has the full hero-burst design (counter-rotating rune rings + 16 spokes + LV X ▶ LV Y slam + power-gain stat tiles + 4-category unlock grid); LongGameOverlay re-skins the Long-Game-pact notification. HudScene's legacy SHOW_TUTORIAL / LONG_GAME_TRIGGERED / SHOW_BOSS_LEVEL_UP listeners + the welcomeintro auto-open are all gated under `!useNewHud`. All four overlays verified in preview (auto-open, dismissal, event payload rendering). Next phase: 34C.3 cinematic moments (PhaseTransition / PactPicker / PostWave / GameOver / FullLog). Previous audit 2026-05-18 (Section 34 — Phase 34C.2.b shipped: KnowledgeMapOverlay + AdventurerIntelOverlay. KnowledgeMap has the full design feature set: 4-stat summary with sparkline placeholder, zoom (− / ◇ / +) with click-drag pan when zoomed, click-to-filter ledger, per-room cards with leak attribution + mitigation + SCRUB INTEL button + 4-state legend. AdvIntel has the full surface but renders an "INTEL UNKNOWN" empty state during night phase since gameState has no party-preview slot yet (real data tracked under `hud2-incoming-wave-data`). HudScene's legacy `OPEN_KNOWLEDGE_MAP` / `OPEN_ADV_INTEL` listeners now gated under `!useNewHud` — all 4 main info popups (Boss / Roster / Knowledge / AdvIntel) are now DOM. End-to-end verified: click-to-filter on a room produces filter chip, + zoom advances to 125%, AdvIntel empty state correct for current data. Total 34C.2 progress: 6 of 10 rows ✅ DONE (BossOverview, Roster, ConfirmPopup, PactDetail, KnowledgeMap, AdvIntel). The 4 re-skin rows (welcomeintro / tutorial / bosslevelup / longgame) remain ⏳. Previous audit 2026-05-18 (Section 34 — Phase 34C.2.a shipped: ConfirmPopup, RosterOverlay, BossOverviewOverlay, PactDetailPopup. ConfirmPopup unblocks PauseOverlay's ABANDON-RUN-with-confirm. RosterOverlay reads live `gameState.minions` and renders the design's 4-stat summary + filter row + scrollable list + detail card with TRAITS / description / actions. BossOverviewOverlay does the 3-tab dossier (OVERVIEW / ABILITIES / PACTS) with cinematic portrait card on the left (counter-rotating rune rings via `spin-slow` keyframes), reading boss data + activeMechanics + bossAbilities cache. PactDetailPopup is the floating viewport-positioned tooltip on pact hovers. HudScene gates its legacy `OPEN_BOSS_OVERVIEW` / `OPEN_MINION_ROSTER` / `SHOW_CONFIRM` / `SHOW_PACT_DETAIL` listeners under `!useNewHud`. End-to-end verified via in-page eval: BossOverview tab switching, Roster list rendering, ConfirmPopup callback firing, PactDetailPopup mounting. Phase 34C.2.b (KnowledgeMap + AdvIntel) and the 4 re-skin popups (welcomeintro / tutorial / bosslevelup / longgame) remain pending. Previous audit 2026-05-18 (Section 34 — comprehensive design-source audit pass. Every porting deliverable from the `claude design ui` handoff README, overlays.jsx, and moments.jsx is now its own tracked row (no more grouped catch-alls). Added 19 new rows covering: per-overlay 34C.2 split (BossOverview / Roster / KnowledgeMap / AdvIntel / ConfirmPopup / PactDetailPopup / WelcomeIntro / Tutorial / BossLevelUp / LongGame), per-moment 34C.3 split (PhaseTransition / PactPicker / PostWave / GameOver / FullLog), per-screen 34C.4 split (MainMenu / Leaderboard), per-FX 34D split (floating numbers / braziers / ambient particles / phase tint / chromatic aberration / room tooltip), plus previously-untracked items: buff-slot data wiring, buff-slot hover tooltip, incoming-wave data infra, 2 inert toast kinds, room hover tooltip, hotkey-hints row, audio SFX cues. `hud2-flag-flip` bumped to ✅ DONE (already shipped). Total section 34 row count: 42. Previous audit 2026-05-18 (Section 34 — Phase 34C.1 shipped: generic `Overlay` shell, `PauseOverlay`, `SettingsOverlay`. Pause/Settings wired through a new `PAUSE_STATE_CHANGED` event on `PauseManager` so ESC in any gameplay scene mounts the DOM overlay (and the Phaser pause scene is skipped under the newhud flag). Settings overlay persists to `localStorage`; palette radio live-previews via the existing `.palette-necro/.palette-hellfire` classes. End-to-end verified: overlay mount → game/night/hud all paused, OPTIONS opens the 4-tab settings panel (AUDIO sliders + VIDEO toggles + CONTROLS keybinds + GAMEPLAY toggles), tab switch + CANCEL + RESUME all clean up correctly. Previous audit 2026-05-18 (Section 34 — LeftPanels + RightPanels + ToastQueue shipped. The full Phase 34B HUD chrome now mounts behind the `?newhud=1` flag: TopBar (real bestiary portrait, name lookup, HP/XP/hearts/buff slots/day-stamp/gold ticker) + BottomBar (PLACE/MOVE/SELL, BEGIN DAY ↔ four-button 1×/2×/4×/8× speed control, four menu buttons) + LeftPanels (1:1 mini-map blueprint + 4-tab construction grid with footer detail + PLACE button, drop-in BUILD_SELECT / BUILD_DESELECT contract with NightPhase) + RightPanels (incoming-wave stub, exposure + leak list from sharedPool, color-coded dungeon log subscribed to 12 gameplay events) + ToastQueue (top-right stack, max 4, 5 wired toast kinds). Phaser MiniMapPanel + BuildMenu + KnowledgePin + DungeonLog all hidden + hit zones disabled while DOM HUD is mounted. Only the flag-flip + buff-slot live wiring + sprite catalogue port remain in Phase 34B. Previous audit 2026-05-18 (Section 34 — BottomBar shipped alongside the existing scaffolding/TopBar rows. New `src/hud/BottomBar.js` mirrors ActionBar's event contract end-to-end: PLACE/MOVE/SELL (PLACE re-emits the armed tool to disarm), BEGIN DAY emits `PHASE_TOGGLE_REQUEST`, four discrete speed buttons emit `TIME_SCALE_SET { scale: 1\|2\|4\|8 }` (no cycle button), four menu buttons emit the corresponding `OPEN_*`. Subscribes to `TOOL_MODE_CHANGED` for armed-state mirroring. TopBar also bumped: real bestiary portrait loaded via `Image()` probe with a CSS-gradient fallback for archetypes without portrait files, plus archetype-id lookup now strips `the_` prefix so name resolves from cache for both raw (`lich`) and legacy-prefixed (`the_lich`) ids. HudScene rebuilds the HudRoot singleton when the gameState reference changes (fix for new-run-after-game-over case — panels cache gameState at construction). Phaser `ActionBar` hidden + `disableInteractive`'d on hit zones while the DOM HUD is mounted. EventBus emissions verified via in-page click harness. Previous audit 2026-05-18 (Section 34 added — claude-design DOM HUD port. Phase 34A scaffolding shipped. Phase 34B.1 TopBar shipped). Previous audit 2026-05-02 (Phase 1b cleanup — Orc Warband (replaces scrapped WAAAGH!) + generic patrolling-minion door re-lock both shipped. (a) Warband: `_tickOrc` runs every frame for the orc archetype; per-orc room lookup → tally other orcs in same room → recompute `stats.attack = round((_orcBaseAttack + lootAtkBonus) × (1 + 5%·allies))` and `stats.defense = round(_orcBaseDefense × (1 + 5%·allies))`. Loot the Fallen refactored to write only `lootAtkBonus`; baselines stamped on `MINION_PLACED` / `BOSS_LEVELED_UP` / save-load. New balance constants `ORC_WARBAND_ATK_PCT_PER_ALLY` (0.05), `ORC_WARBAND_DEF_PCT_PER_ALLY` (0.05). DESIGN.md / bossArchetypes.json bestiary copy updated. (b) Patroller door re-lock: new `MinionAISystem._tickPatrollerDoors` (runs at end of every minion-AI tick); `_isPatrollerMinion(m) = _isVampireThrall || _isDemonImp || _isHauntGhost`. Tracks each patroller's current cp via `dungeonGrid.getCpForDoorTile`; on door-tile entry stores the cp + lock snapshot + calls `openDoor` (door visibly swings open during traversal); on door-tile exit calls `closeDoor` and restores `cp.locked = true` if the snapshot recorded it. Extended `DungeonRenderer.closeDoor` to mirror to the paired cp (matching `openDoor`'s symmetry), so a single close-call closes both sides of the doorway. The Vampire row was the last entry blocking Phase 1b from being fully ✅; that's now closed too. Phase 1b is now functionally complete. (c) Myconid corpse-glyph polish landed too: BossArchetypeSystem captures the dying adv's `spriteVariant` + the last-frame index of its `hurt-down` anim; FungalCorpseRenderer paints that frame at 0.55× scale tinted `0x66ee66` so the corpse looks like the actual collapsed adventurer gone fungal (skull glyph kept as fallback for advs without LPC sprites). Phases shipped this pass (in order): 1b.1 Orc Loot the Fallen, 1b.2 Golem Living Architecture + Earthquake, 1b.3 Beholder Petrify Gaze + Anti-Magic Aura, 1b.4 Lich Phylactery, 1b.5 Lich Necromancy, 1b.6 Lizardman Camouflage + Venom Stack, 1b.7 Myconid Spore Network + Corpse Bloom, 1b.8 Wraith Fear Meter + Haunting, 1b.9 Demon Sacrifice Pact + Hellgate, 1b.10 Vampire Charm + Blood Tax, 1b.11 Gnoll Hunters Pack + Bloodlust, 1b.12 Orc Warband, 1b.13 patrolling-minion door re-lock. Ten new behaviors share one system (`BossArchetypeSystem`) and one UI panel (`BossArchetypeUI`); two purely-visual renderers were added (`PhylacteryRenderer`, `FungalCorpseRenderer`); two existing systems gained event hooks (`KnowledgeSystem.observeMinion` emits `MINION_OBSERVED`, `CombatSystem.tryAttack` clears camouflage on first hit). New gameplay state slots on `gameState`: `_lich`, `phylactery`, `_myconid`, `fungalCorpses`, `_demon`, `_gnoll`, `_antiMagicRoomIds`. ~25 new balance constants added. Previous audit 2026-05-02 (Monster boss redesign — spec lock & doc pass. User finalized 1-2 unique playstyle-defining abilities per boss for all 10 monsters; replaces the older "headline mechanic + supporting modifiers" structure that was approved 2026-04-28 but never implemented. (a) DESIGN.md § "Monster boss redesign" rewritten end-to-end with the locked specs, niche-coverage table updated, implementation order numbered 1-11 (Orc → Gnoll). Doc cleanup also landed: room-connection paragraph clarified to note Corridor is now a placeable room type (no auto-routed segments); "Adventurer resources" mana-pool reference replaced with cooldowns / per-day-budget per the Class ability rework. (b) bossArchetypes.json rewritten: each boss carries one or two new mechanics with brief bestiary copy, old mechanic-text rows wiped. Status legend on each ability set to `implemented: false` since none have shipped yet. (c) DESIGN_COVERAGE.md § 1b table rebuilt with the new headline-mechanic descriptions, phase column locked to 1b.1 → 1b.11, ArchetypeSelect re-skin row marked ✅ DONE since the bestiary is data-driven. Note: Orc boss only has one ability locked (Loot the Fallen); the WAAAGH! second ability was scrapped and a replacement is pending a follow-up design pass — row noted as 🟡 PARTIAL design until the second ability lands. Previous audit 2026-05-02 (Gold-economy + sell + mimic pass — (a) `STARTING_GOLD: 999999 → 30` (DEV_INFINITE_GOLD still on for testing). (b) All 21 T1 minions retuned: unlock-level spread 1→10 (~2 unlocks per boss level), gold cost 6→35 by power, three outliers stat-reduced (golem1 60/10/8 → 42/8/6, demon1 35/9/4 → 28/8/3, orc1 32/9/3 → 28/8/3) and a few under-tuned T1s buffed (vinekin/plant1 atk 2 → 4, mushroom1 atk 3 → 4, beholder1 atk 4 → 5, vampire_minion1 hp 14 → 16, lich1 atk 4 → 5, ent1 hp 30 → 28). (c) Sell rules unified — `_removeMinion` flipped 100% → 50% refund (matches rooms+traps). MinionRenderer right-click handlers (sprite + placeholder) early-return on right-click — minions are now sell-button-only just like rooms+traps. (d) Mimic chest-disguise mechanic rebuilt: spawn extras add `isMimic + mimicState: 'chest'`; MinionAISystem skips chest-state ticks; AISystem._findEngageableMinion skips chest-state targeting; new RoomBehaviorSystem._rollMimicOpens fires on ADVENTURER_ROOM_CHANGED (40% open chance unknown / 5% known); _revealMimic flips state, applies 30% maxHp bite, marks knowledge across the live party; KnowledgeSystem.sharedPool gained a `mimics` bucket + propagation in _rebuildSharedPool / _ensureAdvKnowledge / initKnowledgeForSpawn; MinionRenderer paints a wooden chest with gold trim until reveal. Constants: MIMIC_OPEN_CHANCE_UNKNOWN, MIMIC_OPEN_CHANCE_KNOWN, MIMIC_REVEAL_BITE_FRAC. DESIGN.md Mimic Vault row + ARCHITECTURE.md mimic section + Phase-9 summary line bumped from "9 active pacts" to 64.) Previous audit 2026-05-02 (Mechanics-cleanup landed — code-side deletion of all four systems the user cut. (1) Soul Essence renamed to gold across all gameplay code, JSON data fields (`essenceCostToPlace` → `goldCost`), balance constants (`STARTING_GOLD`, `GOLD_PER_KILL`, `DEV_INFINITE_GOLD`, `MINION_RESPAWN_COST_GOLD`, `MECHANIC_TAXATION_GOLD_PENALTY`), the archetype modifier (`goldGainMultiplier`), and UI labels. SaveSystem version bumped 1.0.0 → 1.1.0. (2) `EssenceSystem.js` deleted; `upkeepCost` stripped from JSON data and entity factories; `UPKEEP_SHUTDOWN_ORDER` removed; upkeep stat row + bar + deactivation notice deleted from NightPhase; per-card "/day" strings dropped. (3) `ReputationSystem.js` deleted; `REPUTATION_*` constants removed; guild-raid block removed from DayPhase; legendary heroes downgraded to flat 5% per spawn. (4) `LootSystem.js`, `LootRenderer.js`, `LootGreedSystem.js`, `LootItem.js`, `MimicRenderer.js`, `lootDefinitions.json` deleted; `gameState.loot` removed from initial state; `LOOT_PICKED_UP`/`LOOT_SCAVENGED` listeners purged; vulture loot-grab logic and treasury chest carry/escape removed (Treasury keeps flat stipend); mimic chest-disguise mechanic stripped (mimics in Mimic Vault now spawn as plain hostile garrison minions, MimicRenderer/`_tickMimic`/`_tickScavenger` deleted); SEEK_LOOT goal type and `openingChest`/`carriedChest` flags removed from AISystem; mimic sprite preload + animation registration deleted; Greed Trap trigger handler removed (data file was already empty). DESIGN.md "Mana / Essence economy" and "Reputation system" strikethroughs simplified; ARCHITECTURE.md stale-sections banner replaced with a "Removed systems" reference block at the top. Active pact pool unchanged at 13.) Previous audit 2026-05-02 (Mechanics-cleanup intent recorded — user cut four game systems: Soul Essence rename, daily upkeep, reputation, loot pickup. Active pact pool unchanged at 13.) Previous audit 2026-05-02 (Section 5 — Residual-code cleanup pass for the 11 cut mechanics: deleted `EternalNightOverlay.js`, `ParanoiaIndicator.js`; removed dead branches from `CombatSystem._computeDamage` (gravAnomaly), `AISystem._sleep` (noHealthRegen + ADVENTURER_SLEPT emit), `KnowledgeSystem` (isEternalNightActive / visibleRoomIds / _bboxGap), `TrapSystem` (requiresEternalNight gate + _isEternalNightActive), `NewspaperSystem` (HUNGER_BITE listener + huntCount paragraph); dropped orphaned balance constants (HUNGER, KNOWLEDGE_PAIN, MIMICRY, GRAV, LOOT_CURSE, MEMORY_FOG, ETERNAL_NIGHT_VISION_ROOMS); removed the EternalNight/Paranoia overlay constructions, isActive-checks, destroys, and per-frame updates from `Game.js`. Earlier the same day — User cut 11 mechanics from the pact pool: mimicry_plague, gravitational_anomaly, cursed_fountains, no_health_regeneration, memory_fog, eternal_night, hunger, knowledge_is_pain, paranoia_protocol, spectral_reinforcements, loot_curse. All marked 🚫 REMOVED; DESIGN.md numbered list strikethrough'd with `(REMOVED 2026-05-02 — cut from pact pool)` parentheticals per CLAUDE.md "removal-not-deletion" policy. Active pact pool now 9: taxation_of_souls, bloodbound, gold_rush, undying_horde, sealed_paths, pack_synergy, blood_money, hasty_architect, great_erasure. Previous Section-5 row also: two new dark pacts added earlier today: `hasty_architect` (rare, 50% trap discount + 25% jam chance) and `great_erasure` (legendary, escapees forget the dungeon + 2× adventurer base stats). Hooked into NightPhase trap-cost path, BuildMenu cost display, TrapSystem._fireTrap jam roll, KnowledgeSystem._onAdventurerFled erasure, and DungeonMechanicSystem ADVENTURER_ENTERED_DUNGEON stat doubler.) Previous audit 2026-05-01 (Section 31 — UI/HUD overhaul shipped end-to-end. Phases 31A→31I all marked ✅ DONE: Crypt theme + Press Start 2P primitives, title screen rewrite, run-history plumbing, full HUD chrome (top bar / mini-map / build menu / dungeon log / knowledge pin / action bar), Sell+Move tools, four info popups (Adventurer Intel / Boss Overview / Minion Roster / Knowledge Map), Post-Wave Summary + Dark Pact split, Pause Menu redesign + Options scene, Game Over rewrite with animated count-up. Deprecated chrome (BossHpPanel, MiniMap purple variant, transient CombatLog) deleted. Original 'build-action-rotate' row scope-changed mid-phase: ROTATE button dropped from action bar at user request; rotation now happens via R key while a room is held in MOVE mode.) Previous audit 2026-04-30 (Section 6 only — Room redesign 2026-04-30 spec landed: 21-room roster locked, gateway/cap/scaling rules added, 14 existing rooms marked 🚫 REMOVED with handler-cleanup deferred.) Previous audit 2026-04-29 (Class ability rework foundation — mana system removed end-to-end; Vandal personality retired in favor of Ranger's upcoming Trap Expert; new AbilitySystem; vulnerableToElements field seeded; classes reset to PENDING per the per-class spec in DESIGN.md → "Class ability rework". Phase reopened as 5c.) Previous audit 2026-04-29 (earlier): Class pool expansion — Beast Master class promotion + Barbarian/Monk/Bard added.

---

## 1. Boss archetypes (target: 5 starting + ~15 total)

| ID | Name | Phase | Status | Notes |
|---|---|---|---|---|
| the_lich | The Lich | 1 | ✅ DONE | data + selectable |
| the_architect | The Architect | 1 | ✅ DONE | data + selectable |
| the_beast_lord | The Beast Lord | 1 | ✅ DONE | data + selectable |
| the_trickster | The Trickster | 1 | ✅ DONE | data + selectable |
| the_tyrant | The Tyrant | 1 | ✅ DONE | data + selectable |
| (10 more unlockable) | — | QW | ✅ DONE | All 10 unlockable archetypes implemented: warden, pyromancer, shadow, collector, swarm (10b) + archivist, hivemind, alchemist, jester, sleepless (QW). Total 15 archetypes. |
| Archetype-gated dungeon mechanics offered | — | 9 | ✅ DONE | DungeonMechanicSystem.getOfferings filters by archetypeModifiers.blockedMechanics + availableToArchetypes |
| Archetype-gated minion strengths | — | 6e | ✅ DONE — minionStatMultiplier applied at placement; minionXpMultiplier in EvolutionSystem; essenceGainMultiplier in AISystem._kill; blockedTrapTypes filters trap palette |

---

## 1b. Monster boss redesign — supersedes the 15-archetype system (target: 10 monster bosses)

> See DESIGN.md → "Monster boss redesign" for the locked spec per boss (one-or-two playstyle-defining abilities; no padding modifiers).
> Spec finalized 2026-05-02. Implementation in progress — order is `1b.1` (Orc) → `1b.11` (Gnoll), ascending complexity / shared-infra dependencies.

| ID | Boss | Locked abilities (headline summary) | Phase | Status | Notes |
|---|---|---|---|---|---|
| orc         | Orc Veteran      | **Loot the Fallen** — each orc keeps +1 ATK per kill (per-orc, lost on death). **Warband** — orcs in the same room give every other orc in that room +5% ATK and +5% DEF, stacks per ally, no cap. | 1b.1 / 1b.12 | ✅ DONE (2026-05-02 — both abilities) | **Loot the Fallen (1b.1):** `BossArchetypeSystem` listens for `ADVENTURER_DIED`/`MINION_DIED` while archetype = orc + killer is an orc-tagged minion; bumps `lootAtkBonus` (no longer writes `stats.attack` directly — Warband's recompute owns the live value). On death, `lootAtkBonus` is zeroed; the next `_tickOrc` pass auto-rebalances the cluster. `MinionRenderer` paints a small "+N" badge in orc-red at the bottom-left of the orc sprite (gated by archetype + tag + bonus>0). **Warband (1b.12):** `_tickOrc` runs every frame from `tick(delta)` for the orc archetype. Per orc, looks up its current room via `dungeonGrid.getRoomAtTile`, counts other live orc-tagged minions in the same room, derives `atkMult = 1 + allies × ORC_WARBAND_ATK_PCT_PER_ALLY` (0.05) and `defMult = 1 + allies × ORC_WARBAND_DEF_PCT_PER_ALLY` (0.05), then writes `stats.attack = round((_orcBaseAttack + lootAtkBonus) × atkMult)` and `stats.defense = round(_orcBaseDefense × defMult)`. Pristine baselines stamped on `MINION_PLACED`, `BOSS_LEVELED_UP` (rebaselined post-scale by inferring base from current value ÷ live warband multiplier), and at scene boot for save-load via `_captureOrcBaseline` (subtracts active loot bonus before stamping). New balance constants: `ORC_WARBAND_ATK_PCT_PER_ALLY`, `ORC_WARBAND_DEF_PCT_PER_ALLY`. No SaveSystem changes — all new fields (`_orcBaseAttack`, `_orcBaseDefense`, `lootAtkBonus`) are plain numbers that JSON-serialize naturally. |
| golem       | Earth Golem      | **Living Architecture** — every placed room (incl. boss + corridor) → +5 boss HP / +1 DEF permanent. **Earthquake** — 1×/day day-phase button; pick a room, advs inside take (rooms placed × 2) damage. | 1b.2 | ✅ DONE (2026-05-02) | `BossArchetypeSystem` listens for `ROOM_PLACED`/`ROOM_REMOVED` while archetype is golem; tracks `gameState.boss._golem` (`roomsCounted` / `hpApplied` / `defApplied` / `earthquakeUsesLeft` / `firstUseToastShown`) so a save/load and dynamic place/remove stay balanced and don't double-count. On scene init, backfills any rooms not yet counted (handles old saves cleanly). Earthquake: new `BossArchetypeUI` panel above the action bar shows the EARTHQUAKE button only when archetype = golem and phase = day; click → arms; next click on a dungeon tile → resolves the room → emits `GOLEM_EARTHQUAKE_TARGET` → system applies `(rooms × 2)` damage to every adv inside (uses `gameState.dungeon.rooms.length`, no cap). Damage routes through `COMBAT_HIT` and triggers `ADVENTURER_DIED` if HP drops to 0 (so RunHistorySystem / boss XP still fire). UI consumes `GOLEM_EARTHQUAKE_FIRED` and plays a Game-camera shake (450 ms) + a floating `EARTHQUAKE -N` text over the room center. Daily uses reset on `NIGHT_PHASE_STARTED`. First-use toast fires on construction (UI-side gate via `firstUseToastShown` flag, immune to scene-startup ordering). New balance constants: `GOLEM_HP_PER_ROOM`, `GOLEM_DEF_PER_ROOM`, `GOLEM_EARTHQUAKE_DMG_PER_ROOM`, `GOLEM_EARTHQUAKE_USES_PER_DAY`. Click-on-button gate prevents the room-pick handler from resolving a phantom room when the player clicks the button itself to disarm. |
| beholder    | Beholder Tyrant  | **Petrify Gaze** — BossFightScene only; freezes advs 2s every 6s with eye-beam VFX. **Anti-Magic Aura** — 2 random rooms/day disable class abilities for the day (+1 per boss level); purple glow VFX | 1b.3 | ✅ DONE (2026-05-02) | Petrify Gaze: `BossArchetypeSystem` listens to `BOSS_FIGHT_STARTED` and starts a Phaser timer that fires every `BEHOLDER_PETRIFY_INTERVAL_MS` (6000) for the duration of the fight. Each fire stamps `adv._petrifiedUntil = now + BEHOLDER_PETRIFY_DURATION_MS` (2000) on every adv inside the boss chamber. `BossSystem._tickFightAdv` early-returns while petrified (freeze in place); `BossSystem._runOneRound`'s attackers filter excludes petrified advs (no boss damage that round). VFX: dedicated `_petrifyFxGraphics` layer draws eye-beams from boss to each target + stone-crackle rings on targets, fading over the 2 s freeze window (prior tweens killed before re-fire). Timer torn down on `BOSS_FIGHT_RESOLVED`; lingering `_petrifiedUntil` cleared so survivors aren't stuck. Anti-Magic Aura: `DAY_PHASE_BEGAN` triggers `_rollAntiMagicRooms` — Fisher-Yates picks `BEHOLDER_ANTIMAGIC_BASE_ROOMS` (2) + `(bossLevel-1) × BEHOLDER_ANTIMAGIC_PER_BOSS_LV` (1) random non-boss rooms, stamps `room._antiMagic=true` and `gameState._antiMagicRoomIds=[…]`. `ClassAbilitySystem.update` reads the daily set and skips the `consider*(adv)` switch when an adv stands inside a marked room (existing buffs still tick out via `_tickActiveBuffs`). VFX: `_antiMagicFx` graphics layer paints a soft purple wash + bright outline on each marked room. `NIGHT_PHASE_STARTED` clears marks + the overlay. Save/load safe — both per-room flags and the gameState id list serialize naturally; constructor calls `_renderAntiMagicAura` so a mid-day reload still shows the glow. New balance constants: `BEHOLDER_PETRIFY_INTERVAL_MS`, `BEHOLDER_PETRIFY_DURATION_MS`, `BEHOLDER_ANTIMAGIC_BASE_ROOMS`, `BEHOLDER_ANTIMAGIC_PER_BOSS_LV`. |
| lich        | Elder Lich       | **Phylactery** — unlocks at boss lvl 3; free Heart item (200 HP, heart-full sprite), 4th life. 15% per-adv hunt roll on entry. Only one heart, no replacement if destroyed; with no normal lives, every party hunts the heart. **Necromancy** — kills raise as free Skeletons next dawn (last 1 full day, retain class abilities, dead Clerics heal your minions, no minion-cap). | 1b.4 / 1b.5 | ✅ DONE (2026-05-02) — Phylactery (1b.4) + Necromancy (1b.5) both shipped this pass | **Phylactery (1b.4):** new `src/data/items.json` registered in Preload; `BuildMenu` `_itemDefs()` filters by archetype + `unlockLevel` + one-per-run, hidden once placed. Items tab now also re-renders on `BOSS_LEVELED_UP` / `PHYLACTERY_PLACED` / `PHYLACTERY_DESTROYED` so the heart appears/disappears live. `NightPhase._confirmItemPlacement` validates non-boss room + floor tile, stamps `gameState.phylactery = { instanceId, roomId, tileX, tileY, worldX, worldY, resources: { hp, maxHp }, spriteKey, placedDay }`. New `PhylacteryRenderer` paints `heart-full` sprite + 28px HP bar with hurt-flash; instantiated in `Game.js` and ticked each frame. `BossSystem._endFight` was extended: when `winner='party'` drains `deathsRemaining` to 0 AND `phylactery.resources.hp > 0`, the system bumps `deathsRemaining` back to 1 and refills boss HP, swallowing the final-death event and emitting `PHYLACTERY_REVIVED_BOSS` instead. AISystem: new `HUNT_PHYLACTERY` goal type wired into `_goalToTile` (path target = phyl tile; falls back to FLEE if heart gone) and `_onGoalReached` (freeze adv on arrival; only re-pick when heart destroyed). `BossArchetypeSystem._onAdvsSpawned` rolls `LICH_PHYLACTERY_HUNT_CHANCE` (0.15) per adv on dungeon entry — or forces 100% when `boss.deathsRemaining ≤ 0`. New `tick(delta)` runs from `Game.update()` real-time: every `LICH_PHYLACTERY_DMG_INTERVAL_MS` (800 ms), advs adjacent to the heart's tile with `HUNT_PHYLACTERY` deal `stats.attack` damage; when HP hits 0 the system fires `PHYLACTERY_DESTROYED` once, drops `gameState.phylactery`, and routes surviving hunters to FLEE. Unlock toast (`PHYLACTERY_UNLOCKED`) fires on `BOSS_LEVELED_UP` (and on scene boot for save-load) the first time `boss.level ≥ 3`; gated by `boss._lich.unlockToastShown`. `BossArchetypeUI` consumes that event + `PHYLACTERY_REVIVED_BOSS` + `PHYLACTERY_DESTROYED` and shows toasts. New balance constants: `LICH_PHYLACTERY_UNLOCK_LEVEL`, `LICH_PHYLACTERY_HUNT_CHANCE`, `LICH_PHYLACTERY_DMG_INTERVAL_MS`. **Necromancy (1b.5):** `_onAdvDied` queues `{ classId, name, level, tileX, tileY }` onto `gameState._lich.pendingRaises` whenever the active boss is the lich. `_onDayBegan` (DAY_PHASE_BEGAN listener) culls expired raised first via `_cullExpiredRaised` (cull rule: `today < m._expireAtDay`), then drains the pending queue via `_raiseQueuedDead`: spawns `skeleton1` via `createMinion` at the boss-chamber center (offset around a small ring so they don't stack), with `class: 'garrison'` (so they don't count toward the roster cap), `isUndead: true` (so MinionAISystem.respawnAll permanently removes them on death), `_raisedFromAdvDeath: true`, `_raisedClassId`, `_raisedAdvName`, `_expireAtDay = today + NECROMANCY_LIFESPAN_DAYS` (1, so they vanish at the very next dawn). `_applyClassRetentionBuffs` adjusts base stats per the dead adv's class (cleric: +DEF, range 2, support tag; mage: +ATK, range 3, arcane damage; ranger: +ATK, range 3; knight: +HP +DEF; barbarian: +ATK -DEF; monk: +SPD; rogue: +ATK +SPD; others: +1 ATK). Boss-level scaling re-applied on top via `applyBossLevelToMinion`. Raised cleric heal: `_tickRaisedClerics` runs each frame from `tick(delta)`, every `NECROMANCY_CLERIC_HEAL_INTERVAL_MS` (2200) heals the most-wounded ally minion within Manhattan dist 3 for `NECROMANCY_CLERIC_HEAL_AMOUNT` (4). Events: `NECROMANCY_RAISED { count, minionIds }`, `NECROMANCY_RAISED_EXPIRED { count }`, `NECROMANCY_CLERIC_HEAL { sourceId, targetId, amount }`. New balance constants: `NECROMANCY_LIFESPAN_DAYS`, `NECROMANCY_CLERIC_HEAL_AMOUNT`, `NECROMANCY_CLERIC_HEAL_INTERVAL_MS`. |
| lizardman   | Serpent Captain  | **Camouflage** — minions/traps invisible to advs until first attack (per-entity individual reveal); player sees them slightly transparent. **Venom Stack** — every minion attack adds a -1 HP/sec poison stack; persists until adv dies or leaves dungeon; green tint + stack count VFX | 1b.6 | ✅ DONE (2026-05-02) | **Camouflage:** added `lizardman` tag to lizardman1/lizardman2 in minionTypes.json. `BossArchetypeSystem._onMinionPlaced` stamps `_camouflaged = true` on freshly placed lizardman-tagged minions while archetype is lizardman. `CombatSystem.tryAttack` clears the flag on the attacker's first hit and emits `LIZARDMAN_CAMO_REVEAL`. AISystem's `_findEngageableMinion` skips camouflaged minions (advs literally can't see them); `KnowledgeSystem.observeMinion` skips them too (no rumour-pool entry). MinionRenderer now multiplies container alpha by 0.5 when `_camouflaged` so the player sees a translucent ambusher. NIGHT_PHASE_STARTED listener re-camouflages every surviving lizardman minion so each new day = fresh ambush hit. Traps already invisible to advs by default (KnowledgeSystem only adds entries on `TRAP_TRIGGERED`), so no extra trap-side gate was needed. **Venom Stack:** new `_onCombatHit` listener — when the source is a lizardman-tagged minion + the target is an active adv + damage > 0, increments `adv._venomStacks`. New `_tickVenom` runs from `tick(delta)` (always-on while archetype is lizardman); every `LIZARDMAN_VENOM_TICK_INTERVAL_MS` (1000) deals `stacks × LIZARDMAN_VENOM_DMG_PER_STACK` (1) to each poisoned adv via a synthetic `COMBAT_HIT` (sourceId='venom', damageType='poison'); fires `ADVENTURER_DIED` (killerId='venom') when the tick drops them. Stacks cleared on `ADVENTURER_FLED`. AdventurerRenderer paints a `Nx` green badge above the HP bar and tints the LPC sprite green while stacks > 0; clears tint + badge on stack drop. New balance constants: `LIZARDMAN_VENOM_TICK_INTERVAL_MS`, `LIZARDMAN_VENOM_DMG_PER_STACK`. |
| myconid     | Predator Myconid | **Spore Network** — every 3 days all Corridor rooms emit a green spore cloud all day; 0.5×bossLevel HP/tick to advs inside. **Corpse Bloom** — adv corpses become green-tinted fungal terrain hazards for 3 days (-2 HP/sec on touch, stacks across corpses); after 3 days the corpse turns into a free Vinekin (`plant1`), no minion-cap. Despawn on room-move | 1b.7 | ✅ DONE (2026-05-02) | **Spore Network:** `_rollSporeNetwork` runs each `DAY_PHASE_BEGAN`; on days where `dayNumber % MYCONID_SPORE_INTERVAL_DAYS === 0` it stamps every corridor-room id (`starter_corridor` or any room tagged `corridor`) onto `gameState._myconid.activeSporeRoomIds`. `_renderSporeOverlay` paints a faint green wash + dotted edge + deterministic spore-speck dots on each room (depth 2.5 so the boss/minions still draw on top). `_tickMyconid` runs every frame from `tick(delta)`: every `MYCONID_SPORE_TICK_INTERVAL_MS` (1000), advs inside spore rooms take `Math.round(bossLevel × MYCONID_SPORE_DMG_PER_BOSS_LV)` damage, routed through COMBAT_HIT (sourceId='spores', damageType='poison'), with ADVENTURER_DIED on kill (killerId='spores'). Cleared on `NIGHT_PHASE_STARTED`. **Corpse Bloom:** `_onAdvDied` (myconid branch) appends `{ instanceId, tileX, tileY, roomId, daysRemaining: MYCONID_CORPSE_LIFESPAN_DAYS, classId, name }` to `gameState.fungalCorpses`. New `FungalCorpseRenderer` paints each corpse: green wash + mossy ring + green-tinted skull glyph at the corpse tile, ticked from Game.update in both phases. `_tickMyconid` per frame: any adv standing on a corpse tile that hasn't already been listed in `adv._fungalCorpsesStung` adds the corpse id and bumps `adv._venomStacks` by `MYCONID_CORPSE_VENOM_STACKS_ADDED` (2) — reusing the lizardman venom-tick pipeline (`_tickVenom` ungated so any archetype's stacks are processed). `_tickFungalCorpseDay` runs at every `DAY_PHASE_BEGAN`: decrements `daysRemaining` on each corpse and, when it hits zero, sprouts a free `plant1` (Vinekin) at the corpse tile via `createMinion` with `class: 'garrison'` (no minion-cap) + `_myconidSprout: true`, then removes the corpse from the list. `_onRoomRemovedMyconid` (subscribed to `ROOM_REMOVED`) drops every fungal corpse whose `roomId` matches the removed room. New balance constants: `MYCONID_SPORE_INTERVAL_DAYS`, `MYCONID_SPORE_DMG_PER_BOSS_LV`, `MYCONID_SPORE_TICK_INTERVAL_MS`, `MYCONID_CORPSE_LIFESPAN_DAYS`, `MYCONID_CORPSE_VENOM_STACKS_ADDED`. Corpse glyph **(updated 2026-05-02 polish pass):** `_onAdvDied` now captures the dead adv's `spriteVariant` (e.g. `knight/v01`) and the last-frame index of the `<key>-hurt-down` Phaser anim by walking `scene.anims.get(animKey).frames`. `FungalCorpseRenderer._createSprite` paints that exact frame from the LPC sheet at `0.55` scale, tinted `0x66ee66` — so the corpse looks like the actual collapsed adv, just gone fungal. Falls back to the original green-tinted ☠ glyph if the texture or frame isn't available (e.g. an adv without an LPC sprite). The wash + mossy ring underlay still renders in either case. |
| wraith      | Dark Wraith      | **Fear Meter** — adv tracks fear (+5 corpse, +10 trap, +5 minion sighted, +15 ally died); 50%→flee any random room, 75%→attack other advs 5s, 100%→die (gold but no boss XP); floating bar VFX above adv (no overlap with HP bar). **Haunting** — adv deaths spawn a free Ghost (`ghost2`) in that room (permanent, no minion-cap); patrols spawn room, sees advs in adjacent connected rooms, moves through walls to engage, returns home if alive | 1b.8 | ✅ DONE (2026-05-02) | **Fear Meter:** adv carries `_fear` (0..`WRAITH_FEAR_MAX`=100). `BossArchetypeSystem._addFear` clamps + emits `WRAITH_FEAR_CHANGED`. Sources: `_onTrapTriggered` (+10), `_onMinionObserved` (+5; new `MINION_OBSERVED` event emitted from `KnowledgeSystem.observeMinion` only when a minion is observed *for the first time*), `_onAdvRoomChanged` checks the destination room for any adv corpse → +5 per entry, and `_onAdvDied` (wraith branch) bumps every same-party adv within 5 Manhattan tiles (or in the death room) by +15. `_tickWraith` runs each frame: at ≥50 fear, flag `_fearFleeTriggered` and reroute the adv to a random non-entry/non-boss `EXPLORE_ROOM` (one-shot per run-up); at ≥75, set `_fearAttackUntil = now + WRAITH_FEAR_FRIENDLY_FIRE_WINDOW_MS` (5000) and switch goal to `ATTACK_ALLY` against a random same-party member; at ≥100, panic-die — sets `hp=0`, awards `Balance.GOLD_PER_KILL` via `RESOURCES_AWARDED`, fires `ADVENTURER_DIED` with `_noBossXp: true` and `damageType: 'fear'`. AdventurerRenderer paints a 2 px purple→red gradient fear bar **just below the HP bar** (no overlap); hidden at 0, alpha re-renders only when the rounded fear value changes. **Haunting:** `_spawnHauntGhost` runs from the wraith branch of `_onAdvDied`. Creates a `ghost2` minion at the death tile via `createMinion` with `class: 'garrison'` (no minion-cap) + `_isHauntGhost: true` + `_hauntHomeRoomId` + `_hauntHomeTileX/Y` + `isSpectral: true` (renderer translucency) + boss-level scaling. `_tickHauntGhosts` runs every frame from `_tickWraith`: each haunt picks the nearest adv within `WRAITH_HAUNT_DETECT_RANGE_TILES` (8) regardless of walls; lerps `tileX/tileY` directly toward the target at `WRAITH_HAUNT_PHASE_SPEED_TILES_PER_SEC` (1.6 t/s) — no pathfinder, walls are ignored. When in melee (dist ≤ 1) it calls `combatSystem.tryAttack(m, target)`. With no target it lerps back to its home tile (`_hauntPhase: 'home' / 'hunt' / 'return'`). Emits `WRAITH_HAUNT_SPAWNED` for any future UI hookups. `MINION_OBSERVED` event added to KnowledgeSystem — fired only on first sighting per (adv, minion-type, room) tuple. New balance constants: `WRAITH_FEAR_MAX`, `WRAITH_FEAR_PER_CORPSE_SEEN`, `WRAITH_FEAR_PER_TRAP_TRIGGERED`, `WRAITH_FEAR_PER_MINION_SIGHTED`, `WRAITH_FEAR_PER_ALLY_DIED_NEAR`, `WRAITH_FEAR_FLEE_THRESHOLD`, `WRAITH_FEAR_FRIENDLY_FIRE_THRESHOLD`, `WRAITH_FEAR_PANIC_DEATH_THRESHOLD`, `WRAITH_FEAR_FRIENDLY_FIRE_WINDOW_MS`, `WRAITH_HAUNT_DETECT_RANGE_TILES`, `WRAITH_HAUNT_PHASE_SPEED_TILES_PER_SEC`. |
| demon       | Demon Lord       | **Sacrifice Pact** — fire UI button; click → pick a minion → minion permanently burns (fire VFX) and a random adv in the dungeon instakills. 1×/day, greys out at zero minions. **Hellgate** — permanent corner portal in boss room; daily N free Imps (`imp1`) where N=boss level, 10% imp1 stats × (1+0.1 × bossLevel), no cap. Imps roam the dungeon, persist forever (no minion-cap) | 1b.9 | ✅ DONE (2026-05-02) | **Sacrifice Pact:** new SACRIFICE button in `BossArchetypeUI` (danger style, sits in the same slot as EARTHQUAKE — only one shows per archetype). Visible only when archetype = demon + phase = day. Click → emits `DEMON_SACRIFICE_ARM` → BossArchetypeSystem flips `_sacrificeArmed` and emits `DEMON_SACRIFICE_ARMED` → UI swaps label to "PICK A MINION" + installs a Game-scene pointerdown handler that resolves the closest live dungeon-faction minion within 0.7 × tile. Click on minion → `DEMON_SACRIFICE_TARGET { minionId }` → `_fireSacrifice` zeroes the minion's HP, fires `MINION_DIED + DEMON_SACRIFICE_BURN_VFX` (orange-red expanding ring + scale tween at the minion's last position), strips the minion from `gameState.minions` so the night respawn pass can't revive it, picks a uniform random alive adv, fires `COMBAT_HIT` (sourceId='sacrifice_pact', damageType='fire') + `ADVENTURER_DIED` (killerId='sacrifice_pact'). Daily uses tracked on `gameState._demon.sacrificeUsesLeft` (1) and refilled at every NIGHT_PHASE_STARTED. Button greys out via `_refreshVisibility` when uses=0 OR no live minions exist. Right-click on the dungeon disarms; clicking the button itself toggles arm/disarm. **Hellgate:** `_renderHellgatePortal` paints a permanent infernal disc (dark fire halo + orange ring + yellow inner ring + dark core + flame specks) at the inside corner of the boss chamber on construction. `_spawnHellgateImps` runs at every NIGHT_PHASE_STARTED for the demon archetype: spawns N=boss-level imps via `createMinion` from `imp1`, with `class: 'garrison'` (no minion-cap) and explicitly-scaled stats `Math.round(base × (DEMON_HELLGATE_BASE_STAT_FRAC × (1 + bossLv × DEMON_HELLGATE_STAT_PER_LV)))` (0.10 × (1 + 0.10×lv) — so lvl 1 = 10%, lvl 5 = 15%, lvl 10 = 20% of imp1 base for HP/ATK/DEF). Each imp is stamped `_isDemonImp: true` + `_impRoamLastSwapAt: 0`. Permadeath is intrinsic — they keep `class:'garrison'` so the regular respawn pass can't revive them. **Imp roaming:** `_tickDemonImps` runs every frame from `tick(delta)`. Every 6 s per imp, picks a random non-boss room and reassigns `assignedRoomId` + `homeTileX/Y` to that room's center; the base `MinionAISystem` patrol behaviour then paths the imp toward the new home, so imps drift across the dungeon instead of orbiting the boss room. Imps still engage advs in melee/range like any patrol minion. New balance constants: `DEMON_SACRIFICE_USES_PER_DAY`, `DEMON_HELLGATE_BASE_STAT_FRAC`, `DEMON_HELLGATE_STAT_PER_LV`. |
| vampire     | Vampire Sovereign| **Charm** — at dawn the system marks a random adv with a charm VFX; they leave party, walk to boss room, become a Thrall (same class/abilities, `vampire_minion1` sprite). Thralls patrol the whole dungeon hunting advs; survive across days; do not respawn if killed; close (and re-lock) doors behind them — **door re-lock applies to all patrolling minions in the game**. **Blood Tax** — minion damage on advs is rerouted to boss HP (advs still die, boss heals); boss damage works normally; faint red streak VFX from adv to boss | 1b.10 | ✅ DONE (2026-05-02) — Charm + Blood Tax + thrall patrol + generic patrolling-minion door re-lock all shipped | **Charm:** vampire branch in `BossArchetypeSystem._onAdvsSpawned` picks one random alive adv from the spawning batch, sets `_charmed: true`, stashes original `partyId` in `_charmedFormerPartyId`, nulls partyId, and assigns `goal = { type: 'CHARM_WALK', roomId: bossRoomId }`. Emits `VAMPIRE_CHARM_MARKED`. New `CHARM_WALK` goal type wired into `AISystem._goalToTile` (path target = boss-room center) and `_onGoalReached` (path=null + idle). `_tickCharmConversion` (in `_tickVampire`) iterates the active adv list every frame; when a charmed adv is standing inside the boss chamber, spawns a `vampire_minion1` via `createMinion` at the adv's tile with `class: 'garrison'` (no minion-cap) + `_isVampireThrall: true` + `_charmedClassId` + `isUndead: true` (so MinionAISystem.respawnAll permanently strips them on death). Class retention reuses `_applyClassRetentionBuffs` (same lookup table as Lich Necromancy raises), boss-level scaling applied. Adv is spliced from `gameState.adventurers.active` (no FLED/DIED — they defected). Emits `MINION_PLACED` + `VAMPIRE_THRALL_CONVERTED`. **Thrall patrol:** `_tickThrallRoaming` rotates each thrall's `assignedRoomId` to a random non-boss room every `VAMPIRE_THRALL_ROAM_SWAP_MS` (6000), reusing the Demon-imp roaming pattern; the existing `MinionAISystem` patrol behavior paths the thrall toward the new home, so they drift across the dungeon hunting advs. Thralls don't count toward minion cap and don't respawn on death (`isUndead` strip in `respawnAll`). **Blood Tax:** `_onCombatHit` vampire branch — when source is a dungeon-faction minion + target is an adv + damage > 0, the boss gains `dmg` HP (clamped to maxHp) and emits `VAMPIRE_BLOOD_TAX_TICK { fromX/Y, toX/Y, amount }`. Adv still loses HP as normal; lethal hits still kill them. Boss attacks unaffected. **VFX:** `BossArchetypeUI` listens for `VAMPIRE_CHARM_MARKED` (paints a pulsing dark-red ring around the charmed adv until conversion), `VAMPIRE_THRALL_CONVERTED` (cleans up the ring + toasts "A thrall joins your dungeon"), `VAMPIRE_BLOOD_TAX_TICK` (draws a red core + pink halo line from the adv to the boss for 450 ms). New balance constants: `VAMPIRE_THRALL_ROAM_SWAP_MS`, `VAMPIRE_BLOOD_TAX_VFX_MIN_DMG`. **Patrolling-minion door re-lock (shipped 2026-05-02):** new `MinionAISystem._tickPatrollerDoors` runs at the end of every minion-AI tick. For each alive minion flagged as a patroller (`_isVampireThrall || _isDemonImp || _isHauntGhost`), it checks `dungeonGrid.getCpForDoorTile(tileX, tileY)`. On entering a fresh door tile it stores the cp on `m._doorPatLastCp` and snapshots `m._doorPatLockedSnapshot = !!cp.locked`, then calls `DungeonRenderer.openDoor(cp)` so the door visibly swings open during traversal. On stepping off the door tile it calls `DungeonRenderer.closeDoor(cp)` (which now mirrors to the paired cp on its own — matching `openDoor`'s behavior) and restores `cp.locked = true` if the snapshot recorded it. Behavior is generic — automatically benefits Vampire Thralls, Demon Imps, and Wraith Haunt Ghosts. Tracking state self-cleans on death/no-tag. |
| gnoll       | Gnoll Alpha      | **Hunters Pack** — boss room hosts a free `gnoll1` that respawns each day; +1 free gnoll per boss level up to a cap of 5; not counted toward minion cap; can still evolve normally. **Bloodlust** — every minion or boss kill adds +3% ATK to all gnolls for the rest of the day, no cap; resets at dawn; per-stack red flash + "+3% ATK" floater VFX | 1b.11 | ✅ DONE (2026-05-02) | **Hunters Pack:** added `gnoll` tag to gnoll1 + gnoll2 in minionTypes.json so the bloodlust check is robust. `BossArchetypeSystem._refillHuntersPack` runs at every NIGHT_PHASE_STARTED for the gnoll archetype: counts alive `_isHuntersPackGnoll` minions and spawns `min(GNOLL_HUNTERS_PACK_MAX=5, max(1, bossLevel)) - alive` extra gnoll1's via `createMinion` with `class: 'garrison'` (no minion-cap), arranged in a small ring around the boss-chamber center. Each pack member is stamped `_isHuntersPackGnoll: true`. Boss-level scaling applied. The same refill runs on `BOSS_LEVELED_UP` so a level-up immediately tops up the cap, and any active Bloodlust stack is re-applied to the new arrivals so they ATK-match the rest of the pack. Pack members evolve normally (gnoll1 → gnoll2 etc) since they're regular minions. **Bloodlust:** `_onAdvDied` gnoll branch detects when the `killerId` is `'boss'` or any live minion (excludes synthetic killers `unknown / venom / spores / fear`) and calls `_applyBloodlustStack`. That increments `gameState._gnoll.bloodlustStacks` (no cap) and re-derives every gnoll-tagged alive minion's `stats.attack = round(_baselineAttack × (1 + 0.03 × stacks))`. `_captureBloodlustBaselines` runs at NIGHT_PHASE_STARTED to stamp each gnoll's daily baseline; `_resetBloodlust` (also at NIGHT_PHASE_STARTED) wipes the stack count and restores each gnoll's ATK from `_baselineAttack`. `BossArchetypeUI` consumes `GNOLL_BLOODLUST_STACK` and paints, on every alive gnoll-tagged minion, a red ring flash + "+3% ATK" floater that drifts up and fades. New balance constants: `GNOLL_HUNTERS_PACK_MAX`, `GNOLL_BLOODLUST_PCT_PER_KILL`. |
| succubus    | Succubus Queen   | **Bat-Form Seduction** — 1×/boss-level/day, the Queen shapeshifts to a bat-swarm, flies through walls/locks to an adv and charms them (they turn on their party until they kill an ally). **Doppelgänger** — boss-fight only; she hides among illusory duplicates, each round the party's pooled damage may shatter a decoy instead of hitting her (0 boss damage that round); re-splits at 75/50/25% HP. | 1b.13 | ✅ DONE (Bat-Form Seduction pre-existing; Doppelgänger shipped 2026-05-22) | Added to the roster after the 2026-05-02 ten-boss spec lock (the table header still says "target: 10"; live roster is 12 with Succubus + the locked-until-L99 Slime). **Doppelgänger (2026-05-22):** replaces the old placeholder second ability "Bat Form" (a fly-through-walls footnote of the headline, not a distinct ability). Per the user's instruction the bat-form flight gameplay in `BossArchetypeSystem._tickSuccubus` is left untouched — only `bossArchetypes.json` `mechanics[0].text` changed (auto-propagates to the ArchetypeSelect picker + BossOverview overlay/popup, which all read `mechanics[].text`). Gameplay implemented entirely in `BossSystem` (the boss-fight system): `_onIncoming` calls `_initDoppelganger()` — per-fight instance state, not persisted — setting `_doppelDecoys` = `_doppelDecoyCount()` and `_doppelSplitsLeft` = the threshold list. `_runOneRound` calls `_tickDoppelgangerSplit(boss)` at round start (re-conjures decoys for every HP threshold crossed) and routes the party's pooled `dmgToBoss` through `_tryDoppelgangerAbsorb()` — with D decoys a `D/(D+1)` roll negates the round and shatters one decoy. Decoy count: `min(MAX, BASE + floor((bossLevel-1)/LEVELS_PER_DECOY))`. In-fight FX via `_emitFx` + a `_floatText` helper ("ILLUSION" on shatter, "SHE SPLITS" on re-split). **Visible decoy sprites:** `BossRenderer` subscribes to `SUCCUBUS_DOPPEL_SPLIT` / `SUCCUBUS_DOPPEL_SHATTER` (+ `BOSS_FIGHT_RESOLVED` to clear) and renders translucent pink (`0xffaad6`, alpha 0.5) duplicate sprites of the Queen — same `<skin>-idle` sheet, scale, and live animation — fanned out to alternating sides; `_syncDecoys` fades them in on a split, `_shatterDecoy` pops the outermost with a scale-up + fade. New balance constants: `SUCCUBUS_DOPPEL_ENABLED`, `SUCCUBUS_DOPPEL_BASE_DECOYS`, `SUCCUBUS_DOPPEL_LEVELS_PER_DECOY`, `SUCCUBUS_DOPPEL_MAX_DECOYS`, `SUCCUBUS_DOPPEL_SPLIT_THRESHOLDS`. **Bat-Form Seduction** was already implemented (`_tickSuccubus` flight + charm) but never tracked in this table — recorded here now for completeness; its charm cadence was slowed 2026-05-22 (new `SUCCUBUS_CHARM_COOLDOWN_BASE_MS` / `_RAND_MS` constants — recurring cooldown 7–15s → 20–36s, first-charm delay 3–8s → 10–18s) per the user asking it to trigger less often. |
| Migration   | Retire / unlock-gate the existing 15 archetypes | — | TBD | ✅ DONE (2026-04-28) | replaced wholesale; old archetype IDs gone, neutral defaults retained for backward-compat in modifier readers |
| Challenge-mode removal | NO TRAPS / ALL RAIDS / HARDCORE toggles dropped | — | — | ✅ DONE (2026-04-28) | runConfig field removed from ArchetypeSelect, BossSystem hardcoded to 3 lives, NightPhase trap palette no longer reads runConfig.noTraps, DayPhase guild-raid override gone |
| ArchetypeSelect re-skin | Existing carousel UI re-themed for 10 monster types | — | — | ✅ DONE (2026-04-28) | bestiary-book scene reads `bossArchetypes.json` directly; auto-updates whenever the JSON changes |

---

## 2. Personalities (target: 18 from design + open-ended more)

| ID | Name | Phase | Status | Notes |
|---|---|---|---|---|
| greedy | Greedy | 5 | ✅ DONE | data + AI-goal influence |
| speed_runner | Speed Runner | 5 | ✅ DONE | data |
| paranoid | Paranoid | 5 | ✅ DONE | data + 0.55× movement in non-starter rooms (6c proxy until knowledge in Phase 8) |
| party_loyal | Party Loyal | QW | ✅ DONE | AISystem rallies to DEFEND_ALLY goal when a party-mate drops below 40% HP; releases when ally heals back to 60%+ |
| solo | Solo | QW | ✅ DONE | AISystem strips partyId on first tick (records formerPartyId on flags), emits SOLO_SPLIT |
| raid_leader | Raid Leader | QW | ✅ DONE | AISystem._onAdventurerDied cascades flee to all surviving party-mates when a raid_leader falls (RAID_LEADER_FELL event) |
| completionist | Completionist | 5 | ✅ DONE | data + EXPLORE_ROOM steering |
| cartographer | Cartographer | 5 | ✅ DONE | data + full-accuracy map share on flee (8 — KNOWLEDGE_CARTOGRAPHER_BOOST) |
| vandal | Vandal | — | 🚫 REMOVED 2026-04-29 | Personality scrapped during Phase 5c ability rework. Trap-disarm role is now exclusive to Ranger's Trap Expert ability. JSON entry deleted from personalities.json + personalityCombos.json (vandal_speedrunner combo gone) + chatLines.json. TrapSystem._tryVandalDisarm logic removed. Old saves with vandal personality may fail to load (acceptable for jam). |
| martyr | Martyr | 5 | ✅ DONE | data + taunt at low HP (6c — redirects minion aggro) |
| underdog | Underdog | 5 | ✅ DONE | data + 2× adventurer XP per kill (7b — adventurer XP/level system added) |
| inquisitor | Inquisitor | 5 | ✅ DONE | Phase 9b: InquisitorSystem dispels random active mechanic 8s after entry, restores on flee/death/day-end |
| vulture | Vulture | 5 | ✅ DONE | data + skip-combat-when-loot-in-room (7b) + SEEK_LOOT detour via lootPriority 0.85 |
| traumatized | Traumatized | 5 | ✅ DONE | data + PARTY_WIPED reaction (6d — sole survivor flees with full intel flag) |
| the_fan | The Fan | 5 | ✅ DONE | data + spare-idol (6d — picks random minion class to refuse to attack at spawn) |
| coward | Coward | 5 | ✅ DONE | data + flee-on-enemy (6c) + knowledge spread on flee (8 — KNOWLEDGE_COWARD_PARTIAL accuracy) |
| overconfident | Overconfident | 5 | ✅ DONE | low fleeThreshold (0.2) in behaviorWeights makes them stay too long — design intent satisfied without explicit override |
| beast_tamer | Beast Tamer | 5 | 🚫 REMOVED 2026-04-29 | Promoted to a Class ("Beast Master") in §3 with single-companion mechanic; personality entry deleted from personalities.json. Old saves with this personality will fail to load (acceptable for jam). |

---

## 3. Adventurer classes (target: 5 from design + class pool expansion)

| ID | Name | Phase | Status | Notes |
|---|---|---|---|---|
| knight | Knight | 4 | ✅ DONE | stats + class tag |
| mage | Mage | 4 | ✅ DONE | stats + spellcasting + mana drain (6c) + melee fallback when out of mana |
| cleric | Cleric | 5 | ✅ DONE | stats + tags + heal_ally + smite_undead (6c bonus damage vs undead) |
| necromancer | Necromancer | 5 | ✅ DONE | stats + tags + raise_corpse (6d — 2 raises/day, 10 mana, defects on adventurer faction) |
| twitch_streamer | Twitch Streamer | 5 | ✅ DONE | Phase 10b: AISystem chat_poll redirects to random unvisited room every ~10s |
| rogue | Rogue (bonus, not in design) | 4 | ✅ DONE | extra class on top of design — confirm with user if to keep |
| ranger | Ranger | 6e | ✅ DONE — class data + arrow consumption per shot + melee fallback + flee on empty quiver |
| beast_master | Beast Master | 5b | ✅ DONE (re-audit 2026-05-22) | `bm_tame_beast` (50% success, defects to adventurer faction) + `adv.companionId` tracking + `bm_scout_ahead` (1/day reconnaissance) all shipped in `ClassAbilitySystem.js`. Per DESIGN.md line 170 / 181 the original `command_beast` ability was removed and replaced by Scout Ahead — no longer pending. |
| barbarian | Barbarian | 5b | ✅ DONE (re-audit 2026-05-22) | ✅ Rage damage scaling × (1 + (1 − hpFrac)) up to 2× at 1 HP — `CombatSystem.js` ~line 350. ✅ `Unstoppable` passive — `AISystem.js:2067-2068` short-circuits the flee check for `classId === 'barbarian'` ("immune to ALL flee triggers"). `Break Door` is intentionally dormant per design (depends on locked-door event which doesn't ship yet). |
| monk | Monk | 5b | ✅ DONE (re-audit 2026-05-22) | ✅ `monk_focus` — 30% dodge window for 5s every 14s on the focused adv (`CombatSystem.js` ~line 83). ✅ `monk_inner_peace` — large CD +1 HP/sec regen for 8s (`ClassAbilitySystem.js:39, 575-585`). Per DESIGN.md line 170 the original separate `armor_pierce` ability was removed (the "half-defense" identity is delivered through Focus + dodge in the current per-class spec). |
| bard | Bard | 5b | ✅ DONE (re-audit 2026-05-22) | ✅ `bard_inspire` (+15% ATK aura, 2-tile, set duration) + ✅ `bard_speed` (+20% SPD aura, 2-tile, set duration) + ✅ `Encore` passive (`ABILITY_TRIGGERED { abilityId: 'encore' }` on bard death heals party 25%, gated by Crusader's Curse) — all in `ClassAbilitySystem.js`. |

---

## 4. Personality combos (target: 4 from design + open-ended more)

| ID | Combo | Phase | Status | Notes |
|---|---|---|---|---|
| greedy_cartographer_clash | Greedy + Cartographer | 5 | ✅ DONE | detected + banner |
| paranoid_speedrunner_split | Paranoid + Speedrunner | 5 | ✅ DONE | combo banner + component effects produce the split organically: paranoid 0.55× speed in unfamiliar rooms, speed_runner faster movement — they drift apart by ~3 tiles within the first room |
| martyr_vulture | Martyr + Vulture | QW | ✅ DONE | AISystem: vulture refuses to engage when a party-mate martyr is taunting (HP ≤ MARTYR_TAUNT_HP_FRACTION). Sets `flags.vultureWaitingForCarnage`, falls through to loot pickup once carnage drops gear |
| raidleader_traumatized | Raid Leader + Traumatized | 5/QW | ✅ DONE | raid_leader cascade-flee fires from AISystem._onAdventurerDied when leader falls; traumatized survivor adds `fullKnowledgeOnFlee` flag — both effects compound when the combo is in play |
| Other combos (8 more added) | — | 5/QW | ✅ DONE | underdog_cleric_fan: 1.5× extra XP mul (`BELIEVERS_BOOST` event); martyr_coward / solo_coward / loyal_traumatized: traumatized full-knowledge flee + solo split + cleric heal-ally all live; vandal_speedrunner: vandal disarm + speed_runner movement; beasttamer_necromancer: tame + raise both fire; greedy_cartographer / completionist_speedrunner / overconfident_paranoid / greedy_overconfident: produced organically by behaviorWeights |
| Visible relationship icons above heads | — | QW | ✅ DONE | AdventurerRenderer renders ★ comboBadge to the right of any adv with activeCombos.length > 0 |
| 💭 More combos | — | 5–10 | 💭 OPEN | grow as new tags land |

---

## 5. Dungeon mechanics (target: 13 from design + tons more)

**Active pact pool (2026-05-02):** **64 pacts** (added pact_of_the_marionette + completed all UI polish from prior batches: LongGamePopup wired into HudScene, SunderedFloorRenderer for telegraph/pit visuals, CartographerOverlay for adv-path dots, Jester name-scramble in BuildMenu trap tab, right-click-trap Brand selection + gold halo in TrapRenderer, 'C' hotkey + two-click Crucible sacrifice flow in NightPhase. Marionette possession: click-to-possess + WASD + auto-attack + camera follow + MinionAISystem suppression of other dungeon minions while possessed; releases on minion death / NIGHT_PHASE_STARTED / DAY_PHASE_STARTED.) **63 pacts** — Phase 9 Batches A–H all shipped today. Original 13 (taxation_of_souls, bloodbound, gold_rush, undying_horde, sealed_paths, pack_synergy, blood_money, hasty_architect, great_erasure, schism, glory_hounds, sworn_rivals, famine_decree) + 50 new (Batch A: gilded_demise, pyramid_scheme, ransom_note, tax_the_living, tower_tax, crusaders_curse; Batch B: kennel_discipline, ironhide_rite, frenzy_pact, last_stand_doctrine, mage_hunt, vampires_toll; Batch C: tyrants_gaze, soul_tether, avengers_rite, final_breath; Batch D: false_maps, whispered_lies, open_book, whisperers_tongue; Batch E: doomsday_clock, the_long_game, inquisitors_mark; Batch F: summon_adds_i/ii/iii, drill_sergeant, endless_garrison, the_cull, trap_masons_touch, trapsmiths_guild, forbidden_workshop, architects_vision; Batch G: hellfire_breath, lightning_strike, shockwave_slam, spectral_reach, dark_vortex, soul_drain, doppelgangers, petrifying_stare; Batch H: cursed_soil, sundered_floor, pact_of_the_mirror, pact_of_the_cartographer, pact_of_the_jester, pact_of_the_whisperer, pact_of_the_brand, pact_of_the_reaper, pact_of_the_crucible). All entries have JSON defs, balance constants, handlers in DungeonMechanicSystem, and integration across AISystem/CombatSystem/MinionAISystem/BossSystem/TrapSystem/KnowledgeSystem/NightPhase/DayPhase. Each batch live-verified via `preview_eval`. **Deferred:** pact_of_the_marionette (possession system — own focused task). **Polish remaining:** Long Game popup (currently fade-banner), Cartographer path overlay, Jester BuildMenu name-scramble, Brand night-phase trap-selection UI, Crucible sacrifice UI, Sundered Floor pitch-black tile renderer. The 11 entries below marked 🚫 REMOVED were cut from the pact offering pool on 2026-05-02 at user request.

| ID | Name | Phase | Status | Notes |
|---|---|---|---|---|
| mimicry_plague | Mimicry Plague | 10 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| taxation_of_souls | Taxation of Souls | 9 | ✅ DONE | ROOM_OBSERVED first-visit damage + 0.7× essence yield penalty |
| gravitational_anomaly | Gravitational Anomaly | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| cursed_fountains | Cursed Fountains | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| no_health_regeneration | No Health Regeneration | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| memory_fog | Memory Fog | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| eternal_night | Eternal Night | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| hunger | Hunger | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| bloodbound | Bloodbound | 9 | ✅ DONE | MinionAISystem.respawnAll filters dead permanently + CombatSystem 1.5× damage mult |
| gold_rush | Gold Rush | 9 | ✅ DONE | flag-only; AISystem essence yield 2× via MECHANIC_GOLD_RUSH_GOLD_MULT; +1 adv/day spawn modifier |
| undying_horde | Undying Horde | 9 | ✅ DONE | MINION_DIED 40% revive at half HP; flag prevents evolve, dies permanently next time |
| sealed_paths | Sealed Paths | 9 | ✅ DONE | AISystem flee path: 50% reroute back into dungeon; cornered-flee +25% damage |
| pack_synergy | Pack Synergy | 9 | ✅ DONE | flag-only; CombatSystem reads flag, +15%/ally-in-room cap +60% |
| blood_money | Blood Money | 9 | ✅ DONE | RESOURCES_AWARDED kill subscriber; +1 gold/night per lifetime kill; +2% adv HP per 5 kills |
| knowledge_is_pain | Knowledge is Pain | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| paranoia_protocol | Paranoia Protocol | 9b | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| spectral_reinforcements | Spectral Reinforcements | 9b | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| loot_curse | Loot Curse | 9 | 🚫 REMOVED 2026-05-02 | Cut from pact pool. |
| hasty_architect | The Hasty Architect | 9 | ✅ DONE | rare; flag-only activate; NightPhase._effectiveTrapCost applies 50% discount at validate/place + palette display; TrapSystem._fireTrap rolls 25% jam; BuildMenu shows discounted cost on trap slots |
| great_erasure | Pact of the Great Erasure | 9 | ✅ DONE | legendary; ADVENTURER_ENTERED_DUNGEON handler doubles hp/attack/speed; KnowledgeSystem._onAdventurerFled bails before survivor-record save when flag is set |
| schism | Schism | 9 | ✅ DONE | epic; ADVENTURER_ENTERED_DUNGEON nulls partyId + sets noFlee flag; AISystem._setFleeGoal returns on noFlee |
| glory_hounds | Glory Hounds | 9 | ✅ DONE | rare; ADVENTURER_ENTERED_DUNGEON sets noFlee; CombatSystem +50% adv damage when hpFrac <= 0.30 |
| sworn_rivals | Sworn Rivals | 9 | ✅ DONE | epic; ADVENTURER_ENTERED_DUNGEON pairs first two same-party arrivals via flags.swornRivalOf; AISystem combat fork attacks rival in melee when both <= 50% HP; CombatSystem +25% adv damage at full HP |
| famine_decree | Famine Decree | 9 | ✅ DONE | rare; flag-only; CombatSystem scales adv damage by hpFrac (1.5× at 100%, 0.5× below 50%) |
| End-of-day mechanic offering UI | — | 9 | ✅ DONE | EndOfDay scene shows 3 cards filtered by archetype + dungeon level |
| Tradeoff display in offering UI | — | 9 | ✅ DONE | Each card shows tradeoffDescription in gold italic |
| 💭 Many more mechanics | — | 9–10 | 💭 OPEN — TARGET EXCEEDED (audit 2026-05-22) | aim was ≥30 by 1.0; current pool: **64** in `src/data/dungeonMechanics.json`. |

---

## 6. Dungeon room types

**Note (2026-04-30):** The original 9-room design list and the Phase-2/QW additions were **superseded by the Room redesign 2026-04-30** in DESIGN.md. The new spec defines 21 rooms organized around gateway/cap/scaling rules. Existing handler code for removed rooms remains in place as orphaned no-ops; cleanup is a follow-up phase.

### 6a. Required / Fixed

| ID | Name | Cap | Unlock | Phase | Status | Notes |
|---|---|---|---|---|---|---|
| boss_chamber | Boss Chamber | 1 (fixed) | Start | 2 | ✅ DONE | fixed centerpiece, kept |
| entry_hall | Entry Hall | 1 / 2 / 3 (forced) | Start; L5; L10 | 3 | ✅ DONE | required entrance; multi-entry (2026-05-20) — random per-adventurer spawn entry + flee-to-nearest, 2nd Entry Hall forced @ boss L5, 3rd @ L10 |

### 6b. Starter (free, L1)

| ID | Name | Cap | Unlock | Phase | Status | Notes |
|---|---|---|---|---|---|---|
| starter_corridor | Corridor | scales 2→20 (+2/level) | L1, free | 2 | ⏳ PENDING (audit 2026-05-22) | Room exists, no behaviour. Cap-scaling-by-level still not implemented. |
| starter_barracks | Barracks | scales 1→5 | L1, free | 2 | 🟡 PARTIAL (audit 2026-05-22) | ✅ +10 roster slots per Barracks wired (`NightPhase.js` ~line 1714); ✅ sneakable behaviour data present; ✅ roster-vs-garrison minion split shipped (`Minion.js` `class` field defaults to `'roster'`, NightPhase enforces cap, garrison minions exempt). **Remaining**: per-Barracks 1→5 cap-scaling-by-level. |
| starter_guard_post | Guard Post | unlimited | L1 | TBD | ✅ DONE (audit 2026-05-22) | `MinionAISystem._pickTarget` adds a Guard-Post forward-operating-base check (`isGuardPost`) — minions whose home room is a starter_guard_post engage adventurers in any room directly door-connected to the post, bypassing the same-room requirement but NOT escalating to whole-dungeon hunt. Existing `_walkAlongPath` handles cross-door travel; existing no-target return-home flow brings them back when the connected rooms clear. Garrison minions still hard-bound. |

### 6c. New rooms — Room redesign 2026-04-30

| ID | Name | Cap | Unlock | Phase | Status | Notes |
|---|---|---|---|---|---|---|
| crypt | Crypt | 3 | L2 | TBD | ✅ DONE (audit 2026-05-22) | `RoomBehaviorSystem._onNightStart` spawns 4 garrison Risen Bones per Crypt, refilled nightly, room-bound (`class: 'garrison'`, `assignedRoomId` pinned). |
| trap_factory | Trap Factory | scales 1→5 | L3 | TBD | ✅ DONE (audit 2026-05-22) | Each Factory adds `f.trapSlotsPerFactory ?? 5` to the global trap-slot cap (`NightPhase.js` ~line 1883, surfaced in LeftPanels). No upgrade tree per design. |
| treasury | Treasury | scales 1→5 | L3 | TBD | 🟡 PARTIAL (audit 2026-05-22) | ✅ Daily flat 5-gold stipend per Treasury (`RoomBehaviorSystem.js` line 60-64) — replaces the original 4-chest model after the 2026-05-02 loot-pickup retirement. **Remaining (or DEVIATION_NOTED?)**: original design had "4 chests + alive-exit-required theft + raises adventurer arrival rate"; both the chest model and the arrival-rate raise are not implemented. User to confirm whether to keep stipend-only or re-design. |
| armory | Armory | scales 1→3 | L3 | 🟡 PARTIAL (audit 2026-05-22) | ✅ Adjacency +2 ATK buff for minions in rooms door-connected to an active Armory (`CombatSystem.js` ~line 567). **Remaining**: 1→3 cap-scaling-by-level. |
| library_of_whispers | Library of Whispers | 1 | L2 | TBD | 🟡 PARTIAL (audit 2026-05-22) | ✅ Tier-1 forecast: next day's party size + class composition computed nightly into `meta.nextPartyPreview` + `LIBRARY_FORECAST` event (`RoomBehaviorSystem.js` ~line 70). **Remaining**: L6 +personalities / L8 +stats/equipment / L10 +planned route tier scaling. |
| watchtower | Watchtower | 2 | L5 | TBD | ✅ DONE (audit 2026-05-22) | First-strike aura: minions in any room door-connected to an active Watchtower get one free hit on adv entry, bypassing range + cooldown, runs through `CombatSystem._computeDamage` so armory/marked modifiers still apply (`RoomBehaviorSystem._fireWatchtowerStrike`). |
| wandering_gate | Wandering Gate | 1 | L6 | TBD | ✅ DONE (audit 2026-05-22) | 60% neighbour / 35% any built room / 5% Boss Chamber teleport on entry, per-day cooldown, Twitch Streamers excluded from boss bucket (`RoomBehaviorSystem._teleportFromWanderingGate`). |
| veil_of_forgetting | Veil of Forgetting | 1 | L6 | TBD | ✅ DONE (audit 2026-05-22) | Each NIGHT_PHASE_STARTED, scrubs `knowledge.sharedPool` entries (rooms / enemiesPerRoom / traps / loot) for every room door-connected to each Veil, excluding boss_chamber + entry_hall. Emits `VEIL_ERASED_INTEL`. |
| catacombs | Catacombs | 2 | L7 | TBD | ✅ DONE (audit 2026-05-22) | Death-in-room → spawn Tier-2 Revenant garrison via `AISystem._kill` (~line 3097). |
| mimic_vault | Mimic Vault | 1 | L7 | TBD | ✅ DONE — REWORKED 2026-05-22 | Disguised as Treasury; 2 chest-disguised mimics per day (state: 'chest'). Each mimic rolls a `chestTier` (1..10) at spawn — MinionRenderer paints them as the matching `item-treasure-chest-<tier>` sprite with a red tint (`0xff5050`) for player visibility; adv AI ignores the tint. KnowledgeSystem.observeRoomContents adds disguised mimics to `adv.knowledge.treasureChests` with an `_isMimic` flag so the standard chest-tempt loop picks them up. `AISystem._tryTriggerMimic` (runs alongside `_tryOpenTreasureChest` on every tile step) springs an unknowing adv within 1 tile: `mimicState→'sprung'`, MIMIC_SPRUNG emitted, all alive advs + sharedPool gain `knowledge.mimics[id]`, opener killed via `_kill`. `_resetSprungMimics` flips state back to 'chest' on NIGHT_PHASE_STARTED. Knowledge-aware advs see through the disguise (allow-list in MinionAISystem `_pickTarget` adv-side filter); MinionAISystem retaliation carve-out lets mimics swing back when attacked. The placeable Mimic minion (Barracks roster) uses the exact same path. *(Reworked from the original 40%/5% open-roll + 30% max-HP bite + reveal-and-engage mechanic — replaced with stationary instant-kill trap per user 2026-05-22.)* |
| hall_of_trials | Hall of Trials | scales 1→3 | L7 | TBD | 🟡 PARTIAL (audit 2026-05-22) | ✅ Nightly spawn of random T2 evolved minion (garrison) per Hall, if none alive (`RoomBehaviorSystem.js` ~line 219). **Remaining**: 1→3 cap-scaling-by-level. |
| wishing_well | Wishing Well | 1 | L8 | TBD | ✅ DONE (audit 2026-05-22) | Coin flip on first entry per day. Heads: +3 ATK, +20 maxHp, full heal. Tails: `flags.marked` = +50% damage from minions until end of day (`RoomBehaviorSystem._rollWishingWell`). |
| false_exit | False Exit | 1 | L8 | QW | ✅ DONE (audit 2026-05-22) | Fleeing adventurers entering a False Exit are teleported adjacent to the boss chamber and forced into AT_BOSS goal + fighting state — they thought they were leaving, the dungeon kept them (`RoomBehaviorSystem._teleportToNearBoss`). Twitch Streamers excluded. |
| hall_of_madness | Hall of Madness | 1 | L9 | TBD | ✅ DONE (audit 2026-05-22) | Friendly-fire AI state: while in a Hall of Madness, an adventurer rolls each tick to attack a same-party ally (`AISystem.js` ~line 2161), saves their previous goal, and restores it on leaving the room. New `ATTACK_ALLY` goal type. |
| throne_room | Throne Room | scales 1→2 | L9 | TBD | ✅ DONE — DEVIATION (audit 2026-05-22) | 1 Mini-Boss per Throne Room nightly, garrison/room-bound, stats scaled by current boss level via `MINION_HP_PER_BOSS_LV` + `MINION_ATK_PER_BOSS_LV` (`RoomBehaviorSystem.js` ~line 246). DEVIATION: original "T1→T2→T3 family progression" simplified to a fixed skeleton3 baseline whose stats scale instead. Visual family-swap is deferred polish; mechanic intent (escalating threat) is preserved via stat scaling. Noted in DESIGN.md 2026-05-22. |
| sanctum | Sanctum | 1 | L10 (capstone) | TBD | ✅ DONE (audit 2026-05-22) | Boss HP regen between fights: +8 HP per Sanctum per round (`BossSystem.js` ~line 1835). Minion regen aura: minions in any room door-connected to an active Sanctum get the barracks-style regen treatment (`MinionAISystem._isAdjacentToSanctum`). |

### 6d. Top-level rules (Room redesign 2026-04-30)

| Rule | Phase | Status | Notes |
|---|---|---|---|
| Roster vs Garrison minion split | TBD | ✅ DONE (audit 2026-05-22) | `Minion.js` carries `class: 'roster' \| 'garrison'` (default `'roster'`). `NightPhase._rosterUsed/_rosterCap` enforces +10-per-Barracks cap; garrison minions (Crypt, Mimic Vault, Hall of Trials, Throne Room, Catacombs revenants) are excluded from the cap and AI-pinned to their `assignedRoomId`. |
| "Adjacent / connected" = direct shared door | TBD | ✅ DONE (audit 2026-05-22) | `dungeonGrid.getNeighborRooms(instanceId)` returns rooms that share a connection-point, not transitive — consumed by Veil, Sanctum, Watchtower, Wandering Gate. |
| Alive-exit-required for chest/loot theft | TBD | 🚫 SCOPE_CHANGED (audit 2026-05-22) | Original 4-chest Treasury + loot-pickup-on-escape model was retired 2026-05-02 in the loot-pickup cleanup. Treasury became a flat daily stipend; Mimic Vault became a chest-disguise-and-bite mechanic. The "alive-exit-required" rule is no longer relevant to the current design. |
| Cap scaling by boss level | TBD | 🟡 PARTIAL (audit 2026-05-22) | Some scaling exists (throne-room mini-boss stats, library forecast size, beacon buff bonus). The unified per-room cap-scaling-by-level table from DESIGN.md is **not** implemented — caps are mostly fixed-per-instance with +X-per-Room aggregation. |
| Boss level cap = 10 | TBD | ✅ DONE (audit 2026-05-22) | Boss level capped at 10 in current BossSystem. |

### 6e. Removed (data deletion 2026-04-30, handler cleanup deferred)

| ID | Name | Phase | Status | Notes |
|---|---|---|---|---|
| hall_of_echoes | Hall of Echoes | 3 | 🚫 REMOVED 2026-04-30 | data deleted; AISystem sound-alert handler orphaned, cleanup later |
| treasure_room | Treasure Room | 3 | 🚫 REMOVED 2026-04-30 | superseded by Treasury; loot/raid logic orphaned |
| healing_fountain | Healing Fountain | 3 | 🚫 REMOVED 2026-04-30 | data deleted; heal-on-stand handler orphaned |
| necropolis_wing | Necropolis Wing | QW | 🚫 REMOVED 2026-04-30 | user explicitly removed from spec; RoomBehaviorSystem._onNightStart corpse-raise orphaned |
| colosseum | Colosseum | QW | 🚫 REMOVED 2026-04-30 | gate-lock + wave-spawn handler orphaned |
| mirror_maze | Mirror Maze | 8b | 🚫 REMOVED 2026-04-30 | KnowledgeSystem MIRROR_MAZE_KNOWLEDGE_ACCURACY orphaned |
| obelisk_room | Obelisk Room | QW | 🚫 REMOVED 2026-04-30 | heal/charge dual-state handler orphaned |
| trap_room | Trap Room | 3 | 🚫 REMOVED 2026-04-30 | superseded by Trap Factory + slot model |
| prison_block | Prison Block | QW | 🚫 REMOVED 2026-04-30 | detain handler orphaned |
| serpent_pit | Serpent Pit | QW | 🚫 REMOVED 2026-04-30 | poison-tick handler orphaned; may return as a trap |
| lava_floor | Lava Floor | 6+ | 🚫 REMOVED 2026-04-30 | hazard rooms dropped; revisit as trap later |
| collapsing_pillars | Collapsing Pillars | 6+ | 🚫 REMOVED 2026-04-30 | hazard rooms dropped; revisit as trap later |
| secret_passage | Secret Passage | 6+ | 🚫 REMOVED 2026-04-30 | not in new spec |
| power_core | Power Core | 6+ | 🚫 REMOVED 2026-04-30 | superseded by per-Trap-Factory slot model |

### 6f. Open-ended

| Item | Phase | Status |
|---|---|---|
| 💭 More room types beyond the 21 | 6–10+ | 💭 OPEN |

---

## 7. Trap types

**2026-05-20 redesign.** The original 3 standard + 9 interaction traps were scrapped — none ever shipped (`trapTypes.json` was always `[]`; the old `TrapSystem`/`Trap`/`TrapRenderer` code targeted that dead roster). Replaced by the 8-trap roster below. Old rows preserved in git history.

### 7a. Current roster (8 traps) — ✅ DONE

| ID | Name | Footprint | Placement | Trigger | Unlock | Gold | Status |
|---|---|---|---|---|---|---|---|
| spike_pillar | Spike Pillar | 2×2 | floor | adv within 1 tile | L3 | 20 | ✅ DONE |
| shooting_arrows | Shooting Arrows | 1 wall tile | wall (not doorway) | adv in LOS lane | L4 | 30 | ✅ DONE |
| rotating_blades | Rotating Blades | 2×2 | floor | adv in adjacent tile | L4 | 25 | ✅ DONE |
| bomb | Bomb | 1×1 | floor | adv within 2 tiles → 3s fuse | L5 | 35 | ✅ DONE |
| saw_blade | Saw Blade | 3-tile track | floor | saw overlaps adv | L6 | 35 | ✅ DONE |
| spike_pit | Spike Pit | 2×2 | floor (fully interior) | stepped on | L6 | 40 | ✅ DONE |
| cannon | Cannon | 1×1 | floor | adv in LOS (faced dir) | L7 | 45 | ✅ DONE |
| dragon_trap | Dragon Trap | 1 wall tile | wall (not doorway) | adv in LOS lane | L8 | 55 | ✅ DONE |

Locked specs: 1 Trap-Factory slot each; never in boss room / entry hall. Trap Factory (gateway room) unlocks at boss L3; traps unlock L3–8. Trap damage scales +12%/boss level (`Balance.TRAP_DAMAGE_PER_BOSS_LV`); trap gold cost scales +20%/boss level (`Balance.TRAP_COST_PER_BOSS_LV`) — both mirror the minion system. Gold values in `trapTypes.json` are the level-1 base. Knowledge gained only on trigger or damage-survived. Area/contact hazards hit both factions; LOS projectile traps hit adventurers only and are confined to the trap's own room (lane stops at walls/doors).

### 7b. Cross-cutting infrastructure (new — none of this existed) — ✅ DONE

| Item | Phase | Status |
|---|---|---|
| Multi-tile (2×2) footprint + placement validation | TRAPS | ✅ DONE |
| Wall-mounted placement category + auto-door suppression | TRAPS | ✅ DONE |
| Trap collision (solid-tile registration for pathfinding) | TRAPS | ✅ DONE |
| Line-of-sight scan (room-confined; canned projectile animations) | TRAPS | ✅ DONE |
| Proximity / radius / adjacent-contact triggers | TRAPS | ✅ DONE |
| Continuous-damage zones (blades / saw / pillar) | TRAPS | ✅ DONE |
| Moving trap (saw track travel — time-based wave) | TRAPS | ✅ DONE |
| Trap rotation + R-key handling (cannon only) | TRAPS | ✅ DONE |
| Poison damage-over-time status effect | TRAPS | ✅ DONE |
| Bomb fuse countdown + 5-tile-radius explosion + chain | TRAPS | ✅ DONE |
| Knowledge gain (trigger / damage-survived) → pathfinder routing | TRAPS | ✅ DONE |
| Trap damage scales with boss level (+12%/lvl) | TRAPS | ✅ DONE |
| Rewrite `Trap.js` / `TrapSystem.js` / `TrapRenderer.js` (old roster) | TRAPS | ✅ DONE |

---

## 8. Minion / monster types

### 8a. Behavior types

| Type | Phase | Status |
|---|---|---|
| patrol | QW | ✅ DONE | MinionAISystem shuffles idle patrol minions to a random walkable tile in their home room every 3s |
| guard | 6 | ✅ DONE — stand at home tile, engage in same room |
| hunt | QW | ✅ DONE | MinionAISystem._pickTarget skips ENGAGE_REQUIRES_SAME_ROOM when minion.behaviorType === 'hunt' |
| sleeping (in barracks) | QW | ✅ DONE | MinionAISystem._tickMinion regens 0.5 HP/sec for idle dungeon-faction minions in starter_barracks when no adventurer is present |

### 8b. Special types (target: 9 from design)

| ID | Name | Phase | Status | Notes |
|---|---|---|---|---|
| sapper | Sapper | 6e | ✅ DONE — repairs triggered traps in same room (~2.5s per trap) |
| herald | Herald | 6e | ✅ DONE — alerts adjacent rooms when adventurer enters (5s alert window) |
| engineer | Engineer | 6e | ✅ DONE — passive 1.25× damage buff on traps in same room |
| scavenger | Scavenger | QW | ✅ DONE | MinionAISystem._tickScavenger drags floor loot in same room to nearest treasure_room every 4s |
| mimic_handler | Mimic Handler | QW | ✅ DONE | personalities.json + AISystem._pickNextGoal filters out item.isMimicSpawn loot for mimic_handler-tagged advs |
| whisperer | Whisperer | QW | ✅ DONE | MinionAISystem._tickWhisperer corrupts a random sharedKnowledge entry every 10s — accuracy capped at 0.3, trap tile coords jittered ±2 |
| cleaner | Cleaner | QW | ✅ DONE | MinionAISystem._tickCleaner marks recent corpses in same room as collected, denying vulture loot grabs |
| mourner | Mourner | 6e | ✅ DONE — stacking attack buff on each same-room ally death (+2 attack/death) |
| echo | Echo | QW | ✅ DONE | MinionAISystem stamps minion.mimickedClassId from the last-seen adventurer; CombatSystem._computeDamage uses it for class-flavored damage (mage spells, cleric smite, ranger arrows, rogue backstab) |

### 8c. Existing (in `minionTypes.json`)

| ID | Phase | Status |
|---|---|---|
| skeleton_warrior | 6 | ✅ DONE | full data + AI + combat |
| skeleton_archer | 6 | ✅ DONE | ranged guard, attackRange 4 |
| bone_brute | 6 | ✅ DONE | tank, slow heavy melee |

### 8d. Open-ended

| Item | Phase | Status |
|---|---|---|
| 💭 Many more types unlocked as you play | 6–10 | 💭 OPEN |

---

## 9. Evolution Trees (kill-method-driven)

| Evolution | Trigger | Phase | Status |
|---|---|---|---|
| Plague Bringer | killMethod: poison, 3 kills | 7 | ✅ DONE — data + EvolutionSystem condition matcher |
| Shadow Stalker | killMethod: backstab, 3 kills | QW | ✅ DONE | CombatSystem._inferMethod returns 'backstab' when a rogue strikes a non-engaging target |
| Mana Eater | killedClassType: mage, 5 kills | 7 | ✅ DONE — data + condition matcher |
| Bone Crusher | killedClassType: knight, 5 kills | 7 | ✅ DONE |
| Lich Apprentice | survives 10 days no kills | 7 | ✅ DONE — daysAliveWithoutKill condition |
| Vengeful Wraith | killed by adventurer 3+ times, then respawned | 7b | ✅ DONE — `timesKilledAndRespawned` counter, evolution check fires on MINION_RESPAWNED |
| Kill events carry damageType + method | — | 6 | ✅ DONE — CombatSystem emits both fields |
| EvolutionSystem applies stat deltas + new abilities | — | 7 | ✅ DONE |
| 💭 Many more evolution paths | 7–10 | 💭 OPEN | |

---

## 10. Boss fight & abilities

| Item | Phase | Status |
|---|---|---|
| Auto-resolved boss fight (no boss control) | 10 | ✅ DONE | BossSystem._resolve runs iterative slug-fest: party totalAtk vs boss def vs boss atk spread across alive party until either side wipes |
| 3 boss deaths → game over → eulogy → new run | 10 | ✅ DONE | gameState.boss.deathsRemaining; BOSS_DEFEATED_FINAL → GameOver scene with eulogy + NEW RUN button |
| Boss evolution tree (XP-spent abilities) | 10b | ✅ DONE | bossAbilities.json with 6 nodes (3 tiers); BossSystem.unlockAbility ~~spends Dark Power~~ (Dark Power retired 2026-05-05 — currency layer needs a follow-up pass to switch to Gold or remove the cost), requires-chain enforced; EndOfDay BOSS UPGRADES modal |
| Summon adds ability | 10b | ✅ DONE | summon_adds ability spawns 2 skeleton helpers in boss chamber on each fight |
| Environmental hazards (lava floor, falling pillars) | 10b | ✅ DONE | lava_floor (3 HP/sec passive fire), collapsing_pillars (random ~4s 8 dmg burst) rooms wired in AISystem._applyRoomEffects |
| 💭 Many more abilities | 10b | 💭 OPEN — 6 abilities in tree; expand in future passes |

---

## 11. Knowledge system

| Item | Phase | Status |
|---|---|---|
| Per-adventurer knowledge of rooms / minions / traps | 8 | ✅ DONE — `adv.knowledge.{rooms,traps,minions}` populated by KnowledgeSystem |
| Knowledge accumulates as adventurer enters dungeon | 8 | ✅ DONE — `observeCurrentRoom` per tick + `observeMinion` on engagement scan |
| Knowledge sharing on flee (to other adventurers) | 8 | ✅ DONE — merges into `gameState.sharedKnowledge` with personality-specific accuracy |
| Knowledge accuracy flag (some intel is wrong) | 8 | ✅ DONE — accuracy multiplier, `'rumor'` source, partial accurate flag |
| Adventurer returns leading party with prior knowledge | 8 | ✅ DONE — *was silently dead until 2026-05-20:* `rollReturnLeader()` was never implemented, so the DayPhase returning-leader block never ran (every wave inherited at the partial rate, never the full-briefing 1.0). `rollReturnLeader()` now exists — KNOWLEDGE_RETURN_CHANCE gates a recently-fled survivor (within KNOWLEDGE_RETURN_MAX_AGE_DAYS) returning as the wave leader |
| Knowledge affects pathfinding cost | 8 | ✅ DONE — PathfinderSystem accepts `costFn`; trap-known tiles get 6× multiplier |
| Veteran return notification | 8 | ✅ DONE (2026-05-20) — returning leader carries `escapeCount` → ToastQueue "VETERAN APPROACHING" toast on entry; also emits `VETERAN_APPROACHING` for the Dungeon Log |
| Veterans stronger than normal adventurers | 8 | ✅ DONE (2026-05-20) — boss-level scaled like any adventurer, then ×`KNOWLEDGE_VETERAN_HP_MULT` (1.2) / ×`KNOWLEDGE_VETERAN_ATK_MULT` (1.15) |
| Veterans worth double gold | 8 | ✅ DONE (2026-05-20) — `goldMul ×2` for `returningVeteran` in the AISystem kill-reward chain |
| Veterans visually distinct at a glance | 8 | ✅ DONE (2026-05-20) — gold aura ring + body stroke + "★ VET n" badge in AdventurerRenderer |
| Returning veteran identity continuity | 8 | ✅ DONE (2026-05-20) — returning leader reuses the survivor's `instanceId` so re-flees accumulate `runCount`; `KnowledgeSystem._onAdventurerDied` purges a veteran killed in the dungeon (their championed intel leaves the shared pool) |

---

## 12. Visual feedback (set-and-forget gameplay)

| Item | Phase | Status |
|---|---|---|
| Knowledge Overlay toggle | 8 | ✅ DONE — DayPhase KNOWLEDGE button; world-space red→blue overlay |
| Thought Bubbles | 5 | ✅ DONE — shows primary personality icon |
| More thought-bubble states (Searching/Scared/Greedy/Healing/Planning) | 6b | ✅ DONE — Fighting/Fleeing/Searching states + personality default |
| Replay Ghost (returning party shows previous run) | 8b | 🚫 REMOVED 2026-05-21 | Cut at user request — the prior-run path trail read as a confusing debug line when a Hero spawned. ReplayGhostRenderer.js stays in the repo but is no longer constructed (Game.js). ADVENTURER_RETURNED + adv.priorPathHistory are retained (NewspaperSystem still uses the sample count for flavor text); only the on-floor dot trail is gone. |
| Combat log (Slay-the-Spire style) | 6b | ✅ DONE — fading event feed bottom-left during day phase |
| Chat bubbles (adventurers talking to themselves/party) | 6b | ✅ DONE — class + personality lines, sparse pacing |
| Time controls (pause/1×/2×/4×) | 4 | ✅ DONE |
| Auto-pause on key events (boss fight, party wipe, mechanic) | 9b | ❌ REMOVED — froze AISystem mid-flee during boss fights; manual time-control buttons suffice |

---

## 13. Mana / Essence economy

| Item | Phase | Status |
|---|---|---|
| Soul Essence (upkeep currency) | 1 | ✅ DONE | renamed to Gold display-side; field still `gameState.player.soulEssence` |
| Dark Power (upgrade currency) | 1 | 🚫 REMOVED 2026-05-05 | currency retired; only Gold remains. `gameState.player.darkPower` may still appear in stale code paths (GameOver.js leaderboard submit, run.totals.souls) — those default to 0 / harmless |
| Daily upkeep deduction at NightPhase start | 3 | ✅ DONE |
| Overdraft → shut off newest rooms | 3 | ✅ DONE |
| Reactivation when essence recovers | 3 | ✅ DONE |
| ESSENCE_WARNING / ESSENCE_CRITICAL events | 3 | ✅ DONE |
| Per-room essenceCostToPlace + upkeepCost | 1 | ✅ DONE |
| Trap upkeep costs | 6b | ✅ DONE — included in EssenceSystem.calculateDailyUpkeep |
| Minion upkeep costs | 6 | ✅ DONE — included in EssenceSystem.calculateDailyUpkeep |

---

## 14. Architectural rules

| Item | Phase | Status |
|---|---|---|
| Some traps need power from a Core room | QW | ✅ DONE | trapTypes flag `requiresPowerSource:true` on greed/mercy/echo_mine/curse_brand; TrapSystem._fireTrap checks `_hasPowerCore()` and silently fizzles (TRAP_FAILED_NO_POWER event) |
| Minions need a Barracks within N rooms | 6 | ✅ DONE — DungeonGrid.hasBarracksWithinDistance + NightPhase validation |
| Treasure rooms must be 3+ rooms deep | 3 | ✅ DONE — placement validation in DungeonGrid |
| `minDepthFromBoss` / `requiresAdjacentTags` placement rules | 3 | ✅ DONE — validation hook exists |
| `requiresPowerSource` validation | QW | ✅ DONE | enforced in TrapSystem._fireTrap — also surfaces TRAP_FAILED_NO_POWER for UI hooks |
| `maxPerDungeon` placement rule | 3 | ✅ DONE |

---

## 15. Loot system

| Item | Phase | Status |
|---|---|---|
| Loot drops on adventurer death | 7 | ✅ DONE — 2 rolls per death, gated by class affinity + dropChance |
| Equip dropped gear to minions | 7 | ✅ DONE — MinionInspector "EQUIP" button moves item from floor → minion equippedGear |
| Loot provenance/history (e.g. "wielded by Sir Aldric") | 7 | ✅ DONE — every drop/equip appends to LootItem.provenance |
| Vendetta system ("avenge my brother" — sibling adventurer hunts gear) | 7b | ✅ DONE — equip flags item, vendettas[] tracked, hunter spawns 35% next day |
| Cursed loot (deliberately let them take) | QW | ✅ DONE | LOOT_DROPPED handler now sets `curseLevel` (1-3 by rarity); lootCurse_tickDebuffs scales attack-loss by level; LootRenderer shows purple halo on cursed items |
| Adventurer hunts for specific mini-boss loot | 7b | ✅ DONE — SEEK_LOOT goal targets floor loot when lootPriority > 0.6 |
| Adventurer leaves to buy gear, returns stronger | 8b | ✅ DONE | RETURNING_GEAR_BONUS_HP/ATK applied to fled-and-returning leader in DayPhase |
| Loot grows in tier as dungeon levels up | 7 | ✅ DONE — maxTier = 1 + floor((dungeonLv-1) / LOOT_TIER_BY_DUNGEON_LEVEL) |
| LootItem.statModifiers / curseLevel / vendetta flags | 7 | ✅ DONE |

---

## 16. Adventurer behavior decisions

| Item | Phase | Status |
|---|---|---|
| Goal: SEEK_BOSS | 4 | ✅ DONE |
| Goal: EXPLORE_ROOM | 5 | ✅ DONE |
| Goal: SEEK_LOOT | 7b | ✅ DONE — fires when adventurer's lootPriority > 0.6 and floor loot exists; pickups transfer to adv.gear |
| Goal: FLEE (leave dungeon) | 6 | ✅ DONE — triggered by hp < personality.fleeThreshold |
| Goal: SLEEP (heal in dungeon) | 6e | ✅ DONE — sleeps when HP < 0.4 + no potions + no hostiles, regens 3 HP/sec |
| Goal: HEAL (use potion / cleric) | 6c | ✅ DONE — adventurers sip potions at HP < 0.4 if any in inventory; cleric heals allies in range |
| Goal: FOLLOW_PARTY | 10 | ✅ DONE | echo personality uses FOLLOW_LEADER goal targeting party-leader's tile (functionally identical) |
| Goal: AVOID_ENEMY (knowledge-driven) | 8 | ✅ DONE | KnowledgeSystem.costMultiplierForTile already applies KNOWLEDGE_TRAP_COST_MULTIPLIER (6×) on tiles with known traps — pathfinder routes around them |
| Goal: SCOUT | 5/8 | ✅ DONE | cartographer-tagged advs prioritise EXPLORE_ROOM goals via personality lootPriority/explorationDrive weights |
| Decision to fight / leave / sleep based on stats | 6/6c/6e | ✅ DONE | AISystem cowardShouldFlee (HP < threshold), resourceExhaustedShouldFlee (mage/ranger), shouldSleep (HP < POTION_HEAL_THRESHOLD) all live |
| Returning next day with knowledge + better gear | 8b | ✅ DONE | KnowledgeSystem.rollReturnLeader + DayPhase priorPathHistory hand-off + RETURNING_GEAR_BONUS_HP/ATK |

---

## 17. Adventurer resources

| Item | Phase | Status |
|---|---|---|
| HP / maxHp | 4 | ✅ DONE |
| Mana (mages, clerics) | 4 | ✅ DONE — mage spells cost 5/swing, cleric heals cost 4 (6c) |
| Arrows (rangers) | 6e | ✅ DONE — ranger class consumes 1 arrow per shot; out → 0.4× melee fallback |
| Potions count | 4 | ✅ DONE — adventurers consume on HEAL goal (6c) |
| Mana regen via standing still | 6d | ✅ DONE — mage regens 0.5 mana/sec when stationary out of combat |
| Resource depletion → leave dungeon | 6e | ✅ DONE — mage out of mana / ranger out of arrows + hostile in room → flee with reason 'out_of_mana'/'out_of_arrows' |
| Buying gear between runs | 8b | ✅ DONE | identical to "returns stronger" — RETURNING_GEAR_BONUS_HP (+8) and RETURNING_GEAR_BONUS_ATK (+2) |

---

## 18. Reputation system

| Item | Phase | Status |
|---|---|---|
| Reputation score in GameState | 1 | ✅ DONE — `meta.reputation` field |
| Reputation grows from adventurer deaths/stories | 10 | ✅ DONE | ReputationSystem accrues +1/kill, +5/bounty, +10/dungeon-level, +5/day-survived; -1 on flee |
| High rep attracts better hunters / legendary heroes / guild raids | 10/10b | ✅ DONE | legendary tier in 10; guild raids in 10b at feared+ reputation |
| Low rep attracts solo scrubs | 10 | ✅ DONE | implicit — base-tier spawn config used when reputation < 75 (whispered tier and below) |

---

## 19. Bounty hunters

| Item | Phase | Status |
|---|---|---|
| Minion-kill threshold (3+) triggers bounty hunter spawn | 7/7b → fixed 2026-05-20, gated 2026-05-25 | ✅ DONE — EvolutionSystem flags `hasBounty` at 3 kills, and DayPhase rolls `BOUNTY_TRACKER_SPAWN_CHANCE` (0.25) per day to spawn a hunter targeting that minion (SEEK_VENDETTA goal). Spawn now requires the wanted minion to have EVOLVED (non-empty `evolutionHistory`) AND no active dungeon event (Tournament/Saboteur/TwitchCon/Cosplay/PATCH 0.0.0/GuildRaid/InfamySpike/NegotiationRefuse) so it doesn't dilute event theming |
| Bounty hunter arrival event banner | 7b | ✅ DONE (2026-05-20) — `BOUNTY_HUNTER_ARRIVED` → top-of-screen EventBanner naming the targeted minion |
| Bounty hunters stronger than normal adventurers | 7b → bumped 2026-05-25 | ✅ DONE — per-day tracker uses `BOUNTY_TRACKER_HP_MULT` (2.2) / `BOUNTY_TRACKER_ATK_MULT` (1.7), event pack stays at `BOUNTY_HUNTER_HP_MULT` (1.6) / `BOUNTY_HUNTER_ATK_MULT` (1.4) |
| Bounty hunters worth extra gold | 7b | ✅ DONE (2026-05-20) — `goldMul ×BOUNTY_HUNTER_GOLD_MULT` (3) in the AISystem kill-reward chain |
| Wanted poster UI (name, kills, gear) | 7b | ✅ DONE — popup banner top-right on MINION_BOUNTY_POSTED; auto-fades 5s |
| Bounty-flagged minions feel "famous" | 7 | ✅ DONE — gold ★ above the sprite (paired with the LV badge), inspector banner, and a gold ★ in the Minion Roster list |

---

## 20. Adventurer-side meta UI

| Item | Phase | Status |
|---|---|---|
| Dossier system (pre-day adventurer info with ?-marks) | 9b/QW | ✅ DONE | DossierPanel reveals progressively: 0 visits → name + ??? for class/tags/stats; 1 visit → class + 1 tag; 2+ → all personalities + stats; returning leaders / vendetta hunters / 3+ visits = fully revealed |
| Click-to-inspect during day phase | 4 | ✅ DONE |
| Trap memory UI ("spent" icons; what adventurers know) | 9b | ✅ DONE | TrapRenderer shows ⌒spent slash on triggered + 👁 eye-badge when any active adventurer or shared pool knows the trap |
| Adventurer "last words" / dying screams | 6b | ✅ DONE — class+killer-keyed lines from lastWords.json |

---

## 21. Minion meta

| Item | Phase | Status |
|---|---|---|
| Minion naming on first level-up | 7 | ✅ DONE — auto-generated name like "Grumbolt the Mage-Slayer" from kill history |
| Player can rename minions | QW | ✅ DONE | MinionInspector ✎ icon next to name opens window.prompt; sets minion.name + emits MINION_NAMED |
| Bounty status flag | 7 | ✅ DONE — `minion.hasBounty` set at BOUNTY_KILL_THRESHOLD kills |
| Per-minion kill history | 6 | ✅ DONE — populated by CombatSystem on kill events |

---

## 22. End-of-Day newspaper

| Item | Phase | Status |
|---|---|---|
| Generated newspaper-style summary | 9 | ✅ DONE | NewspaperSystem.compose() returns headline + body paragraphs |
| Dryly comedic tone | 9 | ✅ DONE | Workplace-memo voice; HR/Operations/bookkeeper framing |
| Mentions specific events (party reductions, traps, mechanics) | 9 | ✅ DONE | Casualties, flees, returned leaders, vendetta, traps fired/disarmed, evolutions, bounties, dungeon level-ups |
| Spread of "secrets" adventurers learned | 9/9b | ✅ DONE | newspaper notes flees + intel; DossierPanel pre-day card shows known personalities/visit history |

---

## 23. End-of-run / game-over UI

| Item | Phase | Status |
|---|---|---|
| Eulogy screen (cinematic, dungeon history) | 10 | ✅ DONE | GameOver scene shows days-survived, top minion of run with kills/level/evolutions, last 5 graveyard guests |
| Every named minion's kill count shown | 10 | ✅ DONE | eulogy shows top minion w/ kill count + level + evolution chain; per-minion drill-down via MinionInspector during run |
| Notable run moments listed | 10 | ✅ DONE | eulogy aggregates days, total kills, casualties, top minion, last 5 graveyard entries, active mechanics |

---

## 24. Adventurer graveyard UI

| Item | Phase | Status |
|---|---|---|
| Persistent graveyard scene/screen | 10 | ✅ DONE | Graveyard scene with scrollable list reachable from GameOver |
| Per-entry: name, class, personality, day died, killer, room, gear | 4 | ✅ DONE | Graveyard renderer shows name + class + day + killer + personalities (room/gear shown via deeper inspector in 10b) |
| Sortable / filterable / searchable | 10b | ✅ DONE | sort chips (recent/day/class/killer) + class filter chips with scene-restart re-render |

---

## 25. Risk / Reward dynamics

| Item | Phase | Status |
|---|---|---|
| Treasure rooms lure raid groups (more XP, but stronger gear leaves) | 7b/10b | ✅ DONE | mini-boss in treasure rooms (7b) + guild-raid spawn at feared+ rep (10b) |
| Tradeoff display on dungeon mechanic offering | 9 | ✅ DONE | EndOfDay offer cards show tradeoffDescription in gold italic |
| Tradeoff display on boss evolution choice | 10b | ✅ DONE | EndOfDay BOSS UPGRADES modal shows ability description per node |

---

## 26. Other ideas (from "Other Ideas" section of design)

| Item | Phase | Status |
|---|---|---|
| Random guild raid teams | 10b | ✅ DONE | DayPhase guild-raid spawn path at feared+ reputation (4-person coordinated party with shared partyId) |
| Adventurers fight among themselves over loot (free XP) | 9b | ✅ DONE | LootGreedSystem scans every 4s for 2+ greedy advs in same room with loot, applies 6% maxHP shove damage to two random combatants |
| Endless mode | 10b | ✅ DONE | GameOver "CONTINUE (ENDLESS)" button resets boss to 1 life, +1 dungeon level, sets runConfig.endless |
| Challenge runs ("no traps", "all raids", etc.) | 10b | ✅ DONE | ArchetypeSelect 3-chip toggle row sets runConfig.{noTraps, allRaids, hardcore} — wired in NightPhase trap palette, DayPhase guild-raid path, BossSystem._init |
| Combat log (condensed, Slay-the-Spire style) | 6b | ✅ DONE |
| Adventurer chat bubbles (talking to selves/party) | 6 | ✅ DONE | ui/ChatBubbles.js — random class/personality lines every 7-15s with 2.2s lifetime |
| Time controls | 4 | ✅ DONE |
| Auto-pause on key events | 9b | ❌ REMOVED — see deviation note below |
| Adventurer last words referencing what killed them | 6b | ✅ DONE |
| Personality interactions (combos) | 5 | ✅ DONE |
| Leaderboard (Supabase backend) | future | 💭 OPEN | requires backend service — explicit defer |

---

## 27. Dungeon-content placements (other than rooms/minions/traps)

| Item | Phase | Status | Notes |
|---|---|---|---|
| Mini-bosses (placeable, guard rooms) | 7b | ✅ DONE — minion placed in treasure_room auto-promotes (3× HP, 1.6× attack), guarantees high-tier drop on death |
| Hidden keys | QW | ✅ DONE | RoomBehaviorSystem._onNightStart drops an iron_key in a random non-boss room each night when a locked room exists but no key is on the floor |
| Locked doors | QW | ✅ DONE | KnowledgeSystem.costMultiplierForTile returns 9999 for tiles inside locked rooms when adventurer lacks an iron_key — A* effectively treats them as impassable |
| Secret rooms | QW | ✅ DONE | secret_passage room type; AISystem._pickNextGoal filters out secret rooms unless adv has `mapper` or `completionist` tag |
| Adventurer level scales with boss level | 7b | ✅ DONE — +10% maxHp, +7% attack per boss level above 1 (applied in DayPhase spawn via ADVENTURER_HP_PER_BOSS_LV / ADVENTURER_ATK_PER_BOSS_LV) |
| Boss level progression (rooms unlock, minions scale) | 7b | ✅ DONE — Boss owns level/xp/xpToNext (moved from meta). Each kill awards BOSS_XP_PER_KILL (10) XP; levels up at curve 100×1.5^(lv-1). Cap 10. BOSS_LEVELED_UP event expands grid + retroactively scales all live minions (+10% HP / +7% ATK per level). Replaces dungeon-level system (2026-05-01). |
| Minions scale with boss level | — | ✅ DONE — applyBossLevelToMinion() applied at spawn, on evolution reset, and retroactively on BOSS_LEVELED_UP. Same +10% HP / +7% ATK curve as adventurers. |
| "Legendary heroes" tier (drawn by high reputation) | 10 | ✅ DONE | DayPhase rolls ReputationSystem.legendarySpawnChance() per spawn; isLegendary flag + 1.5×HP / 1.4×ATK / 1.3×DEF |
| Guild raid teams (full party of coordinated adventurers) | 10b | ✅ DONE | DayPhase._spawnDailyAdventurers branches to a 4-person raid path at feared+ reputation (or whenever runConfig.allRaids is set); shared partyId, +25% HP/atk, 2 personalities each |

---

## 28. Tile / visual / asset

| Item | Phase | Status |
|---|---|---|
| Pixel art tilesets (LoZ-style top-down) | (opportunistic) | ⏳ PENDING — user is sourcing |
| Adventurer sprite sheets | (opportunistic) | ⏳ PENDING |
| Minion sprite sheets | (opportunistic) | ⏳ PENDING |
| Trap / room decoration sprites | (opportunistic) | ⏳ PENDING |
| Tilemap-based renderer (replaces Graphics-based wireframe) | (opportunistic) | ⏳ PENDING |
| 2-thick wall ring + 2×2 doors (geometry change, Phase 1) | QW | ✅ DONE — `Balance.WALL_THICKNESS=2`, `DungeonGrid._writeTiles` paints WT-thick walls and 2×WT door blocks; rooms.json bumped (+2 in each dim) and cps shifted accordingly |
| 2-thick wall ring — procedural renderer rework (Phase 2) | QW | ✅ DONE — `_buildWallOrientation` produces rich tags (`{kind, depth}` for straight walls, `{kind:'corner', side, role}` for corner blocks); `_drawWallCellByTag` dispatcher: outer-ring straight cells get capstone+brick face, inner-ring cells get bricks+baseboard, outer corners use existing `_drawWallCorner` with new `drawBaseboard=false`, h/v-arm sub-cells render as straight outer walls, inner-corner sub-cell uses new `_drawInnerCornerCell` (flat WALL_BASE + baseboard L wrapping room interior). Doors simplified to flat FLOOR_BASE per cell (legacy arch helpers preserved but unused — multi-cell-aware arch is a future polish item). |

---

## 29. Phase-by-phase summary

| Phase | Description | Status |
|---|---|---|
| 1 — Foundation | Phaser setup, scenes, EventBus, SaveSystem, GameState, balance.js | ✅ DONE |
| 2 — Dungeon Rendering | DungeonGrid, Game scene, NightPhase build mode, MiniMap | ✅ DONE |
| 3 — Build Phase | DayPhase stub, EssenceSystem, more rooms, removal/undo | ✅ DONE |
| 4 — Adventurer AI | Adventurer entity, Pathfinder, AISystem, day cycle | ✅ DONE |
| 5 — Personality System | All 18 personalities, 6 classes, 13 combos, thought bubbles, party formation | ✅ DONE |
| 5b — Class pool expansion (2026-04-29) | beast_tamer personality promoted to "Beast Master" class; +3 new classes (Barbarian, Monk, Bard). Class data landed; behavior wiring (rage scaling, fearless, dodge, armor_pierce, party aura, single-companion taming) ⏳ PENDING. | 🟡 PARTIAL |
| 6 — Combat (kernel) | Minion entity + 5 types, placement UI, CombatSystem, MinionAISystem (guard/patrol + same-room engage), adventurer FLEE goal + flee threshold, barracks-distance validation, mid-dungeon death, minion respawn at night | ✅ DONE |
| 6b — Combat enrichment (traps + UI) | Trap entity + system + renderer, 5 traps (spike/arrow/pitfall/patience/speed) + placement UI, trap upkeep, combat log overlay, last words, chat bubbles, wider thought bubble states | ✅ DONE |
| 6c — Combat enrichment (abilities + behavior) | Mage mana drain, cleric heal_ally + smite_undead, HEAL goal (potion sip), martyr taunt, coward flee-on-enemy, paranoid slow-in-unfamiliar, mercy trap | ✅ DONE |
| 6d — Faction system + remaining mechanics | Minion faction field, necromancer raise_corpse, beast_tamer tame, traumatized panic-flee on PARTY_WIPED, curse brand trap, echo mine, mana regen, the_fan spare_idol | ✅ DONE |
| 6e — Backfill (orphaned phase-6 items) | Archetype-gated minion modifiers, ranger class + arrow resource, resource-depletion flee, hall_of_echoes sound-alert, healing_fountain heal-on-stand, barracks sneak-through, utility minions (Sapper/Herald/Engineer/Mourner), SLEEP goal | ✅ DONE |
| 7 — XP / Leveling / Evolution / Loot | LootItem entity + 16 lootDefs, LootSystem (drop/generate/equip/provenance), LootRenderer, MinionInspector UI, EvolutionSystem (XP/level/kill-driven evolution), minion naming generator, bounty flag + visual marker, level badges | ✅ DONE |
| 7b — XP/loot polish | Dungeon level progression, adventurer stat scaling, vendetta system, mini-bosses, SEEK_LOOT goal, vulture loot-stealing, underdog 2× XP + adventurer leveling, wanted poster UI, vengeful_wraith evolution + respawn-count, equipped-item display by name | ✅ DONE (rename UI + scavenger/cleaner deferred to Phase 10 polish) |
| 8 — Knowledge System | KnowledgeSystem (room/trap/minion observation), flee-share with personality accuracy, inheritance on spawn, returning leader brings party w/ full knowledge, pathfinder weighting, knowledge overlay toggle UI, memory_trap firing | ✅ DONE |
| 8b — Knowledge polish | Replay Ghosts, Mirror Maze room, vandal disarm action, between-run shopping (gear buy), Eternal Night fog-of-war foundation | ✅ DONE |
| 9 — Dungeon Mechanics (kernel) | DungeonMechanicSystem, EndOfDay scene, newspaper, **64 active pact definitions** across 5 rarity tiers (common/uncommon/rare/epic/legendary). Tier-first weighted draw via TIER_WEIGHTS in _weightedSample (45/25/15/10/5). DarkPactPopup shows rarity tags with shine animations on rare/epic/legendary. | ✅ DONE |
| 9b — Dungeon polish | Dossier UI, trap memory UI, paranoia chest indicators, auto-pause on key events, inquisitor mechanic disable, adventurers fight over loot, spectral ghost spawning | ✅ DONE (Hidden keys + locked doors, mimic AI, echo personality moved to Phase 10) |
| 10 — Boss Fight + Polish (kernel) | BossSystem with auto-resolved fight, BossFightOverlay, GameOver eulogy, Graveyard scene, ReputationSystem + tier ladder + legendary spawns, Mimic minion + Mimicry Plague reveals, Echo personality, key/locked-door foundation | ✅ DONE |
| 10b — Endgame polish | Boss evolution tree (6 nodes), Summon Adds ability, lava_floor + collapsing_pillars hazards, guild raid teams at feared+ reputation, endless mode continue, hardcore/no-traps/all-raids challenge runs, 5 new boss archetypes, Graveyard sort/filter, twitch_streamer chat_poll | ✅ DONE — Supabase leaderboard + 5 more archetypes + locked-door pathfinder block remain explicit defers |

---

## 30. Sprite-based dungeon tiling — themes + tileset editor + room template editor

> See DESIGN.md → "Sprite-based dungeon tiling" for the full spec.
> Three-phase build (A → B → C). Each row "data + UI + persistence + game wiring".

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| theme-schema | Theme JSON schema (slots: floor, 10 wall slots, 24 door slots; per-slot variant arrays) | 30A | ✅ DONE — DEVIATION (audit 2026-05-22) | `assets/themes/manifest.json` lists 40+ tile sprites (walls, pillars, doors, corners, floors 1-31, dirt floors, wall holes) loaded via `Preload`. Per-room theme indirection skipped — see `theme-per-room` row. |
| sprite-library | Sprite library (PNGs + per-sprite metadata: srcSize 32/64/128, mode scale\|span, tags) | 30A | ✅ DONE (audit 2026-05-22) | `assets/themes/sprites/*.png` + `assets/themes/manifest.json`; loaded as `themesprite-<id>` texture keys in `Preload.js`. |
| variant-pick | Random pick per cell at dungeon-build time, persisted in dungeon state (no per-frame flicker) | 30A | 🚫 SCOPE_CHANGED (audit 2026-05-22) | Replaced by explicit per-cell authoring: every room's `tileLayout` in `rooms.json` names the exact sprite per cell. No runtime random variant pick needed. |
| fs-handle | File System Access API wrapper (pick project root once, persist handle in IndexedDB, write PNGs + JSON) | 30A | ✅ DONE (audit 2026-05-22) | Used by both `TilesetEditor.js` and `RoomTileEditor.js` to write changes back to disk. |
| tileset-editor | TilesetEditor scene: sprite library panel, slot grid w/ variants, scale/span toggle, live preview, save theme | 30A | ✅ DONE (audit 2026-05-22) | `src/scenes/TilesetEditor.js`. Reached from the MainMenu TILESET EDITOR rune (cheat-name-gated). |
| theme-per-room | Each room template in rooms.json gets a `theme: "<name>"` field | 30A | 🚫 SCOPE_CHANGED (audit 2026-05-22) | Per-room `theme:` indirection abandoned in favour of direct per-cell `tileLayout` authoring on each room. 0/21 rooms currently carry a `theme` field. |
| room-tile-editor | RoomTileEditor scene: load any room template, paint cells with library sprites, save tileLayout back to rooms.json | 30B | ✅ DONE | uses existing `tileLayout` field on rooms.json |
| room-tile-rotation | Per-cell rotation in RoomTileEditor: R-key / UI button cycles brush 0/90/180/270; cell entries become `string \| { id, rot }`; renderer applies setAngle | 30B+ | ✅ DONE | scope (1) only — per-variant rotation in TilesetEditor not yet planned |
| room-tile-mirror | Per-cell horizontal + vertical mirror toggles on the brush. Cell entries gain optional `flipH`, `flipV` booleans; renderer applies image.flipX / flipY. Composes with rotation. | 30B+ | ⏳ PENDING | brush state persists across MainMenu round-trips like rotation does |
| renderer-sprite-path | DungeonRenderer adds sprite-based tile path; consumes active theme + per-cell overrides; falls back to procedural when no theme | 30C | ✅ DONE (audit 2026-05-22) | `DungeonRenderer` paints rooms from their `tileLayout` (string \| `{id, rot}` per cell) using `themesprite-<id>` textures and the `_themeTextureKey()` helper. Falls back to procedural when no layout. |
| span-rendering | Renderer honors span sprites: 64 covers 2×2 anchor, 128 covers 4×4; covered cells skipped | 30C | ⏳ PENDING | Not yet implemented — current per-cell pipeline assumes 32px tiles only. Low priority unless larger sprites land. |
| mainmenu-runes | MainMenu gets TILESET EDITOR + ROOM EDITOR runes | 30A/B | ✅ DONE (audit 2026-05-22) | Both runes present in `MainMenuOverlay`, gated behind the `'mango'` cheat name. |

---

## 31. UI / HUD overhaul (2026-05-01)

> See DESIGN.md → "UI / HUD overhaul (2026-05-01)" for the full spec.
> Net new visual system + scene rework + data plumbing for run history. Bestiary boss picker (ArchetypeSelect) is intentionally untouched. All popups land as in-scene UI groups on HudScene, not separate Phaser scenes.

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| ui-fonts | Press Start 2P + VT323 fonts loaded (Google Fonts) | 31A | ✅ DONE | Boot.js / index.html link tag |
| ui-theme | Crypt theme palette constants in UIKit (cool stone gray + blood red `#b03a48` + soul cyan `#6fd8d8`) | 31A | ✅ DONE | distinct from reverted Dark Codex parchment/gold |
| ui-pixel-panel | Pixel-bevel panel primitive (2px hl/sh + 2px black outer outline, hard pixels) | 31A | ✅ DONE | extends UIKit alongside existing glowPanel; added not replaced during overhaul |
| ui-pixel-button | Pixel button primitive incl. primary variant + hover/active states | 31A | ✅ DONE | UIKit |
| ui-build-slot | BuildSlot card component (icon + name + cost, selected ring) | 31A | ✅ DONE | UIKit |
| ui-bar | Themed pixel HP/progress bar w/ inset highlights + label overlay | 31A | ✅ DONE | UIKit |
| ui-tabs | 4-tab strip primitive (active accent fill) | 31A | ✅ DONE | UIKit |
| ui-log-row | Type-coded log row (kill/dmg/warn/know border + color) | 31A | ✅ DONE | extends CombatLog |
| title-screen | MainMenu rewrite — split-screen Variant A: cinematic dungeon left, run-readout menu panel right, 6-button menu | 31B | ✅ DONE | replaces ~911 lines |
| title-continue | CONTINUE menu action — resume active run | 31B | ✅ DONE | wires existing save load |
| title-new-evil | NEW EVIL menu action — opens existing ArchetypeSelect (bestiary) | 31B | ✅ DONE | preserves ArchetypeSelect untouched |
| title-archive | DUNGEON ARCHIVE — leaderboard stub button, no-op | 31B | ✅ DONE | future leaderboard hook |
| title-options | OPTIONS — opens new Options scene | 31B | ✅ DONE | depends on options-scene |
| title-quit | QUIT — closes the game | 31B | ✅ DONE | |
| hud-topbar | HUD top bar: boss avatar + class/day + HP bar (left), wave counter + bar (center), Gold ~~+ Dark Power~~ (right) | 31C | ✅ DONE | replaces BossHpPanel; clicking avatar opens Boss Overview. *(2026-05-05: Dark Power readout removed — Treasury panel now shows Gold only. See `BossTopBar._buildRightCol`.)* |
| hud-resources | "Gold" rename of Soul Essence (display only) ~~+ Dark Power readout~~ | 31C | 🟡 PARTIAL | Gold rename live; Dark Power readout was removed when the currency was retired 2026-05-05. |
| hud-minimap | Mini-map panel — restyled with pixel chrome (data layer unchanged) | 31C | ✅ DONE | reuses MiniMap |
| hud-build-menu | Build menu (left column, locked): tabs Rooms/Minions/Traps/Items, 2-col grid | 31C | ✅ DONE | replaces existing NightPhase palette |
| hud-build-items-stub | ITEMS tab renders empty grid + "Coming soon" caption | 31C | ✅ DONE | placeholder |
| hud-knowledge-pin | Knowledge Pin panel (right column, always visible): top 3–4 leaks + EXPOSURE bar | 31C | ✅ DONE | summary view of existing KnowledgeSystem data |
| hud-dungeon-log | Dungeon Log (renamed from Combat Log, right column, always visible) — type-coded entries | 31C | ✅ DONE | rename + type tags on existing CombatLog |
| hud-action-bar | Bottom action bar: Rotate / Move / Sell / Roster / phase indicator / Begin Day / Knowledge / Adventurer Intel / Menu | 31C | ✅ DONE | replaces design "Repair" with "Roster" |
| hud-overlay-strip | Dungeon scene overlay top-left: LEVEL / ROOMS / MINIONS counts | 31C | ✅ DONE | |
| hud-placing-caption | Bottom-left "PLACING …" caption when build slot selected | 31C | ✅ DONE | |
| build-action-rotate | Rotate action via R key while a room is held in MOVE mode (button dropped from action bar mid-phase per user). 90° CW, minions follow. | 31D | ✅ DONE | scope-changed during 31D — was an action-bar button, now an R-key affordance during MOVE pickup; existing R-on-placement behaviour covers it |
| build-action-move | Move action: button → click room → cursor-follow → click drop, minions stay, free | 31D | ✅ DONE | new gameplay action |
| build-action-sell | Sell action: button → click room → 50% gold refund (room cost + minion costs in room), removes room + minions | 31D | ✅ DONE | new gameplay action |
| build-action-sell-minion | Sell action also accepts a single minion: 50% gold refund of that minion's cost, room stays | 31D | ✅ DONE (audit 2026-05-22) | `NightPhase._sellRefund('minion', minionHit)` at line 2628 + sell tool minion-click path; 50% refund matches rooms + traps. |
| popup-adv-intel | Adventurer Intel popup — opened from HUD button any phase, shows current/incoming party | 31E | ✅ DONE | renamed from "Pre-Wave Prep" |
| popup-adv-intel-library | Library room masks `???` fields on incoming party UNLESS Library room exists in dungeon | 31E | ✅ DONE | new Library role; gates intel reveal |
| popup-adv-intel-knowmap | Adventurer Intel shows the adventurers' knowledge map (replaces design's predicted route) | 31E | ✅ DONE | reuses KnowledgeSystem |
| popup-boss-overview | Boss Overview popup — opened by clicking boss avatar in top bar | 31E | ✅ DONE | new |
| popup-boss-ability | Boss Overview displays the boss's unique ability (per `bossAbilities.json`) | 31E | ✅ DONE | UI slot only — full ability impl deferred per archetype |
| popup-boss-pacts | Active Pacts grid in Boss Overview | 31E | ✅ DONE | depends on pact-history field |
| popup-boss-census | Dungeon Census tile in Boss Overview (rooms/minions/traps/items/doors/paths) | 31E | ✅ DONE | counts derived live from gameState |
| popup-minion-roster | Minion Roster popup — opened from action-bar Roster button, info-only (no summon/heal/reassign) | 31E | ✅ DONE | new |
| popup-minion-detail | Roster detail pane — selected minion sprite + class + name + assignment + HP + kills/dmg/armor/speed + traits | 31E | ✅ DONE | |
| popup-knowledge-map | Knowledge Map popup — opened from action-bar Knowledge button, replaces full-screen KnowledgeScreen | 31E | ✅ DONE | UI rewrite, data layer reused |
| popup-knowledge-leaker | Intel Ledger shows `via {adventurerName} (esc Day N)` per fact | 31E | ✅ DONE | requires KnowledgeSystem to surface leaker per fact |
| popup-postwave | Post-Wave Summary popup: Casualties + Resources Earned + Dungeon Performance, Continue button | 31F | ✅ DONE | splits existing EndOfDay |
| popup-darkpact | Dark Pact popup — 3 cards, Reroll All (1×) + Seal the Pact, no skip | 31F | ✅ DONE | level-gated |
| popup-darkpact-gating | Dark Pact only shown when boss leveled up that day; otherwise skip to night | 31F | ✅ DONE | new gating rule |
| popup-darkpact-reroll | Reroll All button replaces all 3 offerings once, then disables | 31F | ✅ DONE | extends DungeonMechanicSystem |
| pause-menu-redesign | PauseMenu redesigned in pixel-bevel style; entry point moved to action-bar Menu (ESC still works) | 31G | ✅ DONE | restyled, same options |
| options-scene | New Options scene — audio volumes (master/music/SFX), graphics toggles, keyboard ref. Skeleton ships even with stubs. | 31G | ✅ DONE | reachable from title menu OPTIONS |
| gameover-rewrite | Game Over rewrite: header "DUNGEON FALLEN" + Final Tally + Pacts Sealed + Built · Lost panels | 31H | ✅ DONE | replaces existing GameOver.js |
| gameover-anim | Game Over animation: staggered fade-in + integer count-up per row, ~6–8s total, any key skips | 31H | ✅ DONE | sound hook reserved |
| history-pacts | `gameState.history.pacts: [{day, mechanicId, rarity}]` — appended on Dark Pact seal | 31I | ✅ DONE | new gamestate field |
| history-minion-lifetime | Per-minion `lifetime: { kills, damageDealt }` | 31I | ✅ DONE | grep first; add only if missing |
| history-adv-escapecount | Per-adventurer `escapeCount` to derive "biggest leak" | 31I | ✅ DONE | grep first; add only if missing |
| history-run-totals | Per-day `gameState.run.totals` rolling counters: kills, dmgDealt, dmgTaken, advsKilled, advsEscaped, gold, souls, roomsBuilt, roomsDestroyed, minionsSummoned, minionsLost, trapsPlaced, trapsDisarmed | 31I | ✅ DONE | SaveSystem serializes |
| save-compat | SaveSystem rehydrates the new history fields cleanly on old saves (defaults to empty arrays / 0s) | 31I | ✅ DONE | back-compat |
| design-handoff-bundle | Local copy of design bundle preserved in `qf_design_temp/` for reference during build | — | ✅ DONE | unzipped from Claude Design handoff |

---

## 32. Adventurer emote bubbles (2026-05-01)

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| emote-assets | Copy 60 emote PNGs from staging into `assets/sprites/emotes/`; each is 96×32 with three 32×32 frames | 32 | ✅ DONE | filenames map to triggers |
| emote-preload | Register emote spritesheets in Preload.js; build a manifest mapping triggerId → variant texture keys | 32 | ✅ DONE | catalog lives in EmoteSystem.js, Preload imports `allEmoteVariants` |
| emote-system | `EmoteSystem.js` — subscribes to trigger events, rolls 20% chance, attaches sprite to adv container at y≈-52, plays once, destroys | 32 | ✅ DONE | per-adv cooldown 1500ms; priority replaces lower/equal |
| emote-trigger-random | Ambient `random_exploring` roll while walking (~6–10s window, 20% chance) | 32 | ✅ DONE | low priority — never overrides state emote |
| emote-trigger-rooms | Hook `ROOM_OBSERVED` — `firstVisit:true` → discovered/unknown pool; `firstVisit:false` → known-room pool | 32 | ✅ DONE | KnowledgeSystem already emits both flavors |
| emote-trigger-combat | Hook fighting state transition → `fighting + found minion + class-specific` pool, filtered by classId | 32 | ✅ DONE | tracked via aiState change in update tick |
| emote-trigger-flee | Hook fleeing state / `ADVENTURER_FLED` → fleeing pool | 32 | ✅ DONE | aiState/`goal.type === FLEE` edge-trigger |
| emote-trigger-low-hp | Hook HP fraction crossing below 30% → low_health pool | 32 | ✅ DONE | edge-trigger only |
| emote-trigger-loot | Hook `MIMIC_REVEAL_TRIGGERED` and `TREASURY_CHEST_GRAB_STARTED` → found_loot pool | 32 | ✅ DONE | gear pickup deprecated |
| emote-trigger-trap | Hook `TRAP_TRIGGERED` → found_something pool | 32 | ✅ DONE | |
| emote-trigger-boss | Hook `BOSS_FIGHT_INCOMING` → entered_boss_room | 32 | ✅ DONE | |
| emote-trigger-tame | Hook `MINION_TAMED` → tame_success | 32 | ✅ DONE | |
| emote-trigger-resurrect | Hook `ADVENTURER_RESURRECTED` → resurrected | 32 | ✅ DONE | |
| emote-trigger-doors | `breaking down door*` and `finding a locked door` triggers | — | ⏳ PENDING | DEFERRED — no locked-door event in code yet |

---

## 33. Dungeon events (2026-05-05)

> See DESIGN.md → "Dungeon events" for full per-event spec. All events are pre-announced during the night phase before the day they fire (Dark Deal + Loot Goblin Heist resolve during the night phase itself).

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| event-system | `EventSystem` — schedule + roll engine, event registry, night-phase notification banner, JSON-driven event definitions in `src/data/events.json` | EV | ✅ DONE | **Cadence (revised 2026-05-21): first event day 3, then every 3 days (was 6–8). Same event cannot fire back-to-back. Speed Runner / Rival Dungeon gated to boss level ≥ 3; every event also carries a per-event `minBossLevel`.** (The Tournament was removed 2026-05-21 — see `event-tournament`.) |
| event-guild-raid | Guild Raid! — next day spawns 2× adventurers as steady pressure (longer wave, not one-shot surge) | EV | ✅ DONE | |
| event-legendary-speedrunner | Legendary Speed Runner — single 2× stats / 2× speed adventurer, ignores non-essential rooms + minions, beelines to boss; killing them grants massive boss XP | EV | ✅ DONE | Boss level ≥ 3. Name intentionally fourth-wall-breaking. Pathfind: shortest path to boss room, only engages chokepoint-required minions. |
| event-pestilence | Dungeon Pestilence — minions start day at 50% HP; melee with adventurer applies "Blighted" DoT until they die or leave; affects existing + newly-placed minions; Blight does NOT persist after adv leaves | EV | ✅ DONE | Adds new status: `blighted` on adventurer entity. |
| event-cartographers | Cartographer's Convention — 3 scholar adventurers visit every non-boss room then leave; each room they map raises that room's "infamy" so future advs spawn with that knowledge | EV | ✅ DONE | New per-room `infamy` field; KnowledgeSystem reads on adv spawn. AI: visit-all goal type. |
| event-cartographer-sprites | 3 LPC scholar adventurers (with glasses) for the Cartographer event | EV | ✅ DONE | Content task — uses existing LPC pipeline. |
| event-blood-moon | Blood Moon Eclipse — minions deal 2× damage AND take 2× damage; no gold from killed adventurers | EV | ✅ DONE | |
| event-negotiation | Negotiation Day — non-combat. Modal at start of day: pay 25% treasury (free day) OR refuse (next day +50% wave size) | EV | ✅ DONE | New popup component. Decision persists into following days' adv counts. |
| event-tournament | ~~The Tournament — 3 named rivals enter, hostile to each other AND the dungeon; sabotage each other's loot, body-block, attack each other when in same room~~ | EV | ❌ REMOVED | **Removed 2026-05-21 at player request** — the rival-vs-rival bloodsport never read cleanly in play. Event entry deleted from `events.json`, so it can no longer be scheduled or debug-forced. Inert spawn/AI code (`_spawnTournamentRivals`, `_tournamentRival` AI blocks, `tournament_rival_*` classes) left in place but unreachable. |
| event-rival-dungeon | Rival Dungeon — instead of adventurers, a group of random monsters enters; final entrant is a random boss from the boss pool that fights the player's boss; killing it grants big XP + gold | EV | ✅ DONE | Boss level ≥ 3. Reuses boss roster as enemy spawns. **Rival boss uses simple AI — basic chase + attack, NOT full BossArchetypeSystem behaviors.** |
| event-twitch-con | Twitch Con — entire next wave is `twitch_streamer` class adventurers; killing them does NOT trigger the usual escalation/+adventurers-next-day penalty | EV | ✅ DONE | Class already exists in `adventurerClasses.json`. |
| event-dark-deal | Dark Deal — demon appears in boss room during NIGHT phase, offers a free dark pact; if accepted, boss max HP halved for next day; if ignored, demon leaves with no penalty. Click demon to engage. | EV | ✅ DONE | Asset: `assets/!To do/Demon.png` — 4-row sheet (rows 1-2 appearing, rows 3-4 leaving). Spawns sprite into boss room scene + click handler + pact picker reuse. |
| event-cosplay-contest | Cosplay Contest — adventurers in monster outfits; do not attack minions unless attacked first; 75% chance to walk past minions ignoring them | EV | ✅ DONE | Adv AI variant: passive-pass behavior, retaliation flag. |
| event-cosplay-sprites | New LPC adventurer variants wearing animal/monster parts (zombie, skeleton, tails, wings, fantasy, beastman, farm animal, furry, undead, reptilian) — colored correctly per part theme | EV | ✅ DONE | Content task. Reptilian = green, etc. |
| event-loot-goblin-heist | Loot Goblin Heist — goblins spawn in BOSS ROOM (reverse direction) and run for the dungeon exit; each kill drops large gold; goblins never stop to fight, only flee. **Each goblin that escapes steals 10% of current gold total** (per goblin, applied at exit) | EV | ✅ DONE | New spawn-point: boss room. New AI: pure-flee-to-exit goal (no engagement). Gold-drain stacks per escapee. |

### 33b. Event pool expansion — 2026-05-21 (15 new events)

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| event-tax-season | Tax Season — 20% treasury levied at day start; every kill that day pays 2× gold | EV | ✅ DONE | minBossLevel 1. AISystem goldMul ×2. |
| event-patrons-blessing | Patron's Blessing — boss XP from every kill that day doubled | EV | ✅ DONE | minBossLevel 1. AISystem `_awardBossXp` called twice. |
| event-gamblers-coin | The Gambler's Coin — night modal; WAGER 50/50 double-or-halve the treasury | EV | ✅ DONE | minBossLevel 1. SHOW_CONFIRM modal. |
| event-memory-plague | Memory Plague — shared knowledge pool wiped at announce; next wave blind | EV | ✅ DONE | minBossLevel 1. `KNOWLEDGE_WIPE_ALL` → KnowledgeSystem clears survivors + rebuilds pool. |
| event-dense-fog | Dense Fog — intel gathered that day registers only as RUMOR tier; exposure barely rises | EV | ✅ DONE | minBossLevel 1. KnowledgeSystem `_fogActive` gates confirmed/stale. Grey screen tint. |
| event-miasma | Creeping Miasma — chip DoT every 2s to all; invaders lethal, minions floored at 1 HP, boss floored at 25% | EV | ✅ DONE | minBossLevel 1. EventSystem timer. Green tint. |
| event-tremors | Tremors — every 8s a quake hits one random room (shake + damage to occupants) | EV | ✅ DONE | minBossLevel 1. EventSystem timer; minions non-lethal. |
| event-arcane-storm | Arcane Storm — adventurer class-ability cooldowns ×0.4 | EV | ✅ DONE | minBossLevel 1. `AbilitySystem.setCooldownScale`. Boss-archetype abilities (separate timer system) unaffected. Purple tint. |
| event-bounty-hunters | Bounty Hunters — 5-strong pack replaces wave, all hunting the highest-level minion | EV | ✅ DONE | minBossLevel 2. Reuses SEEK_VENDETTA goal + bounty_hunter sprite. |
| event-zombie-horde | Zombie Horde — 14 slow/weak/never-flee undead replace the wave; maul everything to the boss | EV | ✅ DONE | minBossLevel 2. Rendered with zombie minion sheets (tiers 1–3, varied). |
| event-infamy-spike | Infamy Spike — wave +50% size, every adv buffed to hero grade + `hero` tag | EV | ✅ DONE | minBossLevel 2. Gold ring + ★ HERO badge via AdventurerRenderer. |
| event-black-market | Black Market — night modal; pay 50g for a free random unlocked minion | EV | ✅ DONE | minBossLevel 1. Minion spawned as `garrison` class (no Barracks-cap interaction). |
| event-mercenary-contract | Mercenary Contract — night modal; pay 120g to hire a Tier-3 minion (doubled stats) for 3 days; permadeath if killed | EV | ✅ DONE | minBossLevel 2. `_mercenary` tag; stats + `_base*` doubled; contract expiry culled by EventSystem; dead mercenaries filtered out in `respawnAll` (no revive). |
| event-cursed-relic | Cursed Relic — night modal CLAIM/BANISH; CLAIM drops a cursed Tier-5 chest (gold/day) that DOUBLES every wave (with a daily toast) until sold | EV | ✅ DONE | minBossLevel 1. Reuses treasure-chest entity; `_cursed` flag → purple-black glow in TreasureChestRenderer + wave-double in DayPhase. |
| event-saboteur | The Saboteur — lone invulnerable all-black ninja-rogue replaces the wave; tours every trap disabling it (re-arms overnight), then flees | EV | ✅ DONE | minBossLevel 2. New `DISARM_TRAP` goal; minions ignore `_saboteur`; traps can't damage `_invulnerable`. Dark-tinted rogue sprite. |

---

## 34. UI / HUD overhaul v2 — claude-design DOM port (2026-05-18)

> Second pass on the HUD chrome. Section 31's Phaser-rendered Crypt-themed implementation ships and stays default until each panel here lands. Source design files: `D:\Documents\Game Jam Code\Quest-Failed assets\claude design ui\design_handoff_quest_failed_ui\` (high-fidelity HTML/React/CSS mockups; the README in that folder is the source of truth for tokens, animations, and panel layout). Render strategy: DOM overlay on top of the Phaser canvas, vanilla JS modules in `src/hud/`, no build step. A 1920×1080 logical stage is transform-scaled to fit the viewport so design coordinates stay literal. Gated behind a feature flag (`?newhud=1` URL param or `localStorage.newhud='1'`) until every Phase 34B panel has shipped — then the flag flips default-on and section 31 chrome moves to removed-not-deleted.

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| hud2-scaffolding | DOM HUD layer: `src/hud/styles.css` (design tokens + animations), `src/hud/dom.js` (h/mount/tween helpers), `src/hud/HudRoot.js` (1920×1080 stage scaler + feature flag), `#hud-root` + `#hud-stage` in `index.html`, JetBrains Mono font added | 34A | ✅ DONE | Stage scaler matches the source `fit()` exactly: `transform: translate(-50%, -50%) scale(min(vw/1920, vh/1080))`. Singleton `window.__game._hudRoot` so scene-restarts don't re-mount. |
| hud2-topbar | TopBar — boss portrait button (emits `OPEN_BOSS_OVERVIEW`), name, LV chip, HP chip-bar with ghost fill + heartbeat at <30%, XP thin bar, 4 buff slots, hearts row, phase eyebrow + DAY {n} stamp animation on phase change, treasury label + animated coin + gold ticker with pulse on gain. Wired to `gameState.{meta.phase, meta.dayNumber, boss.{hp,maxHp,xp,xpToNext,level,deathsRemaining}, player.gold}` and EventBus `BOSS_LEVELED_UP` / `DAY_PHASE_BEGAN` / `NIGHT_PHASE_BEGAN`. Boss portrait reads the existing `assets/ui/bestiary/portraits/{id}_p.png` via an `Image()` probe so a 404 falls back to a CSS radial-gradient placeholder. Archetype-id lookup strips a leading `the_` prefix so the JSON-id (`lich`) and a legacy gameState id (`the_lich`) both resolve | 34B | ✅ DONE | Phaser `BossTopBar` instance is hidden via `_objects[*].setVisible(false)` while DOM HUD is mounted. |
| hud2-bottombar | BottomBar — build modes (PLACE / MOVE / SELL), phase label + status, BEGIN DAY primary button (night) or 1×/2×/4×/8× speed control (day), ROSTER / KNOWLEDGE / ADV INTEL / MENU. Drop-in event contract with the Phaser `ActionBar`: `TOOL_MOVE` / `TOOL_SELL` (PLACE re-emits the armed tool to disarm it), `PHASE_TOGGLE_REQUEST`, `TIME_SCALE_SET { scale: 1\|2\|4\|8 }`, `OPEN_*`. Subscribes to `TOOL_MODE_CHANGED` to keep armed-tool highlight in sync, and to `DAY_PHASE_ENDED` / `NIGHT_PHASE_BEGAN` to reset speed to 1× | 34B | ✅ DONE | Phaser `ActionBar` is hidden + `disableInteractive`'d on its hit zones while DOM HUD is mounted. End-to-end verified: clicks emit the expected EventBus payloads (TOOL_MOVE / TOOL_SELL / PHASE_TOGGLE_REQUEST / TIME_SCALE_SET{2}, TIME_SCALE_SET{8} / OPEN_MINION_ROSTER / OPEN_KNOWLEDGE_MAP / OPEN_ADV_INTEL / OPEN_PAUSE_MENU). |
| hud2-leftpanels | LeftPanels — MiniKnowledgeMap (1:1 dungeon blueprint, room blocks tinted by definitionId, scan line, boss + minion dots) + ConstructionPanel (4 tabs Rooms / Minions / Traps / Items, 2-col card grid, footer detail with stats + description + PLACE button). Replaces `MiniMapPanel` + `BuildMenu` | 34B | ✅ DONE | Reads `rooms` / `minionTypes` / `trapTypes` / `items` JSON from Phaser cache; filters by `gameState.unlocks` and `gameState.boss.level` exactly like `BuildMenu`. Emits `BUILD_SELECT { def, kind }` / `BUILD_DESELECT` (drop-in for the Phaser BuildMenu). Subscribes to `BUILD_DESELECT`, `BOSS_LEVELED_UP`, `ROOM_PLACED`, `ROOM_REMOVED`, `GRID_EXPANDED`. Intel-state coloring on the mini-map deferred — current tints match the existing MiniMapPanel contract (definitionId-keyed). Re-clicking a selected card deselects. Phaser MiniMapPanel + BuildMenu are hidden + their hit zones disabled while DOM HUD is on. |
| hud2-rightpanels | RightPanels — IncomingWave (night-only, now renders a wave-size + threat forecast computed from `Balance.ADVENTURERS_PER_DAY_BASE`, treasury count, active mechanics; previously a stub), AdventurerIntel (leak list from `gameState.knowledge.sharedPool` + exposure % heuristic), DungeonLog (event feed with 7 color-coded kind glyphs: info / kill / gold / leak / pact / level / spawn; ring-buffer at 200 rows; auto-scrolls). Replaces `KnowledgePin` + `DungeonLog` | 34B | ✅ DONE | Subscribes to `ADVENTURER_ENTERED_DUNGEON`, `ADVENTURER_DIED`, `ADVENTURER_FLED`, `MINION_DIED`, `TRAP_TRIGGERED`, `PACT_SEALED`, `BOSS_LEVELED_UP`, phase + intel-leak events. Phaser KnowledgePin + DungeonLog hidden under flag. |
| hud2-toasts | ToastQueue — top-right stack (max 4), 360ms slide-in / 280ms slide-out / 3.4s persist using the existing `.toast` keyframes in `styles.css`. Toast kinds wired to events: `BOSS_LEVELED_UP` → level, `PACT_SEALED` → pact, `ADVENTURER_DIED` → kill, `ROOM_DAMAGED` / `ROOM_DESTROYED` → damage, `INTEL_LEAKED` → leak. Gold-looted + veteran-approaching toasts have no canonical event in code yet — added in the kind palette but inert until events ship | 34B | ✅ DONE | Pointer-events: none on the queue so toasts never eat clicks on the HUD beneath. |
| hud2-sprites | Port `sprites.jsx` — 16×16 ASCII-grid CSS pixel sprites for minion / adventurer kinds (gnoll, hyena, knight, monk, cleric, imp, lich, slime, spore, bone) + 14×14 room-icon glyphs (entry / corridor / barracks / guard / crypt / library / trap / item). `spriteKindForDefId()` strips trailing digits from minion ids (orc1 → orc → gnoll alias) and falls back through an alias table for unmapped families | 34B | ✅ DONE | `src/hud/sprites.js`. Wired into LeftPanels construction cards: minions render real `pixelSprite`, rooms render `roomIcon` keyed by id substring (entry/corridor/barracks/guard/crypt/library), traps + items use their dedicated glyph icons. Boss portraits in TopBar still use the bestiary PNGs (richer art for the boss case). |
| hud2-flag-flip | Flip `?newhud=1` from opt-in to default-on once 34B is fully ✅. Mark section 31 as superseded-but-shipped (removal-not-deletion) | 34B | ✅ DONE | `isNewHudEnabled()` in `src/hud/HudRoot.js` now defaults to true; `?newhud=0` or `localStorage.newhud='0'` reverts to the old chrome. Section 31 superseding pass deferred until 34C ships every overlay. |
| hud2-buff-slots-data | Populate the 4 buff slots in the TopBar with the player's live pact list. Read `gameState.activeMechanics` (array of mechanic IDs); resolve each via `dungeonMechanics.json` for name + rarity + description + tradeoff; show the 4 most recent. Empty slots stay placeholders | 34B | ✅ DONE | `TopBar._renderBuffSlots()`. Per-rarity color + glyph: common ◐ mute, uncommon ✦ poison, rare ◆ gold, epic ◈ info, legendary ▣ blood (with blinking corner pip). Subscribes to `PACT_SEALED` + `PACT_DEACTIVATED` for re-render. |
| hud2-buff-tooltip | Buff-slot hover tooltip — pact name + rarity tag + flavor + boon ▲ / bane ▼ rows. Positioned below the slot (anchor: 'below') | 34B | ✅ DONE | Routes through existing `PactDetailPopup` via a new `anchor: 'below'` option (vs the default `'left'` BossOverview uses). Mouse-enter on slot fires `SHOW_PACT_DETAIL`, mouse-leave fires `HIDE_PACT_DETAIL`. |
| hud2-incoming-wave-data | Real "incoming wave" preview. Renders a wave-size + threat forecast using the same formula as `DayPhase._spawnDailyAdventurers`: `Balance.ADVENTURERS_PER_DAY_BASE + floor((nextDay - 1) / 2)` + treasury count + `goldRush` mechanic +1 + guildRaid ×2. Threat % is a heuristic (day factor + boss-level factor + event flag boost). Notes list called-out modifiers ("Gold Rush — +1", "GUILD RAID — doubled wave", etc) | 34B | ✅ DONE | `RightPanels._forecastWave()`. Doesn't compute classes (those are picked at spawn time in DayPhase). Doesn't write to gameState — pure read-side forecast, so it stays in sync with the real spawn formula automatically. |
| hud2-toast-events | Wire the 2 previously-inert ToastQueue kinds: **VETERAN APPROACHING** fires on `ADVENTURER_ENTERED_DUNGEON` when `adventurer.escapeCount > 0`. **GOLD LOOTED** fires on `LOOT_GOBLIN_ESCAPED` (existing EventSystem event from the Loot Goblin Heist) with the stolen-gold amount | 34B | ✅ DONE | ToastQueue subscriptions added. The canonical "they stole your treasure" event was already in `EventSystem` — no new gameplay-side wiring needed. |
| hud2-room-tooltip | Dungeon-view room hover → DOM 2-tab tooltip (STATS / HISTORY). HudRoot adds a `pointermove` listener on the Phaser canvas, converts client coords to tile coords via `Game` scene's main camera + the 32px tile size, hit-tests against `gameState.dungeon.rooms[]`, and emits `SHOW_ROOM_TOOLTIP { room, x, y }`. RoomTooltip listens + renders. STATS tab shows size + cost + unlockLevel + tags + description; HISTORY tab shows built-on-day + status + a placeholder for per-room event log | 34B | ✅ DONE | `src/hud/RoomTooltip.js` + the canvas-bridge in `HudRoot._tryRoomHover()`. Boss chamber excluded from hit-test (too dominant). Throttled to ~30Hz. Per-room event-log on the HISTORY tab is a follow-up (gameState doesn't store per-room events yet). |
| hud2-hotkey-hints | Bottom-of-screen hotkey hints row, pinned just above the BottomBar. Shows ESC PAUSE · SPACE BEGIN DAY · M MOVE · X SELL · 1-4 SPEED · R ROSTER · K KNOWLEDGE · I INTEL. Visibility gated by `qf.gameplay.hotkeys` localStorage flag (the GAMEPLAY > SHOW HOTKEY HINTS toggle in Settings) | 34B | ✅ DONE | `src/hud/HotkeyHints.js`. Default-on. Re-checks the flag every 2s as a fallback in case the settings-changed signal isn't broadcast; also listens to the `storage` window event for cross-tab consistency. |
| hud2-overlay-shell | Generic `Overlay` shell — backdrop dim + centered `.modal` box with double-accent-band border, header (title + optional badge + close button), body slot, ESC + backdrop-click + X-button close paths. Mounts into `#hud-stage` so it scales with the rest of the new HUD. Reused by every overlay below | 34C.1 | ✅ DONE | `src/hud/Overlay.js`. |
| hud2-pause-overlay | PauseOverlay — DOM port of the design's pause menu (pulsing skull, run snapshot DAY/GOLD/KILLS, 7 menu items: RESUME / BOSS STATS / MINION ROSTER / KNOWLEDGE MAP / DUNGEON LOG / OPTIONS / ABANDON RUN). Wired to `PauseManager` via a new `PAUSE_STATE_CHANGED { isPaused }` event so ESC presses in any gameplay scene mount the DOM overlay too. PauseManager skips booting its Phaser scene under the newhud flag | 34C.1 | ✅ DONE | `src/hud/PauseOverlay.js`. BOSS STATS / ROSTER / KNOWLEDGE route to existing Phaser popups via `OPEN_*` events until 34C.2 ships. DUNGEON LOG closes (the right-column DungeonLog is always visible in the new HUD). ABANDON wires straight to `PauseManager.saveAndExitToMenu` — confirm-dialog gating comes with 34C.2's ConfirmPopup port. |
| hud2-settings-overlay | SettingsOverlay — 4-tab settings panel (AUDIO sliders / VIDEO toggles + particles + palette / CONTROLS keybind reference / GAMEPLAY toggles). Footer: RESET DEFAULTS · CANCEL · APPLY. Settings persist to `localStorage` under `qf.{audio,video,gameplay}.*` keys; CANCEL reverts in-flight changes. Palette radio live-previews via the existing `.palette-necro` / `.palette-hellfire` classes on `#hud-root` | 34C.1 | ✅ DONE | `src/hud/SettingsOverlay.js`. Live preview only landed for palette so far — wiring the audio sliders into `SfxVolume` / `MusicVolume` and the video toggles into renderer flags is a follow-up. |
| hud2-overlay-bossoverview | BossOverview popup (overlays.jsx → BossOverlay) — cinematic portrait card (counter-rotating rune rings, corner registration marks, big sprite, gold LV chip, hearts) + tabbed dossier (OVERVIEW / ABILITIES / PACTS). OVERVIEW shows abilities preview + active pacts + dungeon census. ABILITIES shows the full ability tree with unlock/lock states. PACTS shows expanded rows with rarity chip + flavor + boon ▲ / bane ▼; hovering opens `PactDetailPopup` | 34C.2 | ✅ DONE | `src/hud/BossOverviewOverlay.js`. Opens on `OPEN_BOSS_OVERVIEW` (HudScene's legacy listener is now gated when newhud is on). Reads `gameState.player.bossArchetypeId` → bestiary portrait, `gameState.boss.{hp,maxHp,xp,xpToNext,level,deathsRemaining,unlockedAbilities}`, `gameState.run.totals` for tally, `gameState.activeMechanics` + `dungeonMechanics.json` cache for pacts, `bossAbilities.json` cache for abilities. Tab switch verified. Pact hover emits `SHOW_PACT_DETAIL` payload. |
| hud2-overlay-roster | Roster popup (overlays.jsx → RosterOverlay) — summary strip (GARRISON / KILLS / AVG LV / WOUNDED). Left: filter row (ALL / READY / WOUNDED / IDLE) + per-minion list (sprite + tier badge + status dot + HP bar + LV + kills). Right: big portrait card with stat grid (DMG/ARMOR/SPD/KILLS) + TRAITS chips + description tile + RECENT log + actions (REASSIGN / RENAME / SACRIFICE) | 34C.2 | ✅ DONE | `src/hud/RosterOverlay.js`. Opens on `OPEN_MINION_ROSTER`. Reads `gameState.minions` (filters out dead). Sprite uses bestiary portrait keyed by archetype family (definitionId minus trailing digit). SACRIFICE button emits `SHOW_CONFIRM` → `MINION_SACRIFICE_REQUEST` event (gameplay side TBD). REASSIGN / RENAME buttons UI-only for now. |
| hud2-overlay-knowledgemap | KnowledgeMap popup (overlays.jsx → KnowledgeMapOverlay) — summary strip (EXPOSURE % + sparkline, ROOMS LEAKED, INTEL ENTRIES, LAST LEAK). Left pane: dungeon blueprint with rooms tinted by intel state (FULL red / PARTIAL orange / RUMOR cyan / UNKNOWN dashed grey), animated scan line, zoom (− / ◇ / +) + click-drag pan when zoomed > 100%, click-to-filter ledger. Right pane: per-room intel cards with leak attribution (source name + day stamp), mitigation hint, SCRUB INTEL button with gold cost. Below the map: 4-state legend bar | 34C.2 | ✅ DONE | `src/hud/KnowledgeMapOverlay.js`. Reads `gameState.dungeon.rooms` for placements + `gameState.knowledge.sharedPool` for per-room intel level. Click-to-filter verified, zoom verified (125% on +). Source attribution falls back to "shared pool" entries until per-survivor knowledge tracking lands. Sparkline / "fresh today" / per-leak day are best-effort placeholders — tracked under `hud2-knowledge-history-data` follow-up. SCRUB INTEL emits `KNOWLEDGE_SCRUB_REQUEST { roomId, cost }` (gameplay handler TBD). |
| hud2-overlay-advintel | AdventurerIntel popup (overlays.jsx → AdventurerIntelOverlay) — top strip (NEXT WAVE day / PARTY SIZE / VETERANS / OVERALL THREAT 10-cell meter). Left: party list with per-adv card (sprite + name + class·LV + veteran badge + threat chip + redacted flag). Right: selected-adv detail (portrait + 3-stat row HP/ATK/SPD + RESISTS / WEAK TO chips + notes) and intel ledger ("WHAT THEY KNOW" or "WHAT THEY'LL TELL THE OTHERS" for veterans) + counter-advice callout | 34C.2 | ✅ DONE | `src/hud/AdvIntelOverlay.js`. Pulls from `gameState.adventurers.active` (day phase) or `.known` (night phase); falls back to a "INTEL UNKNOWN — build a LIBRARY" empty state when neither is populated (current night-phase default). RESISTS / WEAK TO from `adventurerClasses.json` class def. Threat score is a heuristic (LV * 8 + atk * 2 + min(40, hp/2)). Veteran detection from `adv.isVeteran` or `adventurers.known` escapeCount > 0. **Open follow-up**: a real party-preview slot (hud2-incoming-wave-data) would let the night-phase view show the next wave instead of "INTEL UNKNOWN". |
| hud2-overlay-confirmpopup | Generic ConfirmPopup — modal with message + Confirm / Cancel buttons. Used to gate destructive actions (ABANDON RUN, sacrifice minion, etc). Receives `SHOW_CONFIRM { title?, message, confirmLabel?, cancelLabel?, onConfirm?, onCancel? }` — same channel the existing Phaser ConfirmPopup uses | 34C.2 | ✅ DONE | `src/hud/ConfirmPopup.js`. Wired live into PauseOverlay's ABANDON RUN (now confirms before exit). HudScene's legacy `SHOW_CONFIRM` Phaser listener gated when newhud is on. End-to-end verified. |
| hud2-overlay-pactdetail | PactDetailPopup — small floating tooltip showing full pact info (name + rarity + flavor + boon ▲ + bane ▼). Opens from BossOverview's pact rows on hover via `SHOW_PACT_DETAIL { pact, x, y }`. Hidden on `HIDE_PACT_DETAIL` | 34C.2 | ✅ DONE | `src/hud/PactDetailPopup.js`. Mounts in `position: fixed` viewport space so it survives overlay scrolling. HudScene's legacy `SHOW_PACT_DETAIL` Phaser listener gated when newhud is on. |
| hud2-overlay-welcomeintro | WelcomeIntro popup — fires once per run after the player picks a boss (gated by `meta.introSeen`). Sets the tutorial-enabled flag. Sticky (no Esc / backdrop / X) | 34C.2 | ✅ DONE | `src/hud/WelcomeIntroOverlay.js`. HudRoot calls `maybeOpen()` on construction; opens after a 180ms settle delay if `meta.introSeen` is false. Tagline + 3-paragraph loop explanation (NIGHT — BUILD / DAY — DEFEND / GROW — REPEAT) + tutorial-enabled checkbox + CONTINUE button. ESC handler defanged, X button hidden. Continue flips `meta.introSeen=true` and `meta.tutorialEnabled` based on checkbox; emits `INTRO_DISMISSED`. HudScene's legacy `welcomeintro.open` auto-fire gated under `!useNewHud`. |
| hud2-overlay-tutorial | Tutorial popup — fires from `SHOW_TUTORIAL { title, body, onClose }` (TutorialSystem). Sticky | 34C.2 | ✅ DONE | `src/hud/TutorialOverlay.js`. Subscribes directly to `SHOW_TUTORIAL`. Sticky (no Esc / backdrop / X). `onClose` fires after GOT IT click so TutorialSystem can advance its queue. Legacy HudScene listener gated under `!useNewHud`. |
| hud2-overlay-bosslevelup | BossLevelUp popup — interim screen between PostWaveSummary and night phase when the boss leveled up that day. Hero burst (boss sprite + counter-rotating rune rings + 16 radial spokes + LV X ▶ LV Y slam-in). POWER GAINS stat tiles. NEWLY UNLOCKED 4 category cards (Rooms/Minions/Traps/Items) | 34C.2 | ✅ DONE | `src/hud/BossLevelUpOverlay.js`. Subscribes to `SHOW_BOSS_LEVEL_UP { fromLevel, toLevel }`. Stat tiles compute live deltas using `MINION_HP_PER_LV` / `MINION_ATK_PER_LV` Balance constants (mirrored locally). Unlocks read each cache JSON filtered by `unlockLevel === toLevel`. CONTINUE emits `BOSS_LEVEL_UP_DISMISSED` so EndOfDay can chain multi-level-up days. Legacy HudScene listener gated under `!useNewHud`. |
| hud2-overlay-longgame | LongGame popup — fires from `LONG_GAME_TRIGGERED` when the Long Game pact procs (every 3 days). Granted free pact + sacrificed minion + permanent slot loss + CONTINUE | 34C.2 | ✅ DONE | `src/hud/LongGameOverlay.js`. Same payload shape as the Phaser popup (`day` / `grantedName` / `lostName`). Legacy HudScene listener gated under `!useNewHud`. |
| hud2-moment-phasetransition | PhaseTransition cinematic — 2.8s screen on `DAY_PHASE_BEGAN` / `NIGHT_PHASE_BEGAN`. Letterbox bars, 18-streak radial speed-lines, vertical light shaft, oversized boss silhouette, eyebrow text, massive DAY {NN} stamp with corner brackets + blur-cleanup, subtitle (THE INVASION / THE BUILD), italic flavor quote, brief screen shake on title impact. Auto-dismisses; blocks input via `pointer-events: auto` on the full overlay | 34C.3 | ✅ DONE | `src/hud/PhaseTransition.js` + ~16 new keyframes in `styles.css` (pt-bg / pt-shaft / pt-streak / pt-silhouette / pt-eyebrow / pt-title / pt-bracket / pt-sub / pt-flavor / pt-letterbox-in / pt-shake). The transition is non-blocking gameplay-wise (it doesn't actually pause the simulation) — that's a deliberate choice so the new cinematic is purely visual on top of existing pacing. |
| hud2-moment-pactpicker | PactPicker — purple radial dim + 3 vertical rarity-tinted cards (per-rarity c1/c2/c3 palette + glow), top gem, hexagonal-clip title banner, icon panel with corner brackets + glyph + rarity stamp, colored-keyword description (boon + bane), SELECT → CONFIRM button per card. Reroll button below (once per pact session) burns all 3 cards and swaps in 3 new pacts. ESC / × / backdrop click dismisses with null pact (emits `DARK_PACT_SEALED { mechanicId: null }`); confirm emits `PACT_SEALED { mechanicId, rarity }` + `DARK_PACT_SEALED { mechanicId }` | 34C.3 | ✅ DONE | `src/hud/PactPicker.js`. Replaces `src/ui/popups/DarkPactPopup.js`. Pulls offers via `Game.dungeonMechanicSystem.getOfferings(3, archId, dLv)` with a JSON-cache fallback. Pact-chosen burn + ash-dismissed dissolve use the existing `pact-chosen` / `pact-dismissed` keyframes from the imported design tokens. **The 18 rarity-particle fly-to-buff-row finale isn't implemented** — that's a polish follow-up; the core flow + dissolve animation ship. |
| hud2-moment-postwave | PostWaveSummary — hero strip (THE DUST SETTLES eyebrow + DAY {n} slam + flavor + SLAIN/ESCAPED tally pills with tickers + NET GOLD vault badge with ticker). Left: per-adventurer Fate cards (slain → red border + giant × stamp + fate line; escaped → warn border + leaked-intel chip). Right: MVP MINION card + DUNGEON PERFORMANCE 6-tile stat grid + INTEL LEAKED pulse warning when any escaped. Footer: VIEW DUNGEON LOG + CONTINUE TO NIGHT | 34C.3 | ✅ DONE | `src/hud/PostWaveOverlay.js`. Replaces `src/ui/popups/PostWaveSummaryPopup.js`. Reads `gameState.run.totals` deltas vs the snapshot baseline that EndOfDay passes via `SHOW_POST_WAVE_SUMMARY { snapshot }`. Uses the existing pws-* keyframes for staggered entrance + the `tween()` helper from `dom.js` for stat tickers. Killer sprite on the right of slain rows uses `spriteKindForDefId`. |
| hud2-moment-gameover | GameOver — "YOUR REIGN ENDS" eyebrow fade-up, giant QUEST / FAILED title burn-in with continuous ember glow, italic quote, THE RECKONING panel (6-stat top row with tickers + DAILY HARVEST timeline + 3 MVP cards). Footer: FULL LOG + RISE AGAIN | 34C.3 | ✅ DONE | `src/hud/GameOverOverlay.js`. Subscribes to a new `SHOW_GAME_OVER` event (Game.js emits it on `_onBossFinal` when the new HUD is on; the legacy Phaser GameOver scene path stays for `?newhud=0`). Daily harvest timeline buckets `adventurers.graveyard` by `diedOnDay`. MVP minion = highest `lifetime.kills`. RISE AGAIN stops all gameplay scenes + starts MainMenu fresh. **Open follow-up**: leaderboard submission still happens in the Phaser GameOver scene; need to either port that logic or hook the DOM RISE AGAIN to trigger it before scene teardown. |
| hud2-moment-fulllog | FullLog — opens from GameOver → FULL LOG. Title "DUNGEON LOG · FULL RUN". Filter strip (ALL / KILLS / LEAKS / PACTS / LOSSES / LEVELS). Chronological log with vertical timeline rail, per-day section with gold-bordered "DN" marker badge (final day red + " · THE FALL"), event rows with timestamp + color-coded glyph + text + optional loot tag. Footer: filter count + "SCROLL · ESC TO CLOSE" | 34C.3 | ✅ DONE | `src/hud/FullLogOverlay.js`. The live game doesn't keep per-event timestamps, so the log is synthesized from `history.pacts` + `adventurers.graveyard` (slain) + `adventurers.known` (leaks) + phase bookends per day. A canonical `history.events` channel would let this be richer — tracked as a future enhancement, but the surface is functional now. |
| hud2-title-mainmenu | MainMenu / Title Screen — split layout 1fr / 520px. Left: dark stage with CRT scanlines + vignette + QUEST / FAILED logo (120px each, blood-red FAILED with deep-red shadow). Right: menu panel with "YOUR REIGN, MY LORD" eyebrow, current saved-boss heading (resolves from `bossArchetypes.json` cache), 7 menu items (CONTINUE / NEW EVIL / LEADERBOARD / ROOM EDITOR / TILESET EDITOR / OPTIONS / QUIT), "PRESS Z TO CONTINUE" prompt + Z hotkey, italic flavor, footer (version / SAVE OK / © BONEMAKER · MMXXVI) | 34C.4 | ✅ DONE | `src/hud/MainMenuOverlay.js`. Phaser `MainMenu.create()` now early-returns under the newhud flag and instead lazy-imports + mounts the DOM overlay via a singleton on `window.__game._mainMenuOverlay`. CONTINUE gating: `SaveSystem.hasSave()` — when no save, button is dimmed and the boss heading shows "A NEW DUNGEON AWAITS". OPTIONS opens `SettingsOverlay`. LEADERBOARD opens `LeaderboardOverlay`. ROOM EDITOR / TILESET EDITOR start the existing Phaser scenes. NEW EVIL starts `ArchetypeSelect`. Z key triggers CONTINUE (or NEW EVIL if no save). |
| hud2-title-leaderboard | Leaderboard / Hall of Evil — tab strip (GLOBAL / FRIENDS / PERSONAL), podium row (top 3 with gold/silver/bronze accolade chips), ranked table (left: #, sprite, keeper name + cause, DAYS, KILLS, LEAKS), detail panel (right: portrait + rank-color border + 3 stat tiles + CAUSE OF END callout + NOTABLE PACTS chips + VIEW FULL CHRONICLE button) | 34C.4 | ✅ DONE | `src/hud/LeaderboardOverlay.js`. Reads from existing `src/systems/Leaderboard.js` (`fetchTop(50)`) — Supabase-backed. Maps `{ days_survived, total_kills, boss_id, end_cause }` rows to the design's `{ rank, days, kills, boss, cause, accolade }` shape. Top-3 get IMMORTAL/BUTCHER/CUNNING accolades + per-rank color tint. "YOU" row detected via `PlayerProfile.getName()` match — blood-red gradient + left border. FRIENDS tab is empty (no friends backend yet). **Open follow-ups**: per-row `leaks_count` always 0 (not tracked in leaderboard schema); VIEW FULL CHRONICLE is inert (per-run logs not persisted). |
| hud2-audio-cues | SFX wiring for the new HUD per the design README's "Audio" section. Suggested hooks: button hover, button click, pact card flip, pact reveal, ash dissolve, toast appear, phase transition, level-up impact, kill stinger, leak warning, game-over title burn-in. Each fires through the existing `SfxVolume` system | 34C.5 | ✅ DONE | Two layers: (1) **`src/systems/SfxSystem.js`** added 4 new EventBus subscriptions for the new-HUD-only cues — `PACT_SEALED` → sfx-dark-pact (the satisfying confirm beat, distinct from `SHOW_DARK_PACT` which fires when the picker opens), `INTEL_LEAKED` → sfx-error (rate-limited so a leak storm at wave end doesn't machine-gun), `SHOW_GAME_OVER` → sfx-boss-death (big dramatic stinger), `MINION_BOUNTY_POSTED` → sfx-door-unlock (portentous bell). All route through the existing volume table + SfxVolume slider. (2) **`src/hud/HudSfx.js`** adds a DOM-side bridge for UI cues the EventBus can't observe — delegated `click` + `pointerover` listeners on `#hud-stage` play short chips for any `.btn` / `.qf-mm-item` / bottombar / tab button. Distinguishes danger buttons (sacrifice / abandon) via class regex for a descending-tone variant. ToastQueue fires `playUi('toast')` on every push. Each cue rate-limited (80–200ms cooldowns), each respects `SfxVolume.isMuted()`. Reuses existing audio assets — no new files needed (the design README marked "wire to existing SfxVolume"). |
| hud2-settings-wiring | Wire SettingsOverlay values into gameplay subsystems. **AUDIO** — master / music / sfx folded together: `SfxVolume.setVolume(master×sfx)`, `TitleMusic.setVolume(master×music)`. Ambient slider persists but inert (no ambient channel yet). **VIDEO** — `scanlines` toggles `.scanlines` class on `#hud-root` (CSS already authored), `vignette` toggles `.crt-vignette` class, `fullscreen` calls `document.documentElement.requestFullscreen()` / `exitFullscreen()`, `shake` gates all 4 `cameras.main.shake` call sites (ScreenShakeSystem + BossArchetypeUI Earthquake + DungeonRenderer big-hit + DarkPactPopup). **GAMEPLAY** — `confirmRun` gates the PauseOverlay ABANDON-with-confirm path, `autosave` gates the 3 automatic save sites (NightPhase start + NightPhase phase-transition + DayPhase end + EndOfDay → night), `hotkeys` toggles the HotkeyHints strip visibility. Palette + hotkey-hints landed in their own rows | 34C.5 | ✅ DONE | All wired via `src/hud/userSettings.js` (shared read helper) + inline `localStorage` reads at hot sites to avoid dep cycles. Live preview on settings drag for audio sliders + video toggles; CANCEL reverts cleanly. End-to-end verified: SFX slider at 25% drops `SfxVolume.getVolume()` from 0.800 → 0.175 (master 70% × sfx 25%, math exact); scanlines/vignette class swap on the DOM root. **Particles setting** is the only sub-item still unwired (would need an emitter audit across `AbilityVfx.js` / `BossPactVfx.js` / ember spawns — tracked as a follow-up below). |
| hud2-settings-wiring-particles | Particles quality setting (off / low / med / high) needs an emitter-audit pass. `userSettings.particlesMultiplier()` is ready to be called from emitters; this row is just the work of scanning each particle source and applying the multiplier. Sources include: ember spawns (`Game.js` braziers), `AbilityVfx.js`, `BossPactVfx.js`, `CoinBurstRenderer.js`, hit sparks, dust motes in the ambient particle layer when 34D ships | 34C.5 | ✅ DONE | Each emitter reads `localStorage['qf.video.particles']` inline (off → skip emit, low → ×0.4, med → ×0.7, high → ×1.0) and rounds to a minimum of 1 so a low-quality burst still emits at least one particle. Wired sites: **`AbilityVfx.particleBurst`** (ability bursts), **`BossPactVfx`** (ember swarm, smoke burst, streak lines — geometric counts like crack-spokes and doppelganger copies stay fixed because they affect effect *readability*, not just density), **`CoinBurstRenderer._onAward`** (coin sprites scale; +Ng label always fires), **`HitSparkSystem`** (skips on off only; per-hit single sprites don't density-scale). The DOM ambient layer in DungeonFx already honors this setting via `userSettings.particlesMultiplier()`. Inline-localStorage approach used in src/ui/ + src/systems/ to avoid src/hud/ cross-folder imports. |
| hud2-cinematic-layering | **BUG (known)** — Phaser-rendered cinematics that should overlay everything (BossFightOverlay intro slate, EventBanner top-of-screen Dungeon Event announcement) are visually obscured by the DOM HUD because the Phaser canvas sits below the `#hud-root` z-stack. Currently the player sees the cinematic only through the middle "dungeon view" gap between the top/bottom bars and side panels. Fix options: (a) port both surfaces to DOM (preferred — see `hud2-aux-bossfight` and `hud2-aux-eventbanner` below); (b) have the DOM HUD listen for `BOSS_FIGHT_INCOMING` / event-banner events and fade out while they play; (c) raise the Phaser canvas above the DOM HUD via z-index when a cinematic is active | 34E | ✅ DONE | Resolved via option (a). Both cinematics now have DOM ports that mount into `#hud-stage` at `z-index: 8` — above the FX layer (5) but below the HUD chrome (9–10) so they render unobstructed. The Phaser counterparts in `src/ui/BossFightOverlay.js` + `src/ui/EventBanner.js` are gated behind `!useNewHud` in HudScene so they don't double-fire under the new HUD. |
| hud2-fx-floatingnumbers | Floating combat numbers — DOM elements at sprite-world-position that rise 42px and fade over 1.2s. Five kinds per the design: -N red damage / +Ng gold / ☠ blood-glow kill / MISS grey / status labels (POISONED / STUNNED / BLEEDING etc). Emit every ~700ms at active sprite positions during day phase only. Currently the existing `CombatFeedback.js` renders these as Phaser objects; this row moves them to DOM overlays so they get the design's exact font + animation | 34D | ✅ DONE | `src/hud/DungeonFx.js` — `_onCombatHit` subscribes to `COMBAT_HIT` and routes through `_spawnFloat` (kinds: damage / crit / miss / kill / gold / minion-fell). Coord conversion: `(worldX - cam.scrollX) * cam.zoom` since Phaser FIT mode keeps canvas logical px = stage logical px. Ring-buffer capped at 24 active floats; oldest evicted. **Open follow-up**: status-text floats (POISONED / STUNNED / BLEEDING) not yet wired — those need an event emit from the status systems. |
| hud2-fx-braziers | ~~Brazier light cones — 4 radial light sources at room corners with flicker animation (`@keyframes light-flicker`). DOM divs with `mix-blend-mode: screen` overlaying the Phaser dungeon view. Phaser braziers stay (they're sprite objects); this is the additive glow layer~~ (REMOVED 2026-05-19 — user cut from FX layer) | 34D | 🚫 REMOVED | Initially shipped (`DungeonFx._refreshBraziers` + per-frame camera-sync tick); pulled at user request the same day. Code excised from `DungeonFx.js` along with the rAF tick (was only there to follow camera scroll for the cones). `.qf-fx-brazier` / `.brazier-light` keyframes remain in `styles.css` per the removal-not-deletion policy in case we revisit. |
| hud2-fx-ambient | Ambient particles — 38 dust motes drifting up + 14 embers near light sources. CSS-rendered (`.particle.mote` and `.particle.ember` keyframes already in styles.css); JS just spawns 52 absolutely-positioned divs with randomized animation delays | 34D | ✅ DONE | `DungeonFx._spawnAmbient` reads `userSettings.particlesMultiplier()` and spawns `Math.round(38 * mult)` motes + `Math.round(14 * mult)` embers. `refreshFromSettings()` hook lets SettingsOverlay APPLY rebuild the layer when particles level changes — closes the only remaining wiring follow-up from `hud2-settings-wiring-particles` for the ambient layer. |
| hud2-fx-phasetint | Phase tint overlay — cool blue rgba layer at night, warm red rgba layer by day. Full-canvas DOM div with low opacity over the dungeon view. Switches on phase change with a fade transition | 34D | ✅ DONE | `DungeonFx._applyPhase` swaps background rgba on `DAY_PHASE_BEGAN` / `NIGHT_PHASE_BEGAN`. Initial value sourced from `gameState.meta.phase` so the tint is correct on game-state-restore (not just on phase transitions). |
| hud2-fx-chromatic | Chromatic aberration — `.chromatic` class already authored in styles.css (radial gradients in `mix-blend-mode: screen`). Day variant uses warmer rgba, night uses cooler. Applied to a full-canvas DOM div behind the HUD | 34D | ✅ DONE | `.qf-fx-chromatic` div nested inside `#qf-fx`. `_applyPhase` toggles a `.day` class on it; CSS variants cover both phases. Sits at `z-index: 5` between Phaser canvas and HUD chrome. |
| hud2-fx-roomtooltip | Dungeon-view room hover tooltip — 2-tab tooltip (STATS / HISTORY). Triggered by Phaser hit detection on placed rooms; renders as a DOM tooltip positioned near the cursor. Tracked separately from `hud2-room-tooltip` in 34B because the FX layer is the natural place to land it once dungeon-view coords ↔ DOM coords sync is solved | 34D | ✅ DONE | Shipped early under `hud2-room-tooltip` (34B). Marked done here for consistency. |
| hud2-aux-archetype-strip | BossArchetypeUI — the in-dungeon archetype-specific action strip (Golem Earthquake target picker, Demon Sacrifice target picker, etc). Currently a Phaser surface (`src/ui/BossArchetypeUI.js`) that positions itself just above where the old ActionBar lived. **Not in design source** — the design's BottomBar has no archetype-specific slot. Resolution: either (a) fold the buttons into a new DOM strip that lives above the new BottomBar with parity styling, or (b) keep Phaser but adjust positioning to clear the DOM BottomBar. Currently works but with subtle visual mismatch (Crypt-themed Phaser buttons next to the new Crypt-themed DOM HUD) | 34E | ✅ DONE | Resolution (a). `src/hud/BossArchetypeStrip.js` renders the two player-facing buttons (EARTHQUAKE / SACRIFICE) as a DOM strip just above the new BottomBar. Hides during night phase + for archetypes that don't have an action; pulse-animates when armed (matches the Phaser version's "PICK A ROOM" / "PICK A MINION" label flip). Emits the same `GOLEM_EARTHQUAKE_ARM` / `DEMON_SACRIFICE_ARM` events and subscribes to the `*_ARMED` / `*_DISARMED` / `*_FIRED` echoes for label sync. The Phaser `BossArchetypeUI` retains all ~400 lines of archetype VFX (charm rings, blood-tax beams, sacrifice flames, etc) — those draw on the Game scene canvas and stay correct regardless of HUD layer. Phaser `_buildEarthquakeButton` / `_buildSacrificeButton` skipped under newhud via an inline `localStorage` check so the buttons don't double-render. |
| hud2-aux-bossfight | BossFightOverlay — full-screen cinematic for boss-fight intro / result slates (`BOSS_FIGHT_INCOMING` / `BOSS_FIGHT_RESOLVED`). Currently Phaser (`src/ui/BossFightOverlay.js`); intended as full-screen but obscured by DOM HUD chrome (see `hud2-cinematic-layering`). DOM port: portrait + name + tagline + diamond ornament, fade in/out animations using existing keyframes | 34E | ✅ DONE | `src/hud/BossFightOverlay.js`. Three pieces mirrored from the Phaser version: (1) intro slate with portrait + name + tagline + diamond ornament + lives remaining, fade in 400ms / hold 1000ms / fade out 400ms; (2) bottom HP bar with rAF tick reading `gameState.boss.hp` each frame, threshold-colored fill (poison > 50% / gold > 25% / blood ≤ 25%), shake 140ms on small hits + 240ms + white flash on heavy hits (≥8 dmg or ≥8% max) — shake gated by `userSettings.isShakeEnabled()`; (3) result slate with party-won / boss-won variants (red "YOU LOST A LIFE" / green "INTRUDER REPELLED"), 1800ms hold + 600ms fade. Vignette is a 4-edge `inset box-shadow` for the dim-around-the-fight look (cheaper than the Phaser 16-rect stack). Portrait is a live snapshot of the Phaser `${bossArchetypeId}-idle` texture's frame 0 painted into a `<canvas>` so it matches the in-game sprite exactly (with pixelated smoothing). Phaser `src/ui/BossFightOverlay.js` gated under `!useNewHud` in HudScene. |
| hud2-aux-eventbanner | EventBanner — top-of-screen themed slate announcing a Dungeon Event during the night phase. Currently Phaser (`src/ui/EventBanner.js`); also obscured by DOM HUD top bar. DOM port should mount below the top bar (clearing the chrome) with the same fade in/out + color-by-event-theme | 34E | ✅ DONE | `src/hud/EventBanner.js`. Listens to `DUNGEON_EVENT_ANNOUNCED`, swaps class name to one of 5 theme variants (`qf-eventbanner-warn` / `-accent` / `-soul` / `-gold` / `-green`) each with its own fill / border / title / sub colors matching the Phaser palette. Anchored top-center at `top: 130px` so it clears the new TopBar; fade in 350ms / hold 4500ms / fade out 600ms with a small Y-translate for arrival/departure motion. A new announcement while one is showing bumps the current banner immediately (timers cleared + reflow before re-opening). Phaser `src/ui/EventBanner.js` gated under `!useNewHud` in HudScene. |
| hud2-aux-dossier | DossierPanel — daily start-of-day briefing card per spawning adventurer (name + class + personalities + prior-visit count + gear bonuses + active personality combos). Auto-dismisses after 5s or on click. Currently Phaser (`src/ui/DossierPanel.js`) shown by DayPhase. DOM port for design consistency — could pair with AdvIntel as the "live" version once the day begins | 34E | ✅ DONE | **Superseded** — Phaser DossierPanel was already disabled in Phase 31C (see `DayPhase.js` comment: "old DossierPanel + ARRIVAL banner are legacy chrome that conflicted visually with the new HUD. The Adventurer Intel popup (31E) replaces the dossier"). The new HUD's design source has AdvIntel, not a separate Dossier, so no DOM port is needed. The Phaser file stays in the repo as legacy chrome under the removal-not-deletion policy. Player can open `OPEN_ADV_INTEL` for the same information at any time during the day. |
| hud2-aux-wantedposter | WantedPoster — parchment-styled popup notification for a minion crossing the bounty kill threshold (`MINION_BOUNTY_POSTED`). Top-right corner, auto-dismiss after ~5s. Currently Phaser (`src/ui/WantedPoster.js`). Natural fit as a toast kind on the new ToastQueue | 34E | ✅ DONE | Folded into ToastQueue as the new `'bounty'` kind. ToastQueue now subscribes to `MINION_BOUNTY_POSTED` and pushes a parchment-styled 3-line toast (`★ WANTED · <NAME> ★` / `<type> · N kills · M gear` / italic *"Hunters approach. Reinforce the wing."*). Longer dwell (5200ms vs the default 3400ms) so the 3 lines have reading time. Bounty variant has its own `.qf-toast-bounty` styling — parchment gradient + gold border + non-truncated subtitle. Phaser `src/ui/WantedPoster.js` gated behind `!useNewHud` in Game.js. Minion type name resolved from the `minionTypes` JSON cache, same source the Phaser version used. |
| hud2-aux-minioninspector | MinionInspector — click-on-minion popup showing name + level + kills + evolution history (`MINION_CLICKED`). Currently Phaser (`src/ui/MinionInspector.js`) in NightPhase. DOM port should align with the design's Roster detail card visually | 34E | ✅ DONE | `src/hud/MinionInspectorOverlay.js`. Mid-right popup (sits to the left of the RightPanels strip), opens on `MINION_CLICKED`, rebuilds itself on `MINION_LEVELED_UP` / `MINION_EVOLVED` / `MINION_NAMED` so it stays current. Layout: title + rename (✎) + close (×) header, type · Level N · ★ BOUNTY sub, 4-stat block (HP / ATK / DEF / SPD) + 3-stat block (XP / Kills / Faction), and EVOLUTIONS history list. Rename now uses the new DOM `NameEntryOverlay` instead of `window.prompt()`. Auto-closes on phase change to match the Phaser version. Phaser `MinionInspector` construction in Game.js gated behind `!useNewHud`. |
| hud2-aux-cartographer | CartographerOverlay — paints adventurer planned paths during the Pact of the Cartographer (every adv's tile-by-tile path renders as dots). Per-frame Phaser overlay on the dungeon view. **Keep Phaser** — this is gameplay-layer overlay tied to world coordinates, not chrome | 34E | ✅ DONE | Marked done; no DOM port needed by design. |
| hud2-aux-knowledgeoverlay | KnowledgeOverlay — toggleable day-phase room heat-map (red→blue gradient by leaked-intel warmth). Phaser overlay over dungeon view. **Keep Phaser** — same reason as cartographer | 34E | ✅ DONE | Marked done; no DOM port needed by design. |
| hud2-aux-nameentry | NameEntryPanel — text-entry modal for naming a minion. Used by Roster's RENAME button (which is currently UI-only in the DOM port). DOM port = small text-input modal using the Overlay shell | 34E | ✅ DONE | `src/hud/NameEntryOverlay.js`. Centered modal with title + instruction + native `<input type="text">` (real DOM input → free selection/copy/paste/IME, unlike the Phaser version's manual keystroke loop), CONFIRM + CANCEL buttons. Closes on Enter (submit non-empty), Escape (cancel), backdrop click, or button click. MAX_LEN=16 matches Phaser. Used by both `RosterOverlay`'s RENAME button (previously inert) and `MinionInspectorOverlay`'s ✎ rename pencil. |
| hud2-aux-audiocontrols | AudioControls — Phaser slider widget (`src/ui/AudioControls.js`). **Replaced by SettingsOverlay AUDIO tab + the audio-slider wiring in 34C.5**. The Phaser widget only renders inside the old Phaser PauseMenu, which is not constructed under newhud, so it's effectively dead | 34E | ✅ DONE | Verify by deleting after the section-31 deprecation pass. |
| hud2-section31-deprecation | Once all Phase 34 rows ship, formally mark Section 31 (the original Crypt-themed Phaser HUD overhaul from 2026-05-01) as superseded-but-shipped per CLAUDE.md's removal-not-deletion policy. The Phaser chrome modules (`BossTopBar.js`, `ActionBar.js`, `BuildMenu.js`, `MiniMapPanel.js`, `KnowledgePin.js`, `DungeonLog.js`, all Phaser popups) stay in the repo as fallback under `?newhud=0` but get a "see section 34" note. Delete only after a few weeks of new-HUD playtesting | 34E | ✅ DONE | SUPERSEDED banners added to the top of every Phaser chrome module: `ActionBar`, `BossTopBar`, `BuildMenu`, `MiniMapPanel`, `KnowledgePin`, `DungeonLog`, `DossierPanel`, `BossFightOverlay`, `EventBanner`, `WantedPoster`, `MinionInspector`, `NameEntryPanel`, `AudioControls`, and every popup in `src/ui/popups/` (PopupFrame + 12 popups). Each banner points to its DOM replacement. Files stay in the repo as the `?newhud=0` fallback per CLAUDE.md's removal-not-deletion policy. Actual deletion deferred per the row's original "after a few weeks of playtesting" plan. |

---

## 35. Companion NPC — Lilith (2026-05-21)

> Persistent in-HUD succubus companion who reacts to everything the player does and everything that happens in the dungeon, delivers the tutorials, and breaks the fourth wall. Bottom-left peeking portrait + RPG chat bubble. Personality: flirty + wicked with a sarcastic/sinister edge. 42 hand-drawn expression sprites. Data-driven line bank. Hideable via a 3-way Off/Quiet/Normal option. Source sprites: `D:\Documents\Game Jam Code\Quest-Failed assets\Main NPC` (42 PNGs).

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| npc-assets | Downscale the 42 source expression PNGs (1618×2400) to ~560px-wide and copy into `assets/npc/` with kebab-case ids. One-time bake tool | 35 | ✅ DONE | `tools/bake-npc-sprites.mjs` (sharp). 42 → `assets/npc/<id>.webp` at 560×831, q84. 142.8MB → 3.2MB. Re-runnable; source untouched. Explicit name map normalises the misspelled `mischevious` → `mischievous` etc. |
| npc-data | `src/data/npcLines.json` — data-driven line bank. Trigger categories each with `priority` + `cooldownMs`; lines `{ t, x, w }`. Variable substitution tokens. Expandable | 35 | ✅ DONE | 41 categories, 442 lines. Voice: flirty/wicked/sarcastic/sinister, 4th-wall-aware. Loaded via Preload (`npcLines` cache key). Every `x` resolves to a baked sprite (verified). |
| npc-director | `src/systems/NpcDirector.js` — subscribes to EventBus, selects lines, manages priority bands / per-category cooldowns / interrupt + pending-tutorial handling / recently-said dedup / idle timer. Emits `NPC_SAY`. Wired in `Game.js` (before TutorialSystem) + destroyed in shutdown | 35 | ✅ DONE | The reactive brain. Verified: idle auto-fire, event reactions, token resolution. |
| npc-companion | `src/hud/NpcCompanion.js` + styles — bottom-left peeking portrait, RPG chat bubble (typewriter), idle-breathing + talk-bob animation, two-layer expression cross-fade, paged tutorial mode (▶). Mounted as a `HudRoot` panel | 35 | ✅ DONE | DOM HUD component; listens for `NPC_SAY`, emits `NPC_POKE` / `NPC_TUTORIAL_DONE`. `.qf-npc` CSS block appended to `styles.css`. |
| npc-reactions | Broad event coverage — Director hooks player actions (place/sell/pact/blocked-placement/begin-day/marionette), game events (adv enter/die/flee, intel leak, party wipe, minion place/die/level/evolve/bounty, boss fight/level, grid expand, trap, dungeon event, bounty hunter, Hero), first-blood + wave-cleared beats | 35 | ✅ DONE | ~30 events mapped in `REACTIONS`. Lines span all 42 expressions. minion/room/trap placement gated to night phase to filter free archetype spawns. |
| npc-idle | Context-aware ambient/idle lines — fire after a 23s idle window; phase-aware pools (idle_night / idle_day / idle_general / idle_4thwall) with `{day}` / `{gold}` / `{level}` tokens | 35 | ✅ DONE | Idle only on `normal` mode; suppressed while paused / mid-tutorial. |
| npc-tutorials | Lilith fully delivers tutorials — `NpcDirector` intercepts `SHOW_TUTORIAL`, converts to paged dialogue (lead/body/tips), fires `onClose` when the player pages past the last panel. `TutorialOverlay` defers to her unless the companion is Off | 35 | ✅ DONE | softPause held while a tutorial bubble is up. Welcome greeting plays first, then the first tutorial (pending-tutorial slot). Verified onClose fires → TutorialSystem queue advances. |
| npc-poke | Clicking Lilith pops a random "poke" line — Companion emits `NPC_POKE`, Director answers from the `poke` category | 35 | ✅ DONE | Suppressed during a tutorial. |
| npc-option | `qf.gameplay.companion` setting (Off / Quiet / Normal, default Normal) in `userSettings.js` + a GAMEPLAY radio in `SettingsOverlay.js` (emits `SETTINGS_CHANGED` on APPLY). Off hides her entirely; Quiet suppresses priority < 2 lines + idle | 35 | ✅ DONE | Fulfills the design's "hide the NPC" requirement. Verified off→display:none, normal→shown. |
| npc-x-intro | Lilith delivers the new-run intro herself — paged bubble: self-introduction + her keeper role + game premise, final page a Yes/No choice for tutorial hints. `WelcomeIntroOverlay` defers to her (emits `NPC_DELIVER_INTRO`) unless the companion is Off | 35 | ✅ DONE | `intro` block in `npcLines.json` (6 pages, last carries `choices`). NpcDirector `_deliverIntro` (priority 5) → `NPC_SAY {kind:'intro'}`; `NPC_CHOICE` → sets `meta.introSeen`/`tutorialEnabled` + `INTRO_DISMISSED`. Bubble gained choice-page rendering. Verified end-to-end. |
| npc-x-hintoptout | Tutorial bubbles carry a small "turn off hints" control — sets `qf.gameplay.tutorials=false` and closes the current hint (TutorialSystem drops the rest) | 35 | ✅ DONE | `.qf-npc-optout` link; `_disableHints()` in NpcCompanion. Verified: click sets the flag + fires `NPC_TUTORIAL_DONE`. |
| npc-x-menudock | Docked-beside-menu mode — opening Knowledge / Boss Overview / Adv Intel / Roster / Full Log / Game Over / Post-Wave / Level-Up makes Lilith step out full-body to the left of the modal and comment; returns to the corner on close | 35 | ✅ DONE | Generic `HUD_MENU_OPENED/CLOSED { kind }` on the `Overlay` shell via an `npcKind` opt; tagged on all 8 overlays. Companion `data-dock` state (CSS-animated corner↔menu, z 8↔160 to clear the z:150 overlay backdrop). 8 `menu_*` categories + 15s follow-up commentary. Verified dock/undock + commentary. |
| npc-x-pactbroker | Lilith replaces the Grimoire's demon broker (was the locked VESPERYN succubus sprite) — she docks beside the pact picker and presents the three pacts in her voice. Book + card UI unchanged | 35 | ✅ DONE | `PactPicker.js` dropped the entire `BROKERS` sprite system (data + frame-players + speech bubble, ~150 lines); `_brokerSay()` now emits `NPC_BROKER_SAY { text, expr }` which NpcDirector shows at priority 3. Picker emits `HUD_MENU_OPENED/CLOSED { kind:'pact' }`; the Director skips generic `menu_pact` so the picker's rich contextual LINES (closed/open/view-by-rarity/seal) own her pact speech. |
| npc-x-bigger | Corner portrait sized slightly larger than the first minimised pass (202→236px wide) | 35 | ✅ DONE | CSS only. |
| npc-d-specifics | Keyed "specifics" bank in `npcLines.json` + NpcDirector lookup — when a bespoke line exists for an entity id it is preferred, else the generic category. One mechanism serves boss/event/room/minion/trap/item/class commentary | 35 (batch 1) | ✅ DONE | `specifics[domain][id]`; `_specificId()` resolves the key per domain; `_pickFrom()` picks from a raw pool. Verified: prefers bespoke, falls back to generic. |
| npc-d-boss | Per-boss-archetype lines (11 archetypes × 4) — drawn into idle musings so each run feels distinct | 35 (batch 1) | ✅ DONE | `specifics.boss`; `_maybeIdle` adds a `_boss` source with a 42s cooldown. |
| npc-d-class | Per-adventurer-class lines (13 classes × 4) fire when that class enters the dungeon | 35 (batch 1) | ✅ DONE | `specifics.advClass`; `ADVENTURER_ENTERED_DUNGEON` carries `specific:'advClass'`. Kill/flee keep the generic `{advClass}`-token lines. |
| npc-d-event | Per-dungeon-event lines (26 events × 3) on `DUNGEON_EVENT_ANNOUNCED` | 35 (batch 2) | ✅ DONE | `specifics.event`; `DUNGEON_EVENT_ANNOUNCED` carries `specific:'event'` (keyed by `def.id`). |
| npc-d-room | Per-room-type lines (20 rooms × 3) on `ROOM_PLACED` | 35 (batch 2) | ✅ DONE | `specifics.room` (keyed by `definitionId`); boss_chamber excluded — never player-placed, falls back to generic. |
| npc-d-minion | Per-minion-family lines (19 families × 3, keyed by definitionId minus trailing digits) on `MINION_PLACED` | 35 (batch 3) | ✅ DONE | `specifics.minion`; `specific:'minion'` family-key strip. |
| npc-d-trap | Per-trap lines (8 traps × 3) on `TRAP_PLACED` | 35 (batch 3) | ✅ DONE | `specifics.trap` keyed by `definitionId`. |
| npc-d-item | Per-item lines (4 banks × 3 — door_lock/treasure_chest/soul_bound_beacon/phylactery_heart) | 35 (batch 3) | ✅ DONE | Wired `LOCK_PLACED`/`TREASURE_CHEST_PLACED`/`BEACON_PLACED`/`PHYLACTERY_PLACED` with a fixed `specificId`. |
| npc-d-soldmoved | `sold` split into `sold_room`/`sold_minion`/`sold_trap`/`sold_item` (per-kind, ~6 each); non-sell removals (`reason` set) skipped. New `ROOM_MOVED` event emitted from NightPhase move-drop → `moved` commentary | 35 (batch 3) | ✅ DONE | `ROOM_MOVED` added at NightPhase ~2033, gated by `wasMoveDrop`. |
| npc-d-advisor | Build-suggestion advisor — `suggestion` bank (34 lines, 16 conditions: lowGold/richGold/noTraps/noTrapFactory/noBarracks/highExposure/manyMinions/manyTraps/…); surfaced during night-phase idle, gated by `NpcDirector._checkCond` | 35 (batch 4) | ✅ DONE | Includes playstyle observations (trap-lover, swarm-tyrant, sprawl) as conditions. |
| npc-d-nudge | Inactivity nudges — `idle_nudge` bank, 3 escalating tiers; `_lastActionAt` reset by any player action; fires after 70s idle in night phase, every 32s, escalating | 35 (batch 4) | ✅ DONE | bored/impatient/sleeping/eye-roll/unimpressed faces. Verified tier escalation + action-reset. |
| npc-d-systems | Game-system commentary — `combo` category on `PERSONALITY_COMBO_ACTIVATED`. Knowledge leaks / minion evolution / bounties already had reaction categories from the base bank | 35 (batch 4) | ✅ DONE | — |
| npc-d-extras | `day_milestone` (days 10/25/50/75/100/150/200) + `last_life` (boss down to its final life) | 35 (batch 4) | ✅ DONE | — |
| npc-d-extras2 | The previously-deferred extras, now shipped (batch 5): `streak` (consecutive full-party-wipe days, milestones 3/5/8/12/20/30); `minion_milestone` (named-minion kill milestones 5/12/25/40/60, tracked Director-side off `ADVENTURER_DIED.killerId`); 8 `arch_*` categories wired to 9 boss-archetype signature-ability events (earthquake/sacrifice/hellgate/petrify/charm/hunt-pack/spores/haunt — high-frequency per-hit events excluded); `gamble_won`/`gamble_lost` on the Gambler's Coin outcome | 35 (batch 5) | ✅ DONE | Other night-modal events (Negotiation/Black Market/etc.) have no choice-outcome event to hook — their announce is already covered by `npc-d-event`. |

---

## 36. Second companion — Malakor + CompanionSelect (2026-05-21)

> A second companion, **Malakor** — rude, sinister, roasts the player, but loyal. Built the same way as Lilith; the player picks one companion per run on a new CompanionSelect screen between NEW EVIL and the boss picker. Malakor ships with 39 expression sprites. Full parallel dialogue bank authored in batches. Source sprites: `Quest-Failed assets/Main NPC 2` (39 PNGs).

| ID | Item | Phase | Status | Notes |
|---|---|---|---|---|
| cmp-registry | `src/systems/companions.js` — companion registry (id → name, tagline, spriteDir, restExpr, expressions, linesKey). `getCompanion()` falls back to default for missing/legacy ids. Makes NpcCompanion / NpcDirector / Preload / CompanionSelect companion-agnostic | 36 | ✅ DONE | Lilith (42 expr) + Malakor (39 expr). A 3rd companion is a data edit. |
| cmp-assets | Bake Malakor's 39 source PNGs → `assets/npc-malakor/<id>.webp`. `tools/bake-npc-sprites.mjs` generalised to a per-companion `COMPANIONS` map | 36 | ✅ DONE | 39/39, 93.1MB → 2.8MB at 560px wide. Re-runnable. |
| cmp-aware | `NpcCompanion` + `NpcDirector` resolve sprite dir / expression list / rest face / dialogue bank from the registry via `gameState.meta.companionId`. `HudRoot` passes gameState to NpcCompanion. Preload loads `malakorLines.json` | 36 | ✅ DONE | Backward-compatible: a save without `companionId` falls back to Lilith. |
| cmp-state | `gameState.meta.companionId` (default companion). `createGameState()` takes a `companionId` arg; `ArchetypeSelect` reads the validated localStorage `qf.companion` pick and passes it in | 36 | ✅ DONE | Per-run choice; remembered between runs in localStorage. |
| cmp-select | `CompanionSelect` scene + `CompanionSelectOverlay` DOM screen — runs after NEW EVIL, before ArchetypeSelect. Both companions full-body side by side; hover previews; click selects; CONFIRM → boss picker. Hovered one pitches, rival heckles. Registered in `main.js` | 36 | ✅ DONE | Recruitment dialogue from each bank's `recruit.pitch` / `recruit.heckle`. Keyboard nav (←/→/Enter/Esc). |
| cmp-malakor-lines | `src/data/malakorLines.json` — Malakor's dialogue bank: rude/sinister/roasting/loyal voice, consistent "little king" mocking-honorific address (consolidated 2026-05-22 from the original mix of "boss"/"your dread majesty"/"little king"), his 39 expressions. Same category + specifics structure as Lilith | 36 | ✅ DONE | **981 lines total — exact parity with Lilith's bank (981).** All 70 reaction categories (637 lines, matched line-for-line to Lilith's per-category density) + all 7 `specifics` domains at full key parity (boss 11 / advClass 13 / event 26 / room 20 / minion 19 / trap 8 / item 4 = 327 lines) + intro (6 pages) + recruit (pitch 6 / heckle 5). All 39 expressions used; every line `x` validated against the baked set; 0 duplicate lines. |
| cmp-malakor-sprites | All 39 Malakor expression sprites delivered + baked (was 20; user added the rest) | 36 | ✅ DONE | `assets/npc-malakor/` 39 webp. companions.js + malakorLines.json expression lists updated to match. |
| cmp-recruit-lilith | `recruit` block added to `npcLines.json` so Lilith pitches/heckles on the CompanionSelect screen | 36 | ✅ DONE | 6 pitch + 5 heckle, flirty/charming-dismissal voice. |
| cmp-malakor-verify | End-to-end verification of the second companion + select screen | 36 | ✅ DONE | Isolated-component tests: registry + fallback; NpcCompanion companion-aware (malakor/lilith/legacy); CompanionSelect renders both cards, hover→pitch/heckle, confirm→persist+ArchetypeSelect; all 39 sprites HTTP-served; both banks load (70 categories + 327 specifics). Full in-game run not bootable in the backgrounded preview — same constraint as the Lilith verification. |
| cmp-bossscreen | `ArchetypeSelect` decorative surround — Phaser atmosphere (edge vignette + warm candle-glow + drifting embers), and a DOM overlay (`src/hud/ArchetypeDecorOverlay.js`) for the header (`PICK YOUR DUNGEON BOSS`), the footer instruction line + BACK button, and the chosen companion. Boss-accent tinting (glow + header eyebrow follow the hovered boss's colour). The book + picker grid are untouched | 36 | ✅ DONE | The header/footer/companion are DOM so they reuse the CompanionSelect screen's exact pixel fonts + the in-game `.qf-npc-bubble` chat-bubble styling (via `.qf-cmpsel-bubble`). Companion (Lilith/Malakor) stands at the RIGHT as a DOM `<img>` at natural full size (feet grounded, mirrored to face the book), reacts to each hovered boss with a typed line from their `specifics.boss` bank + matching expression + per-letter speech blip. Overlay root is pointer-events:none so the Phaser book keeps every click. A pixel corner-frame was added then removed at the player's request. Per-visit state reset in `create()` so the companion re-appears after BACK → re-pick. Syntax + data-wiring reviewed; full in-game run not bootable in the backgrounded preview (same constraint as the companion verifications). |

---

## 37. Third companion — Zul'Gath (2026-05-22)

> A third companion, **Zul'Gath** — an ancient male dragon. Personality: dry, deadpan, eons-old, unbothered, takes the long view; breaks the fourth wall (perceives the loop/reloads). Addresses the player as "small one". Built the same way as Lilith / Malakor; full parallel dialogue bank. Source sprites: `Quest-Failed assets/Main NPC 3 - Zul'Gath` (39 PNGs). A 4th companion is planned — the system is built to take it as a data edit.

| Item | Description | Phase | Status | Notes |
|---|---|---|---|---|
| zul-sprites | Bake Zul'Gath's 39 source PNGs → `assets/npc-zulgath/<id>.webp`; `tools/bake-npc-sprites.mjs` extended with his `COMPANIONS` map entry | 37 | ✅ DONE | 39/39. His art is a WIDE landscape composition (~2400×1339, aspect ~1.79) — the bake config gained a per-companion `width` override; he bakes at 1400px wide (781px tall) so he is crisp wherever shown (95.5MB → 4.4MB). Lilith's + Malakor's stale `srcDir` paths fixed to the renamed `Main NPC 1/2 -` folders in the same pass. |
| zul-registry | `companions.js` registry entry — id `zulgath`, name `Zul'Gath`, restExpr `idle`, 39 expressions, `linesKey: zulgathLines`; added to `COMPANION_ORDER` | 37 | ✅ DONE | `portraitFlipX: true` (sits right of the 3-card line-up, faces inward). Wide-sprite tuning so a landscape dragon fits the portrait-shaped slots without affecting the tall humanoids: `portraitScale 0.9` (CompanionSelect), `hudScale 2.5` + `hudBubbleScale 1` + `hudImgOrigin '100% 100%'` (in-game HUD — large, bubble kept low, bulk spills off-screen left). The companion `<img>` CSS in all three places (CompanionSelect, boss-picker, in-game) switched to `contain` sizing so a wide sprite is width-limited instead of overflowing — a no-op for the tall humanoid sprites. |
| zul-lines | `src/data/zulgathLines.json` — full parallel dialogue bank: ancient deadpan dragon voice, "small one" address, central 4th-wall awareness. Same 71 categories + 7 `specifics` domains as the other banks | 37 | ✅ DONE | **1011 lines** (Malakor 1021, Lilith 1041). intro 6 pages + recruit 28 banter + 71 categories + specifics at full key parity (boss 11 / advClass 13 / event 26 / room 20 / minion 19 / trap 8 / item 4). **All 39 expressions used** — the panic/distress faces (`scared`/`crying`/`shocked`/`guilty`/`shame`) appear sparingly, as rare dry cracks in his calm. Preload loads `zulgathLines.json`. |
| zul-recruit | Round-robin bicker on `CompanionSelectOverlay` — `_next()` rotates the turn through every companion in `COMPANION_ORDER` (3 now, 4-ready), not a 2-way back-and-forth. Cross-banter added to Lilith's + Malakor's `recruit.banter` so they reference Zul'Gath (each bank now 35 banter lines) | 37 | ✅ DONE | Zul'Gath's own `recruit.banter` (28 lines) needles both rivals. |
| zul-layout | CompanionSelect card layout sized so up to **four** companion cards fit the 1920-wide stage in one row — card 540→410px, bubble 420→372px, portrait 540→462px, gap 64→34px; entrance-stagger `nth-child(3)`/`(4)` added | 37 | ✅ DONE | 3 cards display now; a 4th is a pure data edit. CSS not visually verified (backgrounded-preview constraint). |
| zul-verify | Verification of the third companion | 37 | ✅ DONE | All 3 banks parse valid; zulgathLines 999 lines, every `x` validated against the baked 39-sprite set; companions.js + CompanionSelectOverlay.js syntax-checked; all 39 webp sprites baked. Full in-game run not bootable in the backgrounded preview — same constraint as the Lilith / Malakor verifications. |

---

## 38. Expanded expression sets (2026-05-22)

> The user added more hand-drawn expression sprites for all three companions, and asked that each new face be used in dialogue as much as the existing ones. Lilith's `cute-2` / `mischievous` / `mischievous-2` art was also redrawn (same ids — old versions removed by re-bake).

| Item | Description | Phase | Status | Notes |
|---|---|---|---|---|
| expr-bake | `tools/bake-npc-sprites.mjs` map extended with 27 new source→id rows (Lilith +18, Malakor +3, Zul'Gath +6); full re-bake run | 38 | ✅ DONE | Lilith 63/63, Malakor 43/43, Zul'Gath 45/45 baked. Re-bake also picked up Lilith's redrawn `cute-2`/`mischievous`/`mischievous-2` art (same filenames + ids). |
| expr-registry | `companions.js` + each bank's embedded `expressions` array extended with the 27 new ids | 38 | ✅ DONE | Lilith 45→63, Malakor 40→43, Zul'Gath 39→45. companions.js ↔ embedded arrays ↔ baked webp all consistent (verified: every expression has a webp, no dupes). |
| expr-lilith | Integrate Lilith's 18 new expressions into `npcLines.json` | 38 | ✅ DONE | Romance/affection shades, vanity, cruelty, idle beats. ~67 lines re-tagged + ~109 new lines authored; npcLines 1041→1150 lines. Each new expression used 10–13×. |
| expr-malakor | Integrate Malakor's 3 new expressions into `malakorLines.json` | 38 | ✅ DONE | `battle-roar` / `menacing` / `salute` — 17 uses each (10 re-tags + 7 new lines per expression); malakorLines 1021→1042 lines. |
| expr-zulgath | Integrate Zul'Gath's 6 new expressions into `zulgathLines.json` | 38 | ✅ DONE | `superior` / `bored-2` / `nostalgic` / `self-satisfied` / `smug` / `wistful` — 10–14 uses each (~31 re-tags + ~31 new lines); zulgathLines 1011→1042 lines. |
| expr-topup | Top-up pass — raise the rarely-used tail of *existing* expressions in all 3 banks | 38 | ✅ DONE | Every expression now used ≥9× — except the genuine-distress faces on the stoic characters, held at a deliberate floor of 6 (Malakor `crying`/`scared`/`smile`; Zul'Gath `crying`/`scared`/`shame`/`guilty`/`sad`/`shocked` — kept rare per the design's "rare dry cracks in his calm"). ~260 lines re-tagged/authored, all in-voice. Final bank sizes: npcLines 1234, malakorLines 1099, zulgathLines 1155 lines. |
| expr-verify | Verify sprites + dialogue | 38 | ✅ DONE | All 3 banks parse valid; every `x` value validated against the (new) expression sets — 0 invalid ids; every expression used ≥1×; all new expressions hit target; all 151 webp (63+43+45) present on disk. Post-topup re-verify: 0 invalid ids, min usage 9 (Lilith) / 6 (Malakor & Zul'Gath distress floor). |

---

## 39. Fourth companion — Safira (2026-05-22)

> The fourth and final companion, **Safira** — a chaotic, dazzling wish-granting genie. Personality: manic over-eagerness, frames the player's actions as "wishes" she grants, swings between giddy delight and theatrical panic; breaks the fourth wall harder than any other companion (direct & intimate, vs Zul'Gath's serene detachment). Addresses the player as "Master". Built the same way as the others; full parallel dialogue bank. Sits 3rd of 4 on the recruit screen (between Malakor and Zul'Gath), facing right. Source sprites: `Quest-Failed assets/Main NPC 4 - Safira` (53 PNGs). Completes the companion roster.

| Item | Description | Phase | Status | Notes |
|---|---|---|---|---|
| safira-sprites | Bake Safira's 53 source PNGs → `assets/npc-safira/<id>.webp`; `tools/bake-npc-sprites.mjs` extended with her map entry | 39 | ✅ DONE | 53/53. Tall portrait art (~1618×2400, aspect 0.67) like the humanoids — default 560px bake width, no per-companion override (170.2MB → 5.4MB). |
| safira-registry | `companions.js` entry — id `safira`, name `Safira`, restExpr `idle`, 53 expressions, `linesKey: safiraLines`; inserted into `COMPANION_ORDER` at index 2 (`lilith, malakor, safira, zulgath`); `Preload` loads `safiraLines.json` | 39 | ✅ DONE | Standard humanoid tuning (`portraitScale 1`, `portraitFlipX false` → faces right toward Zul'Gath, `hudScale 1.15`) — no wide-sprite handling. CompanionSelect overlay + CSS were already 4-card-ready, so the 4th card needs no code change; added a `data-id="safira"` accent colour. |
| safira-lines | `src/data/safiraLines.json` — full parallel dialogue bank: chaotic wish-granting genie voice, "Master" address, heavy 4th-wall. Same category + specifics structure as the other banks | 39 | ✅ DONE | **1156 lines** — structural twin of `zulgathLines.json` (71 categories, 7 `specifics` domains, intro 6 pages, recruit 28 banter). All 53 expressions used (`unimpressed` was added in a fix-up pass — it was omitted from the authoring brief). 0 invalid `x` ids. |
| safira-recruit | Round-robin bicker now 4-way; cross-banter referencing Safira added to Lilith's / Malakor's / Zul'Gath's `recruit.banter`; Safira's boss-select-screen greeting added to `ArchetypeDecorOverlay` | 39 | ✅ DONE | ~7 Safira lines added to each of the other 3 banks (banter now 44 / 41 / 34). Zul'Gath's prescient "soon there will be a fourth" line turned into a direct Safira reference. Zul'Gath's wrong boss-screen greeting ("my liege") fixed — `_greet` is now a per-companion map. |
| safira-verify | End-to-end verification of the fourth companion | 39 | ✅ DONE | All 4 banks parse; safiraLines 1156 lines, 0 invalid `x`, all 53 expressions used, all 53 webp baked; companions.js / Preload / ArchetypeDecorOverlay / CompanionSelectOverlay syntax-checked; `COMPANION_ORDER` = lilith,malakor,safira,zulgath. |
| safira-topup | Top up Safira's rarely-used expressions to the ~9 floor used for the other banks | 39 | ✅ DONE | 20 under-used expressions (flirty / sexy / lamp / devotion / distress faces) raised to ≥9 — ~80 new in-voice lines + 3 re-tags. safiraLines now 1240 lines; every expression used ≥9×; 0 invalid ids; JSON valid. |

---

## How to keep this file honest

- **At every phase exit**: update statuses for items tagged in that phase. If a row is still PENDING or PARTIAL when the phase ends, either fix it or get explicit user approval to defer.
- **When the user adds new design items**: add them here with phase + status before implementing.
- **Open-ended (💭) items**: track running counts in the relevant section (e.g. "12/30 mechanics shipped").
- **When implementation differs from design**: don't silently rewrite this file — flag the divergence to the user and update DESIGN.md if they approve.
